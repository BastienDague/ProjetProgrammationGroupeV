\documentclass{article}

\usepackage[T1]{fontenc}
\usepackage[french]{babel} 
\usepackage{mathptmx}
\usepackage{geometry}
\usepackage{graphicx}
\graphicspath{{Images/}}
\usepackage{minted}
\usepackage{titlesec}
\setcounter{secnumdepth}{4}
\setcounter{tocdepth}{4}
\geometry{a4paper, margin=2cm}

\usepackage[
    colorlinks=true,        
    linkcolor=black,
    urlcolor=blue,          
    citecolor=green,        
    pdftitle={Rapport du Sprint de Décembre 2026}, 
]{hyperref}

\usepackage[acronym, toc]{glossaries}
\makeglossaries

\newacronym[
    description={Delay-Tolerant Networking. Réseau tolérant aux délais et aux ruptures de connectivité, où les nœuds ne sont pas forcément connectés en permanence. Les données sont transmises de manière opportuniste via des contacts planifiés.}
]{dtn}{DTN}{Delay-Tolerant Networking}

\newacronym[
    description={Contact Plan. Fichier de configuration décrivant les fenêtres de communication planifiées entre nœuds : qui communique avec qui, à quelle heure, pendant combien de temps, avec quel débit et quel délai.
    Lu par la bibliothèque, il est transformé en \gls{mg}}
]{cp}{CP}{Contact Plan}

\newacronym[
    description={Schedule-Aware Bundle Routing. Algorithme de routage pour réseaux DTN qui utilise des métriques de distance (temps d'arrivée, nombre de hops) pour trouver les meilleures routes entre nœuds. \cite{sabr}}
]{sabr}{SABR}{Schedule-Aware Bundle Routing}

\newacronym[
    description={Structure de données centrale du projet représentant le réseau. Contient les nœuds (senders/receivers) et les contacts. Permet plusieurs contacts entre une même paire de nœuds à des moments différents, d'où le préfixe "multi"}
]{mg}{Multigraph}{Multigraph}

\newacronym[
    description={Equivalent du paquet dans les échanges tradionnels, se définit par sa source, sa ou ses destinations,
    sa priorité,  sa taille et son délai d'expiration.}
]{bundle}{Bundle}{Bundle}

\newacronym[
    description={Mécanisme du Dynamic Loader des systèmes UNIX permettant de charger un module dont les fonctions seront utilisées en priorités avant celles de la glibC}
]{ldp}{PRELOAD}{LD\_PRELOAD}

\newacronym[
    description={Bundle Protocole Socket. Type spécifique de socket utilisée dans le cas des DTN \cite{bp}}
]{bp}{BP Socket}{Bundle Protocole Socket}

\newacronym[
    description={Interplanetary Overlay Network. implémentation de référence développée par la NASA pour les protocoles de réseaux tolérants aux délais }
]{ion}{ION}{Interplanetary Overlay Network}

\title{Contributions on Schedule-Aware Bundle Routing \\ \Large Projet de Programmation 2 }
\author{DAGUE Bastien, AL FARAJ Samy, JOUINI Yassine, BROCHARD Vincent \\ \small L3 Informatique - Faculté des Sciences - Université de Montpellier}
\date{2025 - 2026}

\begin{document}

\maketitle
\tableofcontents

\newpage
\printglossary[type=\acronymtype, title={Glossaire et Abréviations}, style=altlist]

\newpage
\section{Introduction}
\subsection{A-SABR}
Notre projet de programmation porte sur la librairie A-SABR, portée par Mr. Olivier De Jonckère du LIRMM.

Le réseau de communication spatial repose sur le \gls{dtn}. Avec ce type de protocole, le routage s'effectue à l'aide d'algorithmes de type \gls{sabr}.\\

La librairie A-SABR implémente donc ce type d'algorithme en proposant une grande modularité et une bonne performance.\cite{sabr}
C'est également pour suivre ce choix de performance que celle-ci est programmée en Rust, langage connu pour son compilateur strict et sa fine gestion de la mémoire.

\begin{figure}[h]
    \centering
    \includegraphics[width=1.1\textwidth]{resume_asabr.png}
    \caption{Schéma récapitulatif d'A-SABR}
    \label{fig:resume_asabr}
\end{figure}

\subsection{Sujet}

L'intérêt pour ce sujet a été immédiat au sein de notre groupe, motivé par la perspective de contribuer à un projet Open Source d'envergure réelle. 
Ce cadre de travail nous a offert l'opportunité de nous initier au langage Rust, 
dont la montée en puissance dans l'industrie logicielle souligne son importance actuelle. 
Pour plusieurs membres de l'équipe aspirant à se spécialiser en informatique système (bas niveau) ou en 
réseaux, ce projet constituait une application concrète et exigeante de nos ambitions professionnelles.\\

Le sujet présentait plusieurs point à aborder : \\
\begin{itemize}
    \item Portage au no-std
    \item Divers tâche de DevOps (Tests, Pipeline)
    \item Création d'un nouvel onglet dans l'interface graphique DT-Chat
    \item Intégration dans le logiciel Hardy \\
\end{itemize}


Au final, tout a été un peu chamboulé, le no-std a été abandonné et l'intégration à Hardy jugée peu intéressante. \\

Nous avons surtout compris qu'on ne dépendait pas seulement de nous, mais qu'il s'agissait d'un immense travail d'équipe.
On commence une tâche, on l'accomplit, on lance une Pull Request, on discute, on modifie. Tout ça prend un certain temps et nécessite beaucoup de coordination.\\

Arrivé à la fin du projet voici les objectifs sur lesquelles nous avons travaillés :  \\

\begin{description}
    \item[Interface graphique DT-Chat :] Développement d'un volet « Options » dans l'interface graphique DT-Chat : 
    Ce module permet désormais de modifier dynamiquement, au cours de l'exécution, 
    l'algorithme de routage utilisé ou le \gls{cp} chargé. \\
    
    \item[Automatisation de test unitaires :] 
    Mise en place d'une pipeline CI/CD robuste pour assurer la pérennité de la bibliothèque. Ce travail a été complété par la mise en place de plusieurs tests unitaires couvrant les modules principaux du projet.
    La pipeline teste la compilation, l'éxecution des tests ainsi que la rédaction de la coverage sous forme d'HTML\\

    \item[Expérimentation Système (BP Socket) :] 
    Pendant le dernier mois du projet, nous avons travaillé sur \gls{bp}.

    \gls{bp}, telle qu'elle a été développée, utilise un module kernel nécessitant 
    les droits root pour être utilisé.
    L'objectif est de permettre l'usage de \gls{bp} sur des systèmes embarqués contraints où les privilèges « root » ne sont pas disponibles. L'expérimentation du mécanisme \gls{ldp}
    vise à intercepter les appels système pour s'affranchir de la dépendance au module noyau actuel.

    Ce dernier objectif est une totale découverte de ce mécanisme offert par le Dynamic Loader et 
    toute avancée même non complète intéresse Mr De Jonckère.
\end{description}

La compilation de la librairie et de l'interface graphique nécessitant divers paquets Linux, notre travail a été 
réalisé soit par WSL, soit directement depuis une distribution Linux.


\newpage
\section{Développements Logiciel : Conception, Modélisation, Implémentation}
\subsection{DT-Chat}
DT-Chat est une application graphique codée en Rust permettant d'illustrer une communication DTN. 
Elle supporte UDP,TCP et \gls{bp}.
\begin{figure}[h]
    \centering
    \includegraphics[width=0.49\textwidth]{DT_Message.png}
    \hfill 
    \includegraphics[width=0.49\textwidth]{DT_Network.png}
    \caption{Onglet Message (gauche) et Events (droite) d'un terminal DT-Chat}
    \label{fig:comparaison_dt}
\end{figure}

A gauche nous pouvons voir l'onglet principal, nous voyons un graphique des différents message envoyée,
un rectangle vert signifie un message envoyée et reçu avec confirmation. En bas il y a la possibilité de choisir 
entre UDP,TCP et \gls{bp}. Egalement, il y a la possibilité de cocher la méthode A-SABR, ceci permet qu'a l'envoie d'un message
l'application prédit et indique le moment où le message devrait être reçu par le recepteur.\\

A droite nous voyons l'onglet Event, celui ci recapitule de manière textuelle toute interaction de l'interface.
A gauche le recapitulatif de l'interaction logicielle et à droite celle du reseau, on voit bien les différents message envoyée et reçu.

Sur cette exemple, il y a eu 3 envois du même terminal, 1 UDP, 1 TCP, 1 \gls{bp}.
\gls{bp} dans le network Event est marqué comme UDP car il s'agit d'une variante d'UDP.\\

L'objectif sur cette interface graphique est de créer un nouvel onglet Options permettant de changer à la volée 
de \gls{cp} et/ou d'algorithme. Cela nécessite donc un travail à la fois coté Backend et FrontEnd\\

Maintenant, détaillons le travail effectuée sur chacun des parties de cette interface.

\subsubsection[DT-Chat Backend]{Backend}
 \label{DTBackend} (Code en annexe \ref{CodeDTBackend})\\\\
Premièrement, on créer une fonction update tiré de la fonction start de l'interface, celle-ci prend en argument un str et un chemin de fichier.
Il faut aussi modifier la fonction de lancement de l'interface, qui ici se nomme try\_init pour qu'elle prenne également un str et un chemin de fichier, on récupère également les valeurs de 
départ qu'on met dans l'appel de la fonction. try\_init à aussi été modifie pour retourner le nombre de node et de contact trouvée dans le \gls{cp} chargé.\\

La fonction update prend donc en entrée un str qui correspond au nom de l'algorithme utilisée, et 
un Path sous forme de string qui correspondra au chemin jusqu'au \gls{cp}.

Elle va ensuite faire un match sur \texttt{"PredictionConfig::try\_init(path.clone(), algo)"} qui retournera donc un Result,
pour empêcher un crash total de l'interface au moindre soucis.

Si Ok alors on met à jour la configuration qu'on envoie au Frontent et on l'affiche dans l'App Event,
si Err, on retourne l'erreur et on l'affiche dans l'App Event.

Etant basé sur A-SABR, il faut régulièrement faire de petite modif dûe à des refactorisation de fonction ou de
l'ajout de Result remplaçant des Options dans la librairie. \\

Toute ces modifications sur la partie Backend ont fait l'objet de 2 Pull request acceptées de Vincent pour 52 lignes de code ajoutée (PR \texttt{\#6} et \texttt{\#7}).

\subsubsection[DT-Chat Frontend]{Frontend}
\label{DTFrontend} (Code en annexe \ref{CodeDTFrontend})\\

L'interface DT Chat peut être vue comme un modèle MVC en programmation Web, le Modèle serait le Backend,
la Vue toute les interactions possibles avec l'interface, et le contrôleur le code qui résulte de ces interactions.

Ici le but est donc d'afficher ce fameux onglet Options et d'envoyer les différents ordre au Backend suivant les actions faite.

Le raccourci ci-dessus permet d'accéder au code la vue Options, il se résume simplement en un bouton qui ouvre un explorateur
pour choisir son \gls{cp} et une ComboBox pour le choix d'algorithme, dès qu'une action est réalisé pour un ou pour l'autre,
l'interface se met directement à jour et affiche les nouveaux paramètres chargées dans l'App Event.

Une spécificité existe pour le choix d'algorithme, certain algorithme sont conditionnées à l'activation ou non
de "features", des fonctionnalités qu'on choisit d'activer ou non lors du lancement de l'interface, on peut voir
dans le code que la ComboBox s'adapte aux choix de features choisies lors de la compilation.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.49\textwidth]{DT_Algos.png}
    \hfill  
    \includegraphics[width=0.49\textwidth]{DT_Changes.png}
    \caption{Onglet App Event mis à jour (gauche) et ComboBox adaptatif (droite) d'un terminal DT-Chat}
    \label{fig:dt_changes}
\end{figure}

L'interface graphique à sur cette exemple illustrée été compilé avec la feature \texttt{contact\_work\_area}, 
donc la ComboBox s'est adaptée. J'ai ensuite sélectionnée l'algorithme \texttt{VolCgrNodeParenting} ainsi que
chargé le \gls{cp} \texttt{ion\_format.cp} et nous voyons bien dans l'App Event que le changement est effectif
et que le nombre de node et de contacts à bien été détectée.\\

Dans les fait, l'algorithme choisit ne change pas réellement ce que l'interface nous montre lors de la réception
ou l'envoi de message, il s'agit surtout ici de préparer de possible futur ajoux car le Backend lui comprend totalement les changement effectués.\\

Pour finir, un soucis à été détecté en ce qui concerne le changement de \gls{cp}.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.30\textwidth]{Contact_Plan_DT.png}
    \caption{Contact plan "A-SABR/examples/contact\_plans/ion\_format.cp"}
    \label{fig:dt_cp_ion_format}
\end{figure}

Ci-dessus, on a le format classique de \gls{cp} que l'interface comprend correctement :

Ce \gls{cp} au format ION, format de référence de la NASA, définit un réseau de 6 nœuds avec une topologie linéaire 1→2→3→4→5→6. 
Chaque lien est actif entre t=+60s et t=+7260s.

Les lignes contact définissent les fenêtres de communication avec leurs débits respectifs
 — le lien 1→2 dispose de 10 000 octets/s, 2→3 de 15 000,
et ainsi de suite jusqu'à 30 000 octets/s pour 5→6.

Les lignes range définissent le délai de propagation physique du signal sur chaque lien
 — 10 secondes entre les nœuds 1 et 2, 15 entre 2 et 3, jusqu'à 30 secondes pour 5→6,
  simulant des distances croissantes entre les nœuds
 comme on pourrait en trouver en situation réelle.

Hors il existe plusieurs autre format. 
De base il n'y avait pas l'affichage du nombre de nodes et de contact détectée lors du chargement d'un contact plan,
mais j'ai trouvée étrange que l'appli prennent sans erreurs tous type de fichiers et donc, après investigation et discussion, on à décider 
de faire afficher le nombre de contact et de noeuds. La plupart des contact plan qui ne sont pas en format "ION" comme ci-dessus
ne sont pas du tous compris par l'application. Elle ne crash pas mais affiche simplement 0 contacts et 0 noeuds.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.55\textwidth]{Contact_Plan_ASABR.png}
    \caption{Contact plan "A-SABR/examples/contact\_plans/asabr\_format\_dynamic.cp"}
    \label{fig:dt_cp_asabr}
\end{figure}

Ci-dessus un exemple de contact plan au format ASABR qu'il ne lit pas du tous. On voit que les contacts précisent le contact Manager à adoptée
et qu'ils peuvent être différent au sein du même \gls{cp}.

Pour régler le soucis, il aurait fallu développer tous un parser qui s'adapte au type de \gls{cp} chargé. Dans les fait c'était réalisable 
mais cela prenait beaucoup de temps sachant que le coeur du sujet est A-SABR donc on a juste rajouté le compteur de noeuds et de contacts.\\

Toutes ces modifications sur le FrontEnd ont fait l'objet d'une Pull Request de Vincent acceptée pour 145 lignes de codes ajoutée (PR \texttt{\#13}). 

\subsection[Pipeline]{Pipeline CI/CD}
\subsubsection{Première version du fichier:}

L'une des tâches principales de notre projet était d'apporter des pipelines d'automatisation pour faciliter le travail futur de l'équipe. 
Pour ce faire, il a fallu déterminer ce dont le projet avait besoin et comment le mettre en place grâce aux outils propres au langage Rust. 
Après en avoir discuté avec Mr De Jonckère, nous avons décidé de travailler sur 4 aspects principaux: le format du code, la validité, l'optimisation et son bon fonctionnement grâce aux tests.
Commençons par détailler la première version de notre travail (à noter que le fichier présenté ensuite est commun à \texttt{A-SABR}, \texttt{dtchat-egui} et \texttt{dtchat-backend}).
\label{CI/CD} (Code en annexe \ref{CodeCIv1})

Ce fichier ci.yml crée dans le dossier \texttt{.github/workflows} se présente de la manière suivante:

\begin{itemize}
    \item Les déclencheurs: comme nous pouvons le voir, il est précisé que le fichier doit se lancer
          dans les branches \texttt{main}, \texttt{develop} et toutes les branches commençant par \texttt{feature/} afin d'offrir à notre équipe
          localement le travail de cette Pipeline. 
    \item L'environnement: afin d'effectuer notre travail, il nous faut travailler dans un environnement Linux. Nous utilisons donc \texttt{ubuntu-latest} pour la richesse de ses outils.
          Cependant, un problème est rapidement apparu: utiliser cet environnement tel quel fait appel à bien trop de ressources, ainsi nous
          avons décidé d'utiliser le container \texttt{rust-slim} car c'est un bon compromis entre légèreté et la présence des outils nécessaires.
    \item La préparation du travail: il nous faut ensuite configurer tous les outils que nous utiliserons ensuite.
    
          Tout d'abord, \texttt{actions/checkout@v4} s'occupe de récupérer le code source du projet, ensuite pour avoir
          la version stable du compilateur Rust nous utilisons \texttt{dtolnay/rust-toolchain@stable} avec deux outils supplémentaires indispensables: \texttt{rustfmt} et \texttt{clippy} dont nous reparlerons.
          Enfin, pour avoir de meilleurs performances et un temps d'exécution réduit nous utilisons \texttt{swatinem/rust-cache@v2}, cela nous permet sauvegarder les dépendances téléchargées pour les réutiliser ensuite.
    \item Le travail effectué: maintenant que tout est prêt, nous pouvons utiliser nos commandes pour faire le travail attendu. 
    
          Pour commencer, \texttt{cargo fmt --all -- --check} est l'outil qui vérifie
          si le code respecte bien le format standard Rust, nous remarquons que l'appel de cette commande est suivi par \texttt{continue-on-error: true} 
          cela veut dire que si une erreur de format est détectée elle est signalée mais n'arrête pas le workflow.


          Le second outil utilisé est \texttt{cargo clippy}. Il nous permet de vérifier beaucoup de choses cruciales pour le projet:
          la bonne compilation du code et si malgré une compilation réussie il reste des erreurs de logique. Ces erreurs peuvent être de différents types:
          trop d'arguments dans une fonction, code inutile ou encore des erreurs de sécurité. 
          Pour terminer, nous ajoutons \texttt{cargo test} pour, comme son nom l'indique, lancer nos tests implémentés.


          On y ajoute les cibles \texttt{--lib}, \texttt{--bins} et \texttt{--tests} afin de tester respectivement
          la partie bibliothèque du code, les exécutables et les fichiers du dossier \texttt{tests/}.
\end{itemize}


Avec cette première version, nous répondons bien aux problèmes évoqués plus tôt. 
Cependant, après discussion lors de l'ouverture de la Pull Request, il était clair que ce fichier manquait d'optimisation et n'était pas assez strict (les erreurs fmt et clippy sont tolérées). 
Tout est rassemblé en un seul job rendant plus difficile le diagnostic d'une potentielle erreur et aucun commentaire ne permet aux personnes ne connaissant pas cette syntaxe de comprendre le fonctionnement du fichier.

\subsubsection{Optimisation du fichier de base:}

Après avoir reçu les premiers retours, voici ce qui en est ressorti: la manque de clarté du fichier,
le fait d'autoriser certaines erreurs, un temps d'installation des dépendances pouvant être raccourcis et
la possibilité de rajouter un job de coverage. Afin de répondre aux attentes, voici comment le fichier a été corrigé (à noter qu'ici les modifications ne concernent que le fichier lié à \texttt{A-SABR}, nous reparlerons de \texttt{dtchat} dans la section suivante):
\label{CI/CD} (Code en annexe \ref{CodeCIv2})

\begin{itemize}
    \item Grâce à la partie \texttt{concurrency}, nous permettons à Github d'arrêter l'exécution 
          en cours de la Pipeline si un nouveau commit est poussé afin que seul ce dernier profite des vérifications. 
          Cela permet de gagner un peu de temps si on pousse un commit avec une erreur puis qu'on la corrige directement dans le commit suivant.
    \item Pour répondre aux problèmes de clarté, nous avons extrait les différentes vérifications en 4 jobs: \texttt{fmt}, \texttt{clippy}, \texttt{tests} et \texttt{coverage}.
          De plus, grâce à la balise \texttt{name}, nous ajoutons un titre à chaque nouvelle commande afin de permettre à d'autres de mieux comprendre le travail effectué.
    \item Il fallait également rendre la Pipeline moins permissive, nous avons donc retiré la mention \texttt{continue-on-error: true} dans le job \texttt{fmt} afin de faire en sorte que les erreurs de format
          deviennent bloquantes. Concernant clippy, on lui ajoute les cibles \texttt{--all-targets}, \texttt{--all-features} et \texttt{-- -D warnings}.
          Ces ajouts permettent respectivement de lancer clippy sur l'intégralité du code, d'utiliser toutes les features disponibles et de rendre les avertissements bloquants.
    \item Concernant la partie test, nous avons juste ajouté la cible \texttt{--verbose} à \texttt{cargo test} afin d'avoir des messages d'erreurs plus détaillés.
    \item Enfin, nous avons ajouté un job de coverage. Le but de ce travail est de générer un fichier html qui permettra de voir où en est la couverture de code globale du projet.
          Pour ce faire, nous utilisons l'outil tarpaulin. Le fichier généré est mis dans les artefacts GitHub et sauvegardé pendant 7 jours. 
          Ce job étant le plus demandant en ressources, nous avons décidé avec Mr De Jonckère de ne l'exécuter que lorsqu'il est appelé sur la branche main grâce à \texttt{if: github.ref == 'refs/github/main}
          Dans les captures d'écran suivantes, nous pouvons voir le poucentage de couverture pour chaque dossier et fichier.
          Quand nous cliquons sur un fichier, les lignes couvertes apparaissent en vertes et celles non couvertes en rouge afin de mieux guider le développeur implémentant les tests.
\end{itemize}

Il est intéressant de remarquer également les lignes du type \verb|if: ${{ !env.ACT }}|.
ACT est un outil installé localement permettant de lancer le fichier ci.yml directement sur notre machine sans l'envoyer vers GitHub permettant de garder un historique plus propre sur le dépôt.
\begin{figure}[h]
    \centering
    \includegraphics[width=0.55\textwidth]{CoverageHomePage.png}
    \includegraphics[width=0.55\textwidth]{CoverageExemple.png}
    \caption{Captures d'écrans du fichier de coverage géneré}
    \label{fig:CoveragePage}
\end{figure}

Grâce à ces ajouts, nous avons implémenté un travail de meilleure qualité tout en ajoutant des fonctionnalités non prévues au début.
Cependant, la bonne utilisation de ces fonctionnalités dépend d'un envoi vers le serveur GitHub. Nous avons bien essayé de contourner ce problème via ACT
mais cet outil est soumis au même temps d'exécution que celui sur le serveur ce qui peut devenir très chronophage lorsqu'il faut le tester plusieurs fois.

\subsubsection{Implémentation de hooks pour la pipeline}

Lors de la suite de nos discussions, Mr De Jonckère nous a suggéré d'utiliser le principe de pre-commit grâce aux hooks.
C'était à ce moment du travail une notion que nous ne maîtrisions pas, ce qui nous a pris un temps de recherche pour bien adapter cette solution au projet.
Le principe est simple, comme le nom "pre-commit" l'indique l'analyse interviendra juste avant de préparer le commit du travail effectué et pourra le bloquer s'il ne respecte pas certaines règles. 
On procèdera donc à une analyse locale avant envoi puis une analyse après envoi.
Pour cela, il nous a fallu créer un fichier \texttt{.pre-commit-config.yaml} et modifier notre \texttt{ci.yml}.
Voyons plus en détail ces nouveaux fichiers en
commençant par le fichier de pre-commit: \label{CI/CD} (Code du fichier de pre-commit en annexe \ref{CodeCIprecommit})

\begin{itemize}
    \item Pour travailler nous utiliserons 2 repositories, le premier utilise des outils disponibles via un dépôt GitHub, le second utilise notre système local.
    
          Le premier repo, nous permet l'utilisation de 4 outils: \texttt{trailing-whitespace} qui supprime les grands espaces dans le code,
          \texttt{end-of-file-fixer} met à la fin de tous les fichiers un unique saut de ligne,
          \texttt{check-yaml} vérifie la bonne syntaxe des fichiers .yml et .yaml et
          \texttt{check-added-large-files} empêche de faire des commits trop lourds (ajout accidental d'un dossier de fichiers compilés).

          Le second repo est utilisé afin d'utiliser les outils Rust avec lesquels nous avons travaillé jusqu'ici
          à savoir cargo fmt et cargo clippy, l'appel des ces outils reste le même que précedemment. 
          Nous pouvons relever un détail qui a son importance qui est la ligne \texttt{language: system}.
          En effet, cette ligne précise que le fichier devra aller chercher les outils Rust dans la toolchain locale de l'utilisateur,
          nous pouvions également demander d'aller chercher les outils dans un dépôt en ligne cependant nous avons
          considéré que nos machines sont régulièrement mises à jour donc les problèmes de différences de versions ne gêneront pas et
          que ce serait plus simple à maintenir si le dépôt venait à ne pas plus être mis à jour.
\end{itemize}

Pour profiter des fonctionnalités de ce fichier, on peut utiliser la commande \texttt{pre-commit run --all-files} qui lancera tous les jobs de pre-commit sur l'ensemble des fichiers.
\begin{figure}[h]
    \centering
    \includegraphics[width=0.55\textwidth]{run-pre-commit-fail.png}
    \caption{Exemple d'un résultat de lancement de la commande avec détection d'erreurs}
    \label{fig:PreCommitFail}
\end{figure}

Mais le plus intéressant avec cet outil est la possibilité de bloquer un commit défectueux.
Après avoir installé pre-commit sur sa machine, les jobs se lanceront sur les fichiers qui ont été staged au moment de taper \texttt{git commit ...}
Ainsi, grâce à cela il est impossible d'envoyer sur le dépôt un commit qui ne respecte pas les règles établies.

Concernant le fichier \texttt{ci.yml}\label{CI/CD} (Code du fichier ci.yml en annexe \ref{CodeCIyml}), un changement majeur est à relever: 
les jobs lancés localement par le fichier de pre commit sont rappelés par la ligne \texttt{uses: pre-commit/action@v3.0.1}
dans un job nommé "lint". Grâce à cela on assure une stabilité plus importante du code 
et une meilleure optimisation future car le fichier \texttt{ci.yml} utilise le contenu du fichier de pre-commit.

Le reste du fichier présente peu de changements par rapport à la version précédente, nous lançons les tests dans un job dédié suivi du job de couverture des tests.
\\[1cm]
Pour conclure cette partie, nous avons su au fur et à mesure de l'avancée du travail
relever nos erreurs et les corriger tout en prenant en compte les remarques et besoins des autres collaborateurs du projet
ce qui nous a permis de découvrir de nouvelles notions et de consolider nos connaissances déjà existantes dans ce domaine.

Notre travail a finalement été accepté dans le dépôt de Monsieur De Jonckère (PR\texttt{\#30}) et peut maintenant servir à toute l'équipe.


\subsection{Test A-SABR}
\subsubsection[Module : Contact Manager]{Contact Manager}

Les \textit{Contact Managers} sont les composants responsables de la planification des transmissions sur un contact réseau dans A-SABR. Leur rôle est de déterminer si un bundle peut être transmis à un instant donné, de calculer les dates associées à cette transmission, puis de mettre à jour l’état du contact lorsque celle-ci est effectivement planifiée.

Tous les gestionnaires de contacts implémentent une interface commune reposant principalement sur deux opérations :
\begin{itemize}
    \item \texttt{dry\_run\_tx}, qui simule une transmission sans modifier l’état interne du contact,
    \item \texttt{schedule\_tx}, qui planifie effectivement la transmission et met à jour les ressources disponibles.
\end{itemize}

Plusieurs implémentations de \textit{Contact Manager} coexistent dans le projet afin de proposer différents niveaux de précision et de modélisation. Elles sont regroupées en deux grandes familles : les gestionnaires \textit{Legacy}, reposant sur des modèles historiques plus simples, et les gestionnaires basés sur la segmentation, offrant une représentation plus fine des contacts.

\paragraph{Legacy :}~\\\\
Les gestionnaires \textit{Legacy} correspondent aux premières implémentations de gestionnaires de contacts présentes dans A-SABR. Ils sont considérés comme dépréciés car ils reposent sur une modélisation plus simple du contact, avec moins de granularité que les approches plus récentes comme la segmentation. Ils restent présents dans la librairie pour des raisons de compatibilité et de comparaison, mais ne constituent plus l’approche principale utilisée pour représenter finement les transmissions.

Ces gestionnaires restent néanmoins importants à tester, car ils font encore partie du code existant et peuvent être utilisés comme référence pour vérifier le comportement d’autres modules.

\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{Images/legacy_managers_comparison.png}
\caption{Comparaison des comportements des gestionnaires Legacy (ETO, EVL, QD).}- \\
\label{fig:legacy_managers_comparison}
\end{figure}


Le sous-module contient trois implémentations : EVL, QD et ETO. Ces implémentations sont générées à l'aide d'une macro qui prend différents paramètres :

\begin{itemize}
    \item \texttt{\$add\_delay} : indique si le délai de queue doit être pris en compte pour le calcul de \texttt{tx\_start}
    \item \texttt{\$auto\_update} : indique si la queue est mise à jour automatiquement après un \texttt{schedule\_tx} ou manuellement
    \item \texttt{\$prio\_count} : donne le nombre de niveaux de priorités
    \item \texttt{\$with\_budget} : indique si des budgets sont appliqués lorsque des priorités sont présentes
\end{itemize}

EVL, QD et ETO se distinguent par leurs valeurs de ces paramètres. EVL est le manager le plus simple : il se contente de consommer le volume d'un bundle à chaque \texttt{schedule\_tx} sur un contact. La queue est mise à jour automatiquement et il n'y a pas de délai. QD est similaire à EVL, à la différence que \texttt{tx\_start} est décalé depuis le début du contact en fonction du volume déjà réservé. Enfin, ETO est le plus complexe des trois : le décalage de la queue doit être géré manuellement et débute à \texttt{at\_time}, soit le temps courant.

Chacun de ces fichiers instancie la macro pour générer trois variantes : un manager de base sans priorités, un avec priorités, et un autre avec priorités et budgets, un budget étant la quantité maximale de données qu'un bundle peut réserver sur un contact.\\\\



Pour tester ces implémentations, et sachant qu'elles partagent une base de tests en commun, nous avons mis en place des macros de tests dans un fichier \texttt{test\_helpers.rs} afin d'éviter la duplication de code. Quatre macros ont été définies :

\begin{itemize}
    \item \texttt{generate\_common\_tests!} : regroupe les tests communs à tous les managers, en testant le comportement de \texttt{dry\_run\_tx} et \texttt{schedule\_tx} en termes de timing, de volume et de conditions limites.

    \item \texttt{generate\_auto\_update\_tests!} : teste les comportements liés à la mise à jour automatique de la queue, notamment la saturation du contact et l'isolation des priorités. Ces tests ne s'appliquent pas à ETO puisque \texttt{\$auto\_update=false} pour lui.

    \item \texttt{generate\_budget\_tests!} : vérifie que les limites de budget par priorité sont bien respectées.

    \item \texttt{generate\_budget\_auto\_update\_tests!} : teste l'indépendance des budgets entre priorités. Ces tests ont été séparés de \texttt{generate\_budget\_tests!} car ils nécessitent \texttt{auto\_update=true}, ce qui exclut ETO. \\
\end{itemize}

En complément de ces tests communs, des tests spécifiques ont été ajoutés afin de valider les comportements propres à chaque implémentation.

Pour \texttt{ETO}, les tests spécifiques vérifient que la file d’attente n’est pas automatiquement mise à jour après une planification. Ainsi, plusieurs appels successifs à \texttt{schedule\_tx} peuvent produire le même résultat tant qu’aucune mise à jour manuelle de la file n’est effectuée.

Pour \texttt{EVL}, les tests permettent de confirmer que l’occupation de la file d’attente influence la capacité disponible, sans modifier directement la date de début de transmission. Autrement dit, le volume restant diminue, mais \texttt{tx\_start} reste indépendant de cette occupation.

Enfin, pour \texttt{QD}, les tests valident que l’occupation de la file peut décaler la date effective de début de transmission. Ils vérifient également qu’une transmission peut devenir impossible si ce décalage repousse la fin de transmission au-delà de la fenêtre du contact.

L’ajout de ces tests spécifiques permet donc de compléter la couverture existante sur les gestionnaires \textit{Legacy}, tout en documentant plus clairement les différences de comportement entre \texttt{ETO}, \texttt{EVL} et \texttt{QD}.\\

Toute ces modifications sur la partie Legacy ont fait l'objet de 1 Pull request en attente d'acceptation de Samy/Yassine pour 502 lignes de code ajoutées (PR \texttt{\#54})

\paragraph{Segmentation :}~\\\\

Le module \textit{Segmentation} propose une modélisation plus fine des contacts que les gestionnaires \textit{Legacy}, en découpant un contact en plusieurs intervalles temporels indépendants. Chaque segment peut ainsi posséder ses propres caractéristiques de transmission, comme le débit ou le délai.
\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{Images/legacy_vs_segmentation.png}
\caption{Illustration du découpage d’un contact en segments temporels indépendants.}
\label{fig:segmentation_vs_legacy}
\end{figure}

Le gestionnaire de segmentation repose sur trois structures principales : les segments de débit (\texttt{rate\_intervals}), les segments de délai (\texttt{delay\_intervals}) et les intervalles libres (\texttt{free\_intervals}), représentant les portions du contact encore disponibles pour la transmission. Chaque segment est défini par un intervalle temporel et une valeur associée.

Deux opérations principales sont utilisées :
\begin{itemize}
    \item \texttt{dry\_run\_tx} : simulation d’une transmission sans modification de l’état interne,
    \item \texttt{schedule\_tx} : planification effective de la transmission avec mise à jour des intervalles libres.
\end{itemize}

Lorsqu’une transmission est demandée, le gestionnaire recherche un intervalle libre compatible avec l’instant considéré (\texttt{at\_time}). Si cet instant est situé avant le début du contact, la transmission démarre au début du premier intervalle valide ; sinon, elle commence à \texttt{at\_time}. La durée de transmission est ensuite calculée en tenant compte des segments de débit traversés, ce qui permet à une transmission de s’étendre sur plusieurs segments successifs si nécessaire.

\begin{figure}[H]
\centering
\includegraphics[width=0.55\textwidth]{Images/free_intervals_evolution.png}
\caption{Découpage d’un intervalle libre après planification d’un bundle.}
\label{fig:free_intervals}
\end{figure}

La planification d’un bundle entraîne une mise à jour dynamique des intervalles libres. Comme illustré ci-dessus, un intervalle initial peut être découpé en plusieurs sous-intervalles afin de conserver une représentation cohérente des ressources disponibles pour les transmissions futures.

Les tests implémentés dans le fichier \texttt{seg.rs} ont permis de valider plusieurs comportements essentiels :
\begin{itemize}
    \item insertion d’un bundle dans un intervalle libre,
    \item enchaînement de plusieurs transmissions successives,
    \item gestion de débits variables,
    \item prise en compte correcte de l’instant \texttt{at\_time}.
\end{itemize}

Ces tests garantissent la cohérence entre les fonctions \texttt{dry\_run\_tx} et \texttt{schedule\_tx}, ainsi que la mise à jour correcte des intervalles libres après chaque planification.

\vspace{0.3cm}

\textbf{Difficultés rencontrées :}

Plusieurs difficultés ont été rencontrées lors de l’implémentation des tests de segmentation. La manipulation d’intervalles temporels a notamment entraîné des erreurs d’indices, en particulier lors de l’accès à des segments inexistants. Ces problèmes ont été corrigés en ajoutant des vérifications sur les bornes des intervalles et en sécurisant les accès aux structures de données.

La gestion des cas limites s’est également révélée délicate, notamment pour les situations où l’instant de transmission se situe en dehors des intervalles définis ou à leurs frontières. Certains tests ont dû être ajustés afin de respecter strictement les contraintes temporelles du modèle.

Enfin, des contraintes liées aux outils d’intégration continue ont été rencontrées. Les hooks \textit{pre-commit}, incluant \texttt{cargo fmt} et \texttt{cargo clippy}, ont empêché certains commits en raison de problèmes de formatage ou de warnings. Ces difficultés ont été résolues en appliquant les corrections nécessaires et en respectant les règles imposées par le projet.

Ces difficultés ont permis de mieux comprendre les invariants du modèle et de renforcer la robustesse des tests implémentés.\\

Toute ces modifications sur la partie Segmentation ont fait l'objet de 1 Pull request acceptée de Yassine pour 300 lignes de code ajoutées (PR \texttt{\#53})

\paragraph{PrioritySegmentation :}~\\\\
Le module \texttt{PrioritySegmentation} abrégé en \texttt{pseg}, quant à lui, reprend exactement le même principe 
que le module \texttt{Segmentation} mais ajoute, comme son nom l'indique, une gestion de la priorité des paquets.
Le principe central de ce module est le suivant: un paquet de plus haute priorité peut remplacer un segment occupé par un paquet
de plus petite priorité. L'intérêt de cette approche est de pouvoir mettre en place une hiérarchie dans l'importance des paquets envoyés,
dans le cas du routage spatial, on préfèrera faire passer un message d'urgence avant un retour d'analyse quelconque, c'est ce que permet \texttt{pseg}.
Ci-dessous un schéma d'explication du fonctionnement de \texttt{pseg}.
\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\textwidth]{schémapseg.png}
    \caption{Schéma de fonctionnement de PrioritySegmentation}
    \label{fig:SchemaPseg}
\end{figure}

On observe tout d'abord que les segments libres sont notés d'une priorité -1 tandis que les paquets peuvent avoir
une priorité allant de 0 à 2. Un paquet de plus haute priorité ne remplace pas forcément tout le paquet de priorité inférieure,
on peut voir qu'au moment de l'insertion du paquet de priorité 2 en rouge seule la partie dont il avait besoin a été allouée et n'a pas remplacé
l'entièreté du segment occupé par le paquet de priorité 1.

Le fonctionnement de ce module est assez similaire à celui de \texttt{Segmentation} expliqué précedemment. 
Nous retrouvons les segments de débit et de délai pour construire l'univers dans lequel nous travaillerons. Cependant
nous n'utilisons pas ici des intervalles libres mais des segments appelés \texttt{bookings} qui seront initialisés avec 
une date de début et de fin, une priorité -1 pour indiquer qu'ils sont libres.

Nous retrouvons le modèle des deux fonctions centrales: \texttt{dry\_run\_tx} pour la simulation et \texttt{schedule\_tx}
pour l'application effective des modifications. L'analyse algorithmique du module sera traitée dans la partie 3.

Dans le cadre du projet, nous avons apporté certaines modifications au module.
Une procédure permettant de fusionner les \texttt{bookings} adjacents de même priorité a été implémenté
et une couverture de tests importante a été faite. De plus, durant cette phase de tests, un bug a été descelé et résolu: 
il existait un cas où l'insertion échouait quand elle devait être faite à la fin du segment alors qu'elle devait réussir.
Détaillons maintenant le travail implémenté pour créer nos tests. Pour avoir des tests satisfaisants, nous devions
vérifier l'égalité des retours des fonctions \texttt{dry\_run\_tx} et \texttt{schedule\_tx} et couvrir tous les différents scénarios qu'il était possible de rencontrer.
Au début, des tests simples ont été faits: une fonction de test par scénario. Cette approche fonctionnait et offrait une bonne couverture
mais elle pouvait être très largement simplifié pour les futurs utilisateurs.
Après en avoir discuté avec Monsieur De Jonckère, nous avons mis au point une fonction centrale \texttt{start\_test}
qui s'occupe de construire l'environnement et de lancer le test associé et des fonctions qui appelaient notre \texttt{start\_test} avec
des valeurs différentes, expliquons le principe de cette fonction: \label{pseg} (Code de la fonction \texttt{start\_test} en annexe \ref{CodeStartTest})
\begin{itemize}
    \item Le principe global: la fonction prend 3 paramètres qui sont \texttt{input} une liste d'objets de type InputSeg 
    afin de construire l'environnement de base, \texttt{output} qui est une liste de bookings de type OutpuSeg représentant les bookings attendus par le retour de la fonction
    et \texttt{requests} une liste de tuples respectant la norme suivante: en premier, le paquet de type \texttt{Bundle} à insérer, ensuite le moment où il doit être inséré (de type f64) et
    un booléen mis à \texttt{true} si on s'attend à ce que l'inserion réussise et \texttt{false} sinon.
    \item La construction de l'environnement: quand la fonction est appelé elle va utiliser la liste \texttt{input} pour construire l'environnement de test.
    Par exemple, avec un input de type let input = vec![InputSeg::Delay(0.0, 200.0, 4.0),InputSeg::Rate(0.0, 200.0, 100.0),]; la fonction va faire un matching sur les différents champs
    pour construire les segments utiles, elle crée le segment de délai avec les valeurs 0, 200 et 4 et le segment de débit avec les valeurs 0, 200 et 100. Si il n'y a pas de champ \texttt{booking} dans l'input
    alors on part d'une liste de \texttt{bookings} vide sinon on peut partir d'une liste avec des \texttt{bookings} déjà existants.
    \item Les tests effectués: maintenant que l'environnement est crée, nous pouvons commencer les insertions de paquets.
    Pour chaque paquet dans la liste \texttt{requests} on va récupérer les résultats de \texttt{dry\_run\_tx} et de \texttt{schedule\_tx} afin de tester s'ils sont égaux.

    Ensuite, on vérifie que l'insertion du paquet est bien en accord avec le booléen représentant la réussite de l'insertion, si le test renvoie une erreur c'est que le paquet a été inséré alors
    qu'on s'attendait à un échec ou le contraire. 

    Enfin, on construit la sortie réelle de la fonction et on vérifie qu'elle est bien équivalente à la sortie attendue représentée par le champ \texttt{output}.
\end{itemize}

La force de cette fonction est qu'elle permet de faire 3 tests communs en un seul appel pour différents scénarios. 
De plus, si des collaborateurs souhaitent écrire de nouveaux tests, ils n'auront besoin que de construire les valeurs attendues
sans avoir à réecrire à chaque fois les mêmes principes de tests que les précédents.



\subsubsection[Module : Pathfinding]{Pathfinding}
Le rôle de ce module est de calculer, à partir d'un \gls{bundle} et d'un instant donnée, la meilleurs route vers
une destination en tenant compte des contraites temporelles de chaque contact.
Il expose un Trait, \texttt{PathFinding}, implémentée par trois variantes : Node, Contact et Hybride, détaillée ci-après,
ainsi qu'un sous module \texttt{limiting\_contact} qui permet de supprimer temporairement le contact le plus limitant d'une route 
pour forcer la découverte de routes alternatives. 

Chaque variante est instanciée avec une métrique de distance, soit \textbf{Hop} qui minimise le nombre de saut, 
soit \textbf{\gls{sabr}} qui minimise le temps d'arrivée.
\paragraph{Try\_make\_hop :}~\\\\

\texttt{try\_make\_hop} est la fonction centrale du module de pathfinding. Son rôle est de déterminer, depuis un nœud source à un instant donné, le meilleur contact disponible pour acheminer un bundle vers un nœud voisin en un seul saut. Elle prend en entrée un \texttt{RouteStage} source qui représente l'état courant du routage (temps d'arrivée au nœud, délai cumulé, expiration), une liste de contacts triés par ordre de début, ainsi que les nœuds émetteur et récepteur. Elle retourne soit le meilleur saut trouvé, soit \texttt{None} si aucun contact ne permet la transmission.

La fonction parcourt chaque contact fourni en entrée à partir d'un \texttt{first\_contact\_index}, ce qui permet un \textit{lazy pruning} : les contacts déjà dépassés par un saut précédent dans Dijkstra n'ont pas besoin d'être reconsidérés. Pour chaque contact, elle appelle \texttt{dry\_run\_tx} du Contact Manager pour simuler la transmission, puis retient le contact permettant la date d'arrivée \texttt{rx\_end} la plus rapide. Elle s'arrête dès que le début d'un contact est postérieur au meilleur \texttt{rx\_end} déjà trouvé, les contacts étant supposés triés. Si un contact est retenu, elle construit un nouveau \texttt{RouteStage} avec les métadonnées mises à jour : \texttt{hop\_count} incrémenté, \texttt{cumulative\_delay} mis à jour et \texttt{expiration} calculée comme le minimum entre la fin du contact et l'expiration propagée depuis la source.

\texttt{try\_make\_hop} intègre également des \textit{features}, c'est-à-dire des portions de code activées uniquement si la feature est spécifiée au lancement. On trouve ainsi \texttt{contact\_suppression}, \texttt{node\_tx} et \texttt{node\_rx} qui permettent de faire échouer le \texttt{dry\_run} et donc de renvoyer \texttt{None}, ainsi que \texttt{node\_proc} qui permet le traitement du bundle avant l'émission, pouvant décaler le temps d'envoi.

\paragraph{Tests de try\_make\_hop :}~\\\\

Afin de tester cette fonction, nous avons créé un \textit{mock} de \texttt{NodeManager} dans le fichier \texttt{test\_helpers} du dossier pathfinding. En effet, nos tests doivent contrôler le comportement des nœuds émetteurs et récepteurs avec les features \texttt{node\_tx} et \texttt{node\_rx}, contrairement aux autres fichiers qui n'ont besoin que de \texttt{NoManagement}. Ce mock permet d'implémenter et de contrôler un \texttt{NodeManager} avec quatre fonctions : \texttt{accepting()} qui accepte toujours, \texttt{refusing\_rx()} et \texttt{refusing\_tx()} qui rejettent la transmission d'un côté, et \texttt{processing(delay)} qui simule un délai de traitement du bundle. Les fonctions helpers telles que \texttt{make\_node}, \texttt{make\_contact}, \texttt{make\_source} et \texttt{make\_bundle} ont été rendues génériques sur \texttt{NM} pour pouvoir être partagées entre les deux types de manager.

Les tests mis en place couvrent les cas suivants :

\begin{itemize}
    \item \textbf{Cas de base} : appel avec une liste de contacts vide, un \texttt{first\_contact\_index} dépassant le slice, ou un bundle trop volumineux pour un contact donné. On vérifie que la fonction renvoie bien \texttt{None} dans ces cas.

    \item \textbf{Transmission valide} : test avec un seul contact compatible. On vérifie que \texttt{Some} est bien retourné.

    \item \textbf{Lazy pruning} : test avec \texttt{first\_contact\_index=1} qui saute un contact valide. On vérifie que le résultat correspond bien au second contact et non au premier.

    \item \textbf{Feature \texttt{contact\_suppression}} : si tous les contacts sont supprimés, la fonction doit renvoyer \texttt{None}. Si un seul contact parmi deux est supprimé, le second doit bien être choisi.

    \item \textbf{Mock NodeManager (\texttt{node\_tx}, \texttt{node\_rx}, \texttt{node\_proc})} : ces trois tests ne peuvent pas utiliser \texttt{HopContext} car ils nécessitent des managers différents sur \texttt{tx} et \texttt{rx}. On vérifie qu'un nœud refusant l'émission ou la réception bloque le saut, et qu'un délai de traitement décale correctement le \texttt{sending\_time} et donc le \texttt{at\_time} dans le résultat.

    \item \textbf{Sélection du meilleur contact} : test avec 4 contacts dont certains sont élagués par le lazy pruning. On vérifie que le contact avec le \texttt{rx\_end} minimal est bien choisi et que les métadonnées finales sont correctes.

    \item \textbf{Test sur 2 sauts} : le résultat du premier saut est utilisé comme source pour le second. On vérifie que \texttt{expiration} et \texttt{cumulative\_delay} se sont bien propagés et que le meilleur contact est toujours sélectionné avec les bonnes métadonnées.
\end{itemize}

\paragraph{Contact,Hybrid et Node parenting :}~\\\\
Contact,Hybride et Node sont 3 variantes de l'algorithme de Dijkstra qui sont utilisés pour le routage :\\

Node est le plus simple, il explore le \gls{mg} crée noeud par noeud, et ne maintient qu'une seule route optimal.
Il trouve le chemin optimal selon la métrique choisie mais s'arrête dès que la destination est atteinte en mode Path,
contrairement aux autres variantes qui continuent d'explorer.\\

Contact explore lui le \gls{mg} par contact plutôt que par noeud, il va utiliser une "Work Area", permettant d'explorer les contacts directement sans passer par la logique nœud par nœud, 
ce qui peut trouver des routes avec moins de saut dans certains cas.\\

Hybrid est la plus complète mais également la plus complexe, il va maintenir une liste de potentiel route optimal
tous le long du parcours, puis, grâce au fonction "can\_retain" et "must\_prune", va décider laquelle garder.\\

Chacunes de ces variantes possédent les 3 mêmes macros :
\begin{itemize}
    \item Path : Chemin simple vers une destination, s'arrête dès qu'elle est atteinte.
    \item PathExcl : Si l'on veut exclure un noeud précis.
    \item TreeExcl : Explore l'entièreté du \gls{mg} avec exclusion.\\
\end{itemize}

Les tests sonts simples, on créer plusieurs \gls{mg} auquel on applique chacunes des macros,
on utilise le manager le plus simple possible pour ne pas rendre le test trop lourd, on applique en dernier la méthode Hop (le moins de saut) ou la méthode SABR (le plus rapide) 
pour avoir le plus de données de test possible.

Les résultats attendues sont le nombre de saut réalisés, et le temps d'arrivée.
Pour les cas d'exclusion, on donne comme objectif par exemple le noeud 3 en excluant le noeud 2 et l'on doit avoir en retour un échec du au noeud exclus.\\

Dans un soucis de lisibilité et de maintienabilité du code, toute les fonctions de création d'objets (noeud,contact,bundle,multigraph)
sont réunis dans un fichiers à part (test\_helpers). De cette manière, seul les tests sont présents dans leur fichiers cibles. \\

Prenons un test en particulier et décryptons le : \label{testPathFinding} \ref{CodetestPathFinding} (Cliquer sur le numéro pour être envoyé à l'annexe correspondant) \\

Nous avons donc une première fonction qui va créer le \gls{mg} auquel on va applique nos tests, le multigraph créer possèdents
5 noeuds qui ont un manager neutre (qui ne fait rien) pour faire au plus simple et de 5 contacts.

Exemple : le premier contact émet du noeud 0 au noeud 1, début du contact à 0 et fin à 10, 
débit de 1 unité/seconde et aucun délai de propagation.

Ensuite nous créeons un \gls{bundle} à destination du noeud 4, sans priorité et sans taille précise.

On contruit les deux chemin, un avec la méthode SABR, l'autre avec la méthode Hop, 
puis nous lui appliquons notre fonction de test.\\

\begin{itemize}
    \item Hop : On attend 3 sauts en 50 unité de temps.
    \item SABR : On attend 4 sauts en 50 unité de temps.\\
\end{itemize}

Le fait que le nombre de saut diverge entre Hop et \gls{sabr} permet une plus grand couverture pour detecter une casse de la librarie.\\

Cette partie à fait l'objet de 2 Pull Request acceptée de Vincent  pour 629 lignes rajoutée 
correspondant aux  tests dans les 3 fichiers + la création du test\_helpers du module Pathfinding. (PR \texttt{\#38} et \texttt{\#44})


\subsubsection[Module : Routing]{Routing}

Le dossier \texttt{routing} regroupe l'ensemble de la logique de routage du projet. Son rôle est de prendre un bundle en entrée et de retourner un \texttt{RoutingOutput}, c'est-à-dire la liste des premiers sauts à emprunter pour acheminer ce bundle vers sa ou ses destinations.

Le fichier \texttt{mod.rs} de ce dossier définit les bases communes à tous les routeurs. Il contient d'abord le trait \texttt{Router}, qui impose à toute implémentation de routeur de fournir une méthode \texttt{route()} prenant en entrée le nœud source, le bundle à router, le temps courant et une liste de nœuds exclus. Il définit ensuite \texttt{RoutingOutput}, une structure qui associe à chaque premier saut (représenté par un contact) la liste des \texttt{RouteStage} des destinations atteignables via ce saut. Cette structure expose également \texttt{lazy\_get\_for\_unicast()}, une méthode qui recherche le premier saut menant à une destination précise, utilisée notamment dans les tests.

Le fichier contient aussi des fonctions partagées par tous les routeurs, comme \texttt{schedule\_unicast}, \texttt{schedule\_multicast}, \texttt{dry\_run\_unicast\_path}, \texttt{dry\_run\_unicast\_tree} et \texttt{dry\_run\_multicast}. Ces fonctions implémentent deux phases distinctes. Le \textit{dry run} simule le parcours d'un chemin ou d'un arbre sans réserver de ressources, pour vérifier qu'un bundle peut effectivement atteindre sa destination en fonction des contacts. Le \textit{scheduling} effectue ensuite la réservation réelle en appelant \texttt{schedule()} sur chaque \texttt{RouteStage} du chemin.

Le dossier contient trois implémentations concrètes du trait \texttt{Router} : CGR, VolCGR et SPSN. Dans le cadre de notre projet, nous n'avons travaillé que sur VolCGR et SPSN. 

VolCGR est une évolution de CGR qui est la méthode de la NASA, et Spsn qui est une implémentation toute nouvelle venant d'A-SABR.

\paragraph{Tests}~\\

Bien que des tests aient été rédigés et soient en grande partie fonctionnels sur notre branche de 
développement, une refactorisation majeure des Contact Plans ainsi que notre test helpers et nos anciens test ont rendu leur intégration complexe.
L'effort de rebase nécessaire étant important par rapport au temps restant, nous avons privilégié de ne pas les inclures dans le rapport
mais de laisser l'explication du module ci-dessus.

\subsection{BP-Socket}
\subsubsection{Mise en situation}

Le projet  a pour objectif de fournir une interface socket  pour \gls{bp},
utilisé dans les réseaux \gls{dtn}. L'idée est de permettre à une application d'envoyer et
recevoir des bundles BP exactement comme elle enverrait des paquets UDP ou TCP, en utilisant la même API socket.

Il s'agit ici de voir autre chose que la librairie A-SABR, tous en travaillant sur quelque chose de très important
pour le futur de A-SABR.

\begin{figure}[H]
\centering
\includegraphics[width=0.70\textwidth]{bp_kernel.png}
\caption{Architecture original de BP-Socket.}
\label{fig:bp_kernel}
\end{figure}

Voici donc les problématique du projets : \\

\begin{itemize}
    \item Pour charger le module kernel, il faut utiliser \texttt{insmod}, ce qui nécessite les droits root.

    \item Le module kernel doit être recompilé pour chaque version du noyau.

    \item Un bug avec le module provoque un Kernel Panic! .

    \item Avoir un module kernel rajoute une charge en plus pour lancer le tous qui est déjà assez complexe.\\
\end{itemize}

Pour tester le tous, il faut bien suivre le README du projet qui utilisent des VM Vagrant (une VM \gls{ion} et une VM µDTN) et beaucoup de commandes à taper.

\subsubsection{Solution}

La solution vient d'un mécanisme propre au C, le \gls{ldp}.
Sans module kernel, un appel à \texttt{socket(AF,BP)} ne sera pas reconnu et ne retournera aucun file descripteur,
L'idée ici sera de créer un module spécifique qui interceptera l'appel au principales fonction (\texttt{socket,bind,send,recv,close})
puis d'envoyer les infos nécessaire au Daemon.

L'intêret du \gls{ldp} ici vient du fait qu'il force le dynamic loader à charger nos fonctions contenue dans le module
avant celle de la glibC.

\begin{figure}[H]
\centering
\includegraphics[width=0.70\textwidth]{bp_interceptor.png}
\caption{Architecture finale désirée de BP-Socket.}
\label{fig:bp_preload}
\end{figure}

Il faut donc : 

\begin{itemize}
    \item Supprimer tous les fichiers kernel et nettoyer les Makefile.

    \item Nettoyez le daemon et supprimer tous les fichiers du General Netlink.

    \item Définir le nouveau protocol de communication entre le hook et le Daemon, celui ci contiendra toute les options nécessaires(\texttt{OPEN ENDPOINT, CLOSE ENDPOINT, SEND BUNDLE, RECV BUNDLE}).

    \item Ecrire le hook en lui même.
    
    \item Ecrire un fichier qui gérera l'état des files descripteur créer par notre version de \texttt{socket}

    \item Modifier le daemon avec les nouvelles fonctions relatif au hook (\texttt{send bundle, connect, message})
    
    \item Modifier \gls{ion} avec les nouvelles fonctions du Daemon.
\end{itemize}

\subsubsection{Application}

Il y a beaucoup de code, donc pour rester dans le plus intéressant,
présentons le fichier le plus intéressant, le hook. \label{Result_Hook}\ref{CodeHook}\\

Dans les première ligne du fichier, on retrouve les signatures des fonctions \texttt{socket,close,bind,sendto}
avec le nom real avant leur nom. Ceci est à mettre en relation avec la fonction \texttt{attribute} juste en dessous.

Cette fonctionne permet de dire grâce à \texttt{dlsym} : Si j'appelle, par exemple, \texttt{real\_socket},
le dynamic loader ira chercher dans la glibC la fonction s'appelant socket.

De cette manière nous on peut redéfinir socket, et choisir si oui ou non on appelle la vraie fonction.\\

On suit ensuite le schéma suivant, lorsqu'on appelle une fonction, on vérifie si le domaine du fd est \texttt{AF\_BP},
si c'est le cas, là notre hook fait son job, sinon on appelle la vraie fonction de la glibC.\\

\textbf{connect to daemon}: Etablit une connexion avec le Daemon via un Unix Socket.

Crée un socket Unix avec la vraie \texttt{socket()}de la glibc, puis se connecte au fichier \texttt{/tmp/bp\_daemon.sock} .
Retourne le fd de connexion si succès, -1 sinon. Cette fonction est appelée uniquement depuis \texttt{socket()}
quand une socket BP est ouverte.\\

\textbf{socket} :  Intercepte la création d'un socket BP et le redirige vers le daemon au lieu du kernel.

Fonctionnement : Si la famille d'adresse n'est pas \texttt{AF\_BP}, laisse passer normalement à la glibc. 
Sinon, appelle \texttt{connect\_to\_daemon()} pour ouvrir une connexion vers le daemon, 
enregistre ce fd dans \texttt{fd\_table} pour le reconnaître plus tard, et le retourne à l'application.
L'app croit avoir un vrai socket kernel alors qu'elle a en réalité un fd de connexion Unix vers le daemon.\\

\textbf{bind} : Intercepte l'association d'une adresse BP à une socket et demande au daemon d'ouvrir l'endpoint correspondant dans \gls{ion}.

Si le fd n'est pas BP, comportement normal. Sinon, récupère le \textbf{node\_id} et \textbf{service\_id} depuis \texttt{sockaddr\_bp},
les stocke dans \texttt{fd\_table}, puis envoie un message \textbf{BP\_IPC\_OPEN\_ENDPOINT} au daemon.
Attend la réponse OK ou ERR. Si OK, \gls{ion} a ouvert l'endpoint et est prêt à envoyer/recevoir sur \textbf{ipn:node\_id.service\_id}.\\

\textbf{close} : Intercepte la fermeture d'un socket BP et demande au daemon de fermer l'endpoint dans \gls{ion}.

Si le fd n'est pas BP, comportement normal. Sinon, envoie \textbf{BP\_IPC\_CLOSE\_ENDPOINT} au daemon avec le \textbf{node\_id} et \textbf{service\_id} stockés dans \texttt{fd\_table}. 
Attend la confirmation, puis supprime l'entrée de \texttt{fd\_table} et ferme le fd Unix socket vers le daemon. 
Sans ce \texttt{close()}, ION garderait l'endpoint ouvert indéfiniment et bloquerait les prochains \texttt{bind()},
ce qui nous ait arrivée lors du développement.\\

\textbf{sendto} :  Intercepte l'envoi d'un bundle BP et le transmet au daemon qui le passera à ION.

Si le fd n'est pas BP, comportement normal. Sinon, construit un message \textbf{BP\_IPC\_SEND\_BUNDLE} contenant les adresses
source et destination (\textbf{src\_node\_id, src\_service\_id, dst\_node\_id, dst\_service\_id}),
les flags BP (\textbf{MSG\_ACK\_REQUESTED etc.}) et la taille du payload. 
Envoie d'abord ce header au daemon, puis le payload (le contenu du message) juste après. 
Attend la réponse OK ou ERR et retourne le nombre de bytes envoyés si succès.\\

Le sender est parfaitement fonctionnel, il permet d'envoyer des messsages d'\gls{ion} vers µDTN et ceux sans module kernel.

\subsubsection{Avenir}

Il reste le fait de recevoir un message, qui inclut donc le fait de détruire un bundle une fois reçu et lu, mais
par manque de temps nous ne pourrons pas le faire. Néanmoins, Monsieur De Jonckère à un collègue qui aimerait reprendre le travail
donc nous lui transmettrons nos travaux dès que possible.


\newpage
\section{Algorithmes et Structures de Données}
\subsection{PrioritySegmentation}
Dans cette partie, nous allons détailler le fonctionnement algorithmique du module \texttt{PrioritySegmentation (pseg)}.
Pour commencer, étudions la fonction de simulation du module \texttt{dry\_run\_tx}. 
Parlons d'abord de sa signature, la fonction prend 4 paramètres:
\begin{itemize}
    \item \&self: la référence vers l'instance du PSegmentationManager contenant toute la logique. Un détail a son importance ici,
    comme la fonction ne fait qu'une simulation on limite l'attribut à une lecture seule en ne mettant pas le mot clé \texttt{mut} indiquant à Rust que cette variable n'est pas mutable.
    \item contact\_data: \&ContactInfo: c'est une \texttt{struct} contenant des attributs \texttt{source} et \texttt{dest} de type \texttt{NodeID}
    et deux attributs \texttt{start} et \texttt{end} de type \texttt{Date}. Cette structure de données représente les limitations physiques: \texttt{source} et \texttt{dest} sont les noeuds concernés par le contact
    et \texttt{start} et \texttt{end} représentent la fenêtre temporelle du contact.
    \item at\_time: Date: c'est l'instant t à partir duquel nous souhaitons insérer le paquet.
    \item bundle: \&Bundle: la référence vers le paquet que l'on souhaite transmettre.
\end{itemize}
Le type de données renvoyé par la fonction est \texttt{Option<ContactManagerTxData>}. En Rust, \texttt{Option<T>} est une énumération polymorphique qui se définit par deux états, None et Some(T). Dans notre cas voici comment sont traités les deux cas: 
\begin{itemize}
    \item Le cas d'échec symbolisé par \texttt{None}: la transmission est impossible.
    \item Le cas de de succès symbolisé ici par \texttt{Some(ContactManagerTxData)}: un créneau utilisable a été trouvé par la fonction. On renvoie donc un objet de type \texttt{struct ContactManagerTxData}
    qui comprend toutes les informations du paquet inséré: les heures de début et de fin d'émission représentés par \texttt{tx\_start} et \texttt{tx\_end}, les heures de réception prenant en compte le délai de propagation représentés par \texttt{rx\_start} et \texttt{rx\_end}
    et la date d'expiration du paquet représenté par le champ \texttt{expiration}.
\end{itemize}

Passons maintenant au corps de l'algorithme. On commence par initialiser deux variables: \texttt{tx\_start} initialisée à \texttt{at\_time}
et \texttt{tx\_end\_opt} représentant l'état de la recherche, \texttt{None} si on cherche un point de départ et \texttt{Some(date)} sinon.
Ensuite, grâce à une boucle \texttt{for}, l'algorithme évalue chaque segment de \texttt{self.booking} avec une complexité de \texttt{O(n), n} étant le nombre de segments.
Dans un premier temps, l'algorithme applique 2 vérifications, avec la ligne \mintinline{rust}{if seg.end <= at_time { continue; }} on ignore les segments qui se trouvent avant du paramètre \texttt{at\_time}.
Grâce à la ligne \mintinline{rust}{if bundle.priority <= seg.val { tx_end_opt = None; continue; }}, on ignore également les segments dont la priorité est supérieur à celle du paquet à insérer.

Après ça, on peut commencer le calcul débutant par \texttt{match tx\_end\_opt}:
\begin{itemize}
    \item Cas \texttt{None}: un espace libre a été trouvé, on fixe \texttt{tx\_start} à \texttt{at\_time} et on calcule les besoins du paquet.
    Si tout rentre dans le segment en cours, on peut renvoyer le résultat, sinon on passe dans l'état \texttt{Some(tx\_end)} pour aller chercher de l'espace dans le ou les segments suivants.
    \item Cas \texttt{Some(tx\_end)}: on cherchait déjà à insérer dans le segment précédent donc on cherche de l'espace supplémentaire dans ce segment. Si \texttt{tx\_end} tombe dans ce segment, on renvoie le résultat sinon on répète l'opération.
\end{itemize}

Si aucun créneau n'a été trouvé, on renvoie None sinon on renvoie la \texttt{struct ContactManagerTxData} correspondante. 
Dans le pire des cas, on a une complexité de l'ordre de \texttt{O(n * r)} avec n le nombres de segments \texttt{booking} er r le nombre de segments de débit.
En effet, à chaque fois qu'un créneau potentiellement libre est trouvé, le calcul de la fin de transmission est relancé par \mintinline{rust}|super::get_tx_end|, cette fonction doit reparcourir les r intervalles de débit.
Donc dans le pire cas, cette sous-recherche est lancée à chaque nouveau segment libre soit une complexité \texttt{O(n * r)}.

Cependant, il est intéressant de noter que physiquement le délai est une valeur qui reste fixe dans la plupart des cas, il sera donc représenté dans la majorité des cas par un segment unique.
Donc la plupart du temps, on aura plutôt une compelexité de l'ordre de \texttt{O(n)}.
\\[1cm]
Après la fonction de simulation, nous pouvons passer à la fonction d'allocation \texttt{schedule\_tx}.
La signature de la fonction est très similaire à celle \texttt{dry\_run\_tx} mais à une différence près: on utilise \texttt{\&mut self} à la place de \texttt{\&self}.
Là où la première fonction n'était qu'une simulation et n'avait pas besoin de modifier des données, ici il faut expliciter le fait que cette variable est mutable et sera donc
modifié par \texttt{schedule\_tx}. Le type de données renvoyé reste également le même.
Passons directement à l'étude algorithmique. Pour commencer, on récupère le résultat de la simulation via \mintinline{rust}|let out = self.dry_run_tx(contact_data, at_time, bundle)?;|. 
L'opérateur '?' est quelque chose de propre à Rust, il permet d'arrêter l'exécution si None est renvoyé.
Après ça, on peut commencer le découpage des segments. Grâce à une boucle \texttt{while} on parcourt tous les segments afin de trouver tous ceux qui sont dans la fenêtre temporelle \texttt{tx\_start -- tx\_end}, avec une complexité \texttt{O(n)}.
Pour chaque segment, il y a 3 opérations possibles:
\begin{itemize}
    \item La coupe à gauche avec \mintinline{rust}{seg.start < tx_start}: si le segment commence avant le début de la fenêtre, il est scindé en deux parties.
    On insère un "nouveau" segment gauche intact grâce à \mintinline{rust}{insert(i)} et on repousse le début du segment actuel à \texttt{tx\_start}.
    \item La coupe à droite avec \mintinline{rust}{seg.end > tx_end}: le principe est très similaire au cas précédent mais avec le côté droit. Si le segment s'arrête après la fin de notre fenêtre,
    on crée un "nouveau" segment droit et on raccourci la fin du segment à \texttt{tx\_end}.
    \item L'écrasement: maintenant que l'on possède un segment qui rentre dans notre fenêtre, on peut simplement remplacer sa valeur de priorité par celle du paquet à insérer grâce à \mintinline{rust}{self.booking[i].val = bundle.priority}
\end{itemize}

Enfin, une procédure simple parcourt tous les segments nouvellement crées sauf le dernier grâce à une boucle \texttt{while j < self.booking.len()-1 (O(n-1))} 
afin d'agréger les segments adjacents de même priorité. Pour chaque segment i, on vérifie si le segment i+1 commence quand le segment i se termine (\mintinline{rust} {if self.booking[j].end == self.booking[j + 1].start}) et s'ils ont la même priorité.
Si c'est le cas on crée un segment avec la date de début du segment i, la date de fin du segment i+1 et leur priorité commune. Si la fusion est faire on n'augmente pas l'indice j, en effet le segment fusionné nouvellement crée à la possibilité d'être fusionné avec le suivant.
S'il n'y a pas de fusion, on passe au segment suivant en faisant j+=1.

Concernant la complexité, le temps de recherche est de l'ordre de \texttt{O(n)}. Les modifications faites pendant cette recherche comme \mintinline{rust}|Vec::insert()| et \mintinline{rust}|Vec::remove()| ont aussi un coût \texttt{O(n)}.
On a donc dans le pire des cas, une complexité \texttt{O(n²)} pour \texttt{schedule\_tx}.
\subsection{Hybrid Parenting}




\newpage
\section{Analyse des résultats}
\subsection{Benchmark}
Benchmarker une librairie de routage DTN comme A-SABR présente plusieurs défis inhérents.
Contrairement à des algorithmes classiques facilement isolables,
le routage DTN implique une chaîne complète de composants interdépendants
— le parsing du Contact Plan, la construction du Multigraph, le pathfinding, et la gestion des routes — 
ce qui rend difficile l'isolation d'un composant précis pour en mesurer les performances.

Des tentatives de benchmarks réalisés avec Criterion souffrent également du bruit inhérent aux runners GitHub Actions partagés,
où les variations entre deux runs peuvent atteindre ±15\% sans aucun changement de code.
Cela rend la détection de régressions fines impossible en CI.

Une autre tentative, toujours avec critérion mais cette fois ci en local, qui elle génères des schémas
violin plots, bien que lisibles, manquent de conclusivité. Les distributions sont larges et se chevauchent 
partiellement, rendant difficile l'affirmation catégorique qu'une variante est strictement meilleure qu'une autre.
Par exemple la comparaison entre Node, Hybrid et Contact Parenting montre des tendances cohérentes avec la théorie,
mais les intervalles de confiance se superposent trop pour conclure avec certitude.

Enfin le choix d'un seul Contact Plan de référence (\texttt{"A-SABR/benches/ptvg\_files/sample1.json"}) 
avec une unique paire source/destination limite la portée des résultats — un seul scénario peut être 
favorable ou défavorable à une variante particulière sans que ce soit représentatif du comportement général 
de la librairie.\\

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{PathFinding.pdf}
    \caption{Comparaison des 3 algos de routage Contact, Node et Hybrid}
    \label{fig:bench_pathfinding}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{Router.pdf}
    \caption{Comparaison entre les deux principaux routers, VolCgr et Spsn}
    \label{fig:bench_router}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.5\textwidth]{CI_Criterion.png}
    \caption{Exemple qui illustre le bruit de la tentative de CI Régression pour un même code}
    \label{fig:ci_Criterion}
\end{figure}



Pour apporter un peu plus de détail, la question de comment benchmarker proprement et de manière fiable la
libraire est une vraie problématique toujours en cours de recherche de la part de l'équipe de développement,
des tentatives étaient déjà présentes à notre arrivée (c'est de là que provient le gros \gls{cp} en JSON).

\newpage
\section{Gestion du Projet}
\subsection{Organisation prévue en début de projet}
Lors du début de notre travail, il était prévu de travailler sur ces 3 tâches:
\begin{itemize}
    \item Les tests et pipelines
    \item La modification de l'interface graphique \texttt{dtchat}
    \item L'implémentation d'A-SABR dans Hardy
\end{itemize}
Il était prévu de suivre le calendrier suivant.
\begin{figure}[h]
    \centering
    \includegraphics[width=0.55\textwidth]{GanttPrevision.png}
    \caption{Diagramme de Gantt prévisionnel}
    \label{fig:GanttDiagramme}
\end{figure}

Concernant la structure de notre GitHub, comme nous devions travailler sur
plusieurs dépôts, nous avons fait le choix de créer un dépôt principal avec à l'intérieur
des sous-modules pointant vers les dépôts sur lesquels nous avons travaillé en laissant à la racine tout ce qui
concerne le côté scolaire du projet notamment le dossier pour le rapport.

Quant à notre manière de travailler, il était prévu de suivre la structure bien connue Gitflow.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.55\textwidth]{ImageGitflow.png}
    \caption{Structure Gitflow}
    \label{fig:Gitflow}
\end{figure}

\subsection{Organisation réelle du travail}
Après avoir débuté notre travail, nous avons rapidement compris que les choix
d'organisation que nous avons fait allaient devoir rapidement être adaptés afin 
d'être plus efficace. 
\\[1pt]

Pour commencer, nous voulions suivre une structure Gitflow, donc travailler dans des branches feature/
puis mettre en commun dans la branche develop pour ensuite envoyer une version stable vers notre main puis
vers le dépôt de Monsieur De Jonckère. Mais en commençant notamment nos tests, nous nous sommes vite rendus
compte que la mise en commun dans develop n'était pas nécessaire puisque nos tâches ne dépendaient pas de la réussite des autres
Cette approche était même plus compliquée à manipuler pour notre encadrant: à chaque Pull Request de notre main
vers le sien, il aurait dû vérifier un très grand nombre de modifications en une seule fois.
\\[0.5pt]

La solution trouvée a donc été la suivante: notre main suivra toujours la branche main du dépôt d'origine,
à chaque nouvel ajout nous créeons une branche depuis le main, sur cette branche on effectue un \texttt{git rebase main}
pour récupérer le travail le plus récent et nous commençons à travailler. Quand le travail est jugé satisfaisant,
nous ouvrons une Pull Request vers le main du dépôt d'origine afin d'avoir des retours. Après la prise en compte des suggestions,
la Pull Request est acceptée et notre travail est merge dans le dépôt d'origine.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{Workflow_Github_Groupe_V.png}
    \caption{Structure suivie par le groupe}
    \label{fig:StructureGit}
\end{figure}

Concernant les tâches effectivement réalisées, la majorité des objectifs prévus ont été atteints: modification de l'interface \texttt{dtchat}, ajout de nombreux tests et pipeline performante.
Certains changements de tâches ont quand même eu lieu au cours du projet: le travail d'intégration dans Hardy a été mis de côté
pour laisser la place au travail de BP-Socket jugé plus intéressant pour nous
et pour Monsieur De Jonckère. 

Même si l'objectif BP-Socket n'a été atteint que partiellement par manque de temps, 
le début de travail déjà fait pourrait amener à quelque chose de plus élaboré lors de potentielles
nouvelles contributions en dehors du cadre de projet de programmation.
\\[1pt]

Pour conclure, ce projet a été des plus formateurs pour nos qualités de gestion de projet.
Nous avons dû confronter nos connaissances académiques à celles du monde professionnel afin d'en tirer les conclusions
qui ont le mieux fonctionné pour nous. Même si une petite sensation de désordre s'est fait ressentir en début de parcours, 
elle nous a permis de devenir par la suite plus performants avec les outils Git et de faire preuve d'une meilleure flexibilité
face à un environnement inconnu.
\newpage
\section{Bilan et Conclusions}
Bien que les orientations initiales aient évolué pour s'adapter aux contraintes techniques rencontrées, 
les jalons majeurs de développement ont été atteints.

Notre effort s'est concentré sur la fiabilisation de l'existant et l'extension des capacités 
de la bibliothèque.\\


\begin{itemize}
    \item Mise en place d'une pipeline CI/CD spécifique pour la librairie.
    \item Mises en places de nombreux test unitaires pour la librairie.
    \item Refactorisation et ajout d'un onglet pour l'interface graphique DT-Chat.
    \item Correction de bug dans la librairie.
    \item Ajout d'un système d'agrégat dans PrioritySegmentation.
    \item Expérimentation d'interception d'appel système via LD\_PRELOAD pour BP-Socket.\\
\end{itemize}

\begin{table}[h!]
\centering
\begin{tabular}{|l|c|l|}
\hline
\textbf{Composant} & \textbf{Nombre de tests} & \textbf{Cible technique} \\ \hline
Module Legacy & 39 & Stabilité des fonctions historiques \\ \hline
Parenting (Node, Contact, Hybrid) & 18 & Logiques de routage et filiation \\ \hline
Fonction \texttt{Try\_Make\_Hop} & 12 & Précision du calcul de prochain saut \\ \hline
Segmentation (Standard \& Priority) & 10 & Intégrité et agrégation des données \\ \hline
\textbf{Total au 13 Avril} & \textbf{79} & \textbf{Couverture des modules les plus bas} \\ \hline
\end{tabular}
\caption{Répartition de l'effort de test par module.}
\end{table}

La pertinence de notre suite de tests a été confirmée lors de récente modifications de la librairie par Mr De Jonckère.
Nos tests ont agi comme un filet de sécurité critique, permettant d'identifier immédiatement
les régressions et facilitant ainsi une évolution rapide du code et évitant la casse de la librairie.

La librairie évoluant rapidement, nos tests ont été refactorisées et n'apparaissent plus telle que nous les avions écrits.
Le nombre inscrit ci-dessus n'est donc plus valide dans la version la plus récente d'A-SABR.\\

Pour résumé l'état d'esprit du projet, on débarque sur quelque chose de déjà en partie construit, dans un
language moderne, nouveau et d'autant plus Open Source.

Cela à nécessité un certain temps d'adaptation et de modifications de nos habitudes scolaires 
pour produire quelque chose de sérieux et professionnel. C'était extraordinaire à faire, apprendre
le système de Pull Request, apprendre à produire du code fiable et maintenable, manier Git dans les détails
et surtout avoir son nom dans les contributeurs d'un projet important.
\newpage
\section{Annexe de codes}

\phantomsection
\label{CodeDTBackend}
\ref{DTBackend} (Cliquer sur le chapitre pour revenir au texte correspondant)\\

La fonction \texttt{update} est tiré de DTChat Backend \texttt{"src/dtchat.rs"} ligne 218

La fonction \texttt{try\_init} est tiré de DTChat Backend \texttt{"src/prediction.rs"} ligne 30

\texttt{pred\_res} est tiré de DTChat Backend \texttt{"src/config/mod.rs"} ligne 83

\begin{minted}[frame=lines, framesep=2mm, linenos]{rust}
    pub fn update(&mut self, path:String, algo: &str){
        match PredictionConfig::try_init(path.clone(), algo){
            Ok(update_config) => {
                self.a_sabr = ASabrInitState::Enabled(update_config);
                self.notify_observers(ChatAppEvent::Info(format!("Update done with : {path} and  {algo}")));
            }
            Err(error) => {
                self.a_sabr = ASabrInitState::Error(error.to_string());
                self.notify_observers(ChatAppEvent::Info("Update failed".to_string()));
            }
        }
    }

pub fn try_init(cp_path: String, algo : &str) -> io::Result<Self> {
        let cp = IONContactPlan::parse::<NoManagement, EVLManager>(&cp_path)?;

        let nodes_length = cp.nodes.len();
        let contacts_length = cp.contacts.len();

        let node_index_map: HashMap<String, NodeID> = cp.nodes
            .iter()
            .enumerate()
            .map(|(index, node)| (node.get_node_name().to_string(), index as NodeID))
            .collect();

        let router_box = build_generic_router::<NoManagement, EVLManager>(
            algo,
            cp,
            None,
        ).expect("Failed to build router");

        let router: Box<dyn Router<NoManagement, EVLManager> + Send + Sync> =
            unsafe { std::mem::transmute(router_box) };
        // in seconds
        let cp_start_time = DTChatTime::now().timestamp_millis() as f64 / 1000.0;

        Ok(PredictionConfig {
            ion_to_node_id: node_index_map,
            router,
            cp_start_time,
            nodes_length,
            contacts_length,
        })
    }


let pred_res = PredictionConfig::try_init(cp_path_unwrapped,"VolCgrHybridParenting");
\end{minted}

\newpage

\phantomsection
\label{CodetestPathFinding}
\ref{testPathFinding} (Cliquer sur le chapitre pour revenir au texte correspondant)\\

La fonction de création d'un \gls{mg} \texttt{exemple\_2\_graph} provient de A-SABR \texttt{"src/pathfinding/test\_helpers.rs"} ligne 229.

La fonction \texttt{test\_exemple\_2} qui permet de tester le \gls{mg} précedemment créer grâce à la fonction 
ci dessus est présente dans les trois fichiers \texttt{Node,Contact et Hybrid Parenting} dans A-SABR \texttt{"src/pathfinding"}.

\begin{minted}[frame=lines, framesep=2mm, linenos]{rust}
pub(crate) fn exemple_2_graph()
-> Result<Rc<RefCell<Multigraph<NoManagement, EVLManager>>>, ASABRError> {
    Ok(Rc::new(RefCell::new(Multigraph::new(ContactPlan::new(
        vec![
            make_node(0, "source", NoManagement {}),
            make_node(1, "from_C0", NoManagement {}),
            make_node(2, "from_C2_C1", NoManagement {}),
            make_node(3, "from_C3", NoManagement {}),
            make_node(4, "from_C4", NoManagement {}),
        ],
        vec![
            make_contact::<NoManagement>(0, 1, 0.0, 10.0, 1.0, 0.0),
            make_contact::<NoManagement>(0, 2, 25.0, 35.0, 1.0, 0.0),
            make_contact::<NoManagement>(1, 2, 10.0, 20.0, 1.0, 0.0),
            make_contact::<NoManagement>(2, 3, 20.0, 40.0, 1.0, 0.0),
            make_contact::<NoManagement>(3, 4, 50.0, 60.0, 1.0, 0.0),
        ],
        None,
    )?)?)))
}

fn test_exemple_2() -> Result<(), ASABRError> {
        let mg = exemple_2_graph()?;

        let mut algo_hop = NodeParentingPath::<NoManagement, EVLManager, Hop>::new(mg.clone());
        let mut algo_sabr = NodeParentingPath::<NoManagement, EVLManager, SABR>::new(mg.clone());

        let bundle = make_bundle(4, 0, 0.0, 1000.0);

        let res_hop = algo_hop
            .get_next(0.0, 0, &bundle, &[][..])
            .expect("Routing Failed !");

        assert_time_hop(&res_hop, 4, 50.0, 3, "Hop");

        let res_sabr = algo_sabr
            .get_next(0.0, 0, &bundle, &[][..])
            .expect("Routing Failed !");

        assert_time_hop(&res_sabr, 4, 50.0, 4, "SABR");

        Ok(())
    }
}
\end{minted}

\newpage
\phantomsection
(Code du chapitre \ref{DTFrontend})\\
\label{CodeDTFrontend}

Entièreté du code de la vue Options de DT Chat Egui \texttt{"src/options\_view.rs"}

\begin{minted}[frame=lines, framesep=2mm, linenos]{rust}
    use crate::main_view::MirroredData;
use eframe::egui;
use egui_file_dialog::FileDialog;


pub struct OptionsView{
    pub selected_algo: String,
    pub selected_cp_path: String,
    file_dialog: FileDialog,
}

impl OptionsView{
    pub fn new() -> Self {
        Self {
            selected_algo: "VolCgrHybridParenting".to_string(),
            selected_cp_path: "./db/ion.cp".to_string(),
            file_dialog: FileDialog::new(),
        }
    }
    pub fn show(&mut self,ui: &mut egui::Ui, _data: &mut MirroredData) -> Option<(String,String)>{
        self.file_dialog.update(ui.ctx());
        let mut update = false;
        ui.vertical(|ui| {
            ui.add_space(5.0);
            ui.heading("Options");
            ui.add_space(5.0);
            ui.separator();
            ui.add_space(15.0);

            //Contact Plan
            ui.horizontal(|ui| {
                ui.label("Change Contact Plan :");
                if ui.button("Explore ").clicked() {
                    self.file_dialog.pick_file();
                    }
            });

            if let Some(path) = self.file_dialog.take_picked(){
                self.selected_cp_path = path.display().to_string();
                update = true;
            }

            ui.add_space(10.0);

            //Algorithme
            ui.horizontal(|ui| {
                ui.label("Change Algo :");
                let old_algo = self.selected_algo.clone();
                
                let mut algos: Vec<&str> = vec![
                    "VolCgrNodeParenting",
                    "VolCgrHybridParenting",
                    "VolCgrHybridParentingHop",
                    "VolCgrNodeParentingHop",
                ];

                #[cfg(feature = "contact_work_area")]
                algos.extend([
                    "VolCgrContactParenting",
                    "VolCgrContactParentingHop",
                ]);

                #[cfg(feature = "contact_suppression")]
                algos.extend([
                    "CgrFirstEndingHybridParentingHop",
                    "CgrFirstEndingHybridParenting",
                    "CgrFirstEndingNodeParentingHop",
                    "CgrFirstEndingNodeParenting",
                ]);

                #[cfg(all(feature = "contact_work_area", feature = "contact_suppression"))]
                algos.extend([
                    "CgrFirstEndingContactParenting",
                    "CgrFirstEndingContactParentingHop",
                ]);

                #[cfg(all(feature = "contact_suppression", feature = "first_depleted"))]
                algos.extend([
                    "CgrFirstDepletedHybridParentingHop",
                    "CgrFirstDepletedHybridParenting",
                    "CgrFirstDepletedNodeParentingHop",
                    "CgrFirstDepletedNodeParenting",
                ]);

                #[cfg(all(feature = "contact_work_area", feature = "contact_suppression", feature = "first_depleted"))]
                algos.extend([
                    "CgrFirstDepletedContactParentingHop",
                    "CgrFirstDepletedContactParenting",
                ]);

                egui::ComboBox::from_id_salt("algo_selector")
                    .selected_text(&self.selected_algo)
                    .show_ui(ui, |ui| {
                        for algo in algos{
                            ui.selectable_value(&mut self.selected_algo, algo.to_string(), algo);
                        }
                    });
                if old_algo != self.selected_algo{
                    update = true;
                }
            });
        });
        if update{
            Some((self.selected_cp_path.clone(),self.selected_algo.clone()))
        } else{
            None
        }
    }
}
\end{minted}
\newpage
\phantomsection
\label{CodeCIv1}
\ref{CI/CD} (Cliquer sur le chapitre pour revenir au texte correspondant)

Code complet de la première version de notre fichier ci.yml:
\begin{minted}[frame=lines, framesep=2mm, linenos]{yaml}
    name: CI

on:
  push:
    branches: ["develop","main","feature/*"]

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: rust:slim

    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: swatinem/rust-cache@v2

      - run: cargo fmt --all -- --check
        continue-on-error: true
      - run: cargo clippy
      - run: cargo test --lib --bins --tests
\end{minted}

\newpage
\phantomsection
\label{CodeCIv2}
\ref{CI/CD} (Cliquer sur le chapitre pour revenir au texte correspondant)

Code complet de la seconde version de notre fichier ci.yml:
\begin{minted}[frame=lines, framesep=2mm, linenos]{yaml}
name: A-SABR CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fmt:
    name: Check formatting with Rustfmt
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      
      - run: cargo fmt --all -- --check
  
  clippy:
    name: Linting with Clippy
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      
      - uses: swatinem/rust-cache@v2
      - run: cargo clippy --all-targets --all-features -- -D warnings

  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
      - run: cargo test --lib --bins --tests --verbose
  
  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    needs: tests
    steps:
        - uses: actions/checkout@v4
        - uses: dtolnay/rust-toolchain@stable
        - uses: swatinem/rust-cache@v2
        
        - name: Install tarpaulin
          if: ${{ !env.ACT }}
          uses: taiki-e/install-action@cargo-tarpaulin

        - name: Generate coverage report
          if: ${{ !env.ACT }}
          run: cargo tarpaulin --ignore-tests --out Html --out Xml

        - name: Upload report
          if: ${{ !env.ACT }}
          uses: actions/upload-artifact@v4
          with:
            name: code-coverage-report
            path: tarpaulin-report.html
            retention-days: 7
\end{minted}

\newpage
\phantomsection
\label{CodeCIprecommit}
\ref{CI/CD} (Cliquer sur le chapitre pour revenir au texte correspondant)
Code complet du fichier \texttt{.pre-commit-config.yaml}.
\begin{minted}[frame=lines, framesep=2mm, linenos]{yaml}
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files

  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        entry: cargo fmt --all -- --check
        language: system
        types: [rust]
        pass_filenames: false

      - id: cargo-clippy
        name: cargo clippy
        entry: cargo clippy --all-targets --all-features -- -D warnings
        language: system
        types: [rust]
        pass_filenames: false
\end{minted}

\newpage
\phantomsection
\label{CodeCIyml}
\ref{CI/CD}
Code complet de la version finale du fichier \texttt{ci.yml}.
\begin{minted}[frame=lines, framesep=2mm, linenos]{yaml}
name: A-SABR CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint and Style (Pre-commit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: swatinem/rust-cache@v2
      - uses: pre-commit/action@v3.0.1

  tests:
    name: Run Tests
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
      - name: Run Tests
        run: cargo test --all-features --all-targets

  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    needs: tests
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
      - uses: taiki-e/install-action@cargo-tarpaulin
      - run: cargo tarpaulin --ignore-tests --out Html
      - uses: actions/upload-artifact@v4
        with:
          name: code-coverage-report
          path: tarpaulin-report.html
          retention-days: 7
\end{minted}

\newpage
\phantomsection
\label{CodeStartTest}
\ref{pseg}
Code de la fonction \texttt{start\_test}.
\begin{minted}[frame=lines, framesep=2mm, linenos]{rust}

    #[derive(Debug, PartialEq, Clone)]
    enum InputSeg {
        Delay(Date, Date, Duration),
        Rate(Date, Date, DataRate),
        Booking(Date, Date, Priority),
    }

    #[derive(Debug, PartialEq, Clone)]
    enum OutputSeg {
        Booking(Date, Date, Priority),
    }
    #[track_caller]
    fn start_test(
        input: Vec<InputSeg>,
        output: Vec<OutputSeg>,
        requests: Vec<(Bundle, f64, bool)>,
    ) {
        let contact_info = ContactInfo::new(0, 1, 0.0, 200.0);
        let mut delay_segments: Vec<Segment<Date>> = Vec::new();
        let mut rate_segments: Vec<Segment<DataRate>> = Vec::new();
        let mut actual_output = Vec::new();
        let mut initial_bookings: Vec<Segment<Priority>> = Vec::new();

        for seg in input {
            match seg {
                InputSeg::Delay(start, end, val) => {
                    delay_segments.push(Segment { start, end, val })
                }
                InputSeg::Rate(start, end, val) => rate_segments.push(Segment { start, end, val }),
                InputSeg::Booking(start, end, priority) => initial_bookings.push(Segment {
                    start,
                    end,
                    val: priority,
                }),
            }
        }
        let mut manager = PSegmentationManager::new(rate_segments, delay_segments);
        manager.try_init(&contact_info);
        if !initial_bookings.is_empty() {
            manager.booking = initial_bookings;
        }

        for (i, (bundle, at_time, expect_success)) in requests.iter().enumerate() {
            let dry_run_res = manager.dry_run_tx(&contact_info, *at_time, bundle);
            let schedule_tx_res = manager.schedule_tx(&contact_info, *at_time, bundle);

            assert_eq!(
                dry_run_res, schedule_tx_res,
                "TEST N°{} FAILED: dry_run and schedule_tx doesn't match.\n",
                i
            );
            assert_eq!(
                schedule_tx_res.is_some(),
                *expect_success,
                "TEST N°{} FAILED: expected: {} actual: {}",
                i,
                expect_success,
                schedule_tx_res.is_some()
            );
        }

        //Building actual output

        for seg in &manager.booking {
            actual_output.push(OutputSeg::Booking(seg.start, seg.end, seg.val));
        }
        assert_eq!(
            actual_output, output,
            "TEST FAILED: Actual output is not the one expected."
        );
    }
\end{minted}

\newpage
\phantomsection
\label{CodeHook}
\ref{Result_Hook}
Code complet du hook BP Socket \texttt{"bp\_socket/interceptor.c"}.
\begin{minted}[frame=lines, framesep=2mm, linenos]{C}
#define _GNU_SOURCE
#include "bp_ipc.h"
#include "bp_socket.h"
#include "fd_table.h"
#include <dlfcn.h>
#include <sys/socket.h>
#include <sys/un.h>
#include <unistd.h>

static int (*real_socket)(int, int, int) = NULL;
static int (*real_close)(int) = NULL;
static int (*real_bind)(int, const struct sockaddr*, socklen_t) = NULL;
static ssize_t (*real_sendto)(int, const void*, size_t, int, const struct sockaddr*, socklen_t) = NULL;

static void __attribute__((constructor)) init()
{
	real_socket = dlsym(RTLD_NEXT, "socket");
	real_close = dlsym(RTLD_NEXT, "close");
	real_bind = dlsym(RTLD_NEXT, "bind");
	real_sendto = dlsym(RTLD_NEXT, "sendto");
}

static int connect_to_daemon()
{
	struct sockaddr_un addr;
	int fd = real_socket(AF_UNIX, SOCK_STREAM, 0);
	if (fd < 0)
		return -1;

	addr.sun_family = AF_UNIX;
	strncpy(addr.sun_path, BP_IPC_SOCKET_PATH, sizeof(addr.sun_path));

	if (connect(fd, (struct sockaddr*)&addr, sizeof(addr)) < 0) {
		real_close(fd);
		return -1;
	}
	return fd;
}

int socket(int domain, int type, int protocol)
{
    if (domain != AF_BP)
        return real_socket(domain, type, protocol);

    int unix_fd = connect_to_daemon();
    if (unix_fd < 0) {
        return -1;
    }

    fd_table_add(unix_fd, unix_fd, 0, 0);
    return unix_fd;
}

int bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen)
{
    if (!is_bp_fd(sockfd))
        return real_bind(sockfd, addr, addrlen);

    struct sockaddr_bp *bp_addr = (struct sockaddr_bp *)addr;
    bp_fd_entry_t *entry        = fd_table_get(sockfd);

    entry->node_id    = bp_addr->bp_addr.ipn.node_id;
    entry->service_id = bp_addr->bp_addr.ipn.service_id;

    bp_ipc_msg_t msg = {
        .cmd            = BP_IPC_OPEN_ENDPOINT,
        .src_node_id    = entry->node_id,
        .src_service_id = entry->service_id,
        .payload_len    = 0,
    };
    write(entry->unix_fd, &msg, sizeof(msg));

    bp_ipc_response_t resp;
    read(entry->unix_fd, &resp, sizeof(resp));

    if (resp.status == BP_IPC_OK)
        return 0;

    return -1;
}

int close(int fd)
{
    if (!is_bp_fd(fd))
        return real_close(fd);

    bp_fd_entry_t *entry = fd_table_get(fd);

    bp_ipc_msg_t msg = {
        .cmd            = BP_IPC_CLOSE_ENDPOINT,
        .src_node_id    = entry->node_id,
        .src_service_id = entry->service_id,
        .payload_len    = 0,
    };
    write(entry->unix_fd, &msg, sizeof(msg));

    bp_ipc_response_t resp;
    read(entry->unix_fd, &resp, sizeof(resp));

    fd_table_remove(fd);
    real_close(fd);

    return 0;
}

ssize_t sendto(int sockfd, const void* buf, size_t len, int flags,
    const struct sockaddr* dest_addr, socklen_t addrlen)
{
	if (!is_bp_fd(sockfd))
		return real_sendto(sockfd, buf, len, flags, dest_addr, addrlen);

	struct sockaddr_bp* bp_dest = (struct sockaddr_bp*)dest_addr;
	bp_fd_entry_t* entry = fd_table_get(sockfd);

	bp_ipc_msg_t msg = {
		.cmd = BP_IPC_SEND_BUNDLE,
		.src_node_id = entry->node_id,
		.src_service_id = entry->service_id,
		.dst_node_id = bp_dest->bp_addr.ipn.node_id,
		.dst_service_id = bp_dest->bp_addr.ipn.service_id,
		.flags = (uint32_t)flags,
		.payload_len = (uint32_t)len,
	};
	write(entry->unix_fd, &msg, sizeof(msg));
	write(entry->unix_fd, buf, len);

	bp_ipc_response_t resp;
	read(entry->unix_fd, &resp, sizeof(resp));

	if (resp.status == BP_IPC_OK)
		return (ssize_t)len;

	return -1;
}
\end{minted}


\newpage
\begin{thebibliography}{3} 

\bibitem{sabr} 
J. A. Fraire, O. De Jonckère, and S. Burleigh \textit{Routing in the space internet: A
contact graph routing tutorial.}, Journal of Network and Computer Applications, 174, 102884.

\bibitem{a-sabr} 
O. De Jonckère, L. Ma, and J. A. Fraire. \textit{ A-SABR: The Adaptive Library for Schedule-Aware Bundle
Routing.}, WiSEE 2025 - IEEE International Conference on Wireless for Space and Extreme Environments, Oct 2025,
Halifax, Canada.

\bibitem{bp} 
Sylvain Pierrot, Olivier de Jonckère, Scott Burleigh, Charley Geoffroy.\textit{ Design and Implementation of a Native Socket Interface for the Bundle Protocol.}
, WiSEE 2025 - 13th Annual IEEE International Conference on Wireless for Space and Extreme Environments, Oct 2025, Halifax, Canada.

\end{thebibliography}
\end{document}