\documentclass{article}

\usepackage[T1]{fontenc}
\usepackage[french]{babel} 
\usepackage{mathptmx}
\usepackage{geometry}
\usepackage{graphicx}
\graphicspath{{Images/}}
\usepackage{minted}
\usepackage{longtable}
\usepackage{array}
\usepackage{caption}
\usepackage{titlesec}
\setcounter{secnumdepth}{4}
\setcounter{tocdepth}{4}
\geometry{a4paper, margin=2cm}

\usepackage[
    colorlinks=true,        
    linkcolor=black,
    urlcolor=blue,          
    citecolor=green,        
    pdftitle={Rapport du Sprint de Décembre 2026}, 
]{hyperref}

\usepackage[acronym, toc]{glossaries}
\makeglossaries

\newacronym[
    description={Delay-Tolerant Networking. Réseau tolérant aux délais et aux ruptures de connectivité,
    où les nœuds ne sont pas forcément connectés en permanence. Un nœud peut stocker les données,
    les conserver pendant une rupture de connectivité, puis les retransmettre quand un contact devient disponible.
    Ces contacts peuvent être planifiés ou opportunistes selon le contexte}
]{dtn}{DTN}{Delay-Tolerant Networking}

\newacronym[
    description={Contact Plan. Fichier de configuration décrivant les fenêtres de communication planifiées entre nœuds : qui communique avec qui, à quelle heure, pendant combien de temps, avec quel débit et quel délai.
    Lu par la bibliothèque, il est transformé en graphe temporel (\gls{mg})}
]{cp}{CP}{Contact Plan}

\newacronym[
    description={Schedule-Aware Bundle Routing. Standard recommandé par le CCSDS pour le routage de bundles dans les réseaux DTN. Il s’appuie sur un Contact Plan décrivant les contacts planifiés entre nœuds afin de calculer des routes compatibles avec les contraintes temporelles du réseau}
]{sabr}{SABR}{Schedule-Aware Bundle Routing}

\newacronym[
    description={Structure de données centrale du projet représentant le réseau.
Elle contient les nœuds, qui jouent le rôle d'émetteurs ou de récepteurs, ainsi que les contacts, c'est-à-dire les fenêtres temporelles pendant lesquelles deux nœuds peuvent communiquer avec un débit et un délai donnés.
Elle permet de représenter plusieurs contacts entre une même paire de nœuds à des moments différents.
L'objet théorique le plus proche d'une telle structure est le carquois, c'est-à-dire un graphe orienté pouvant contenir plusieurs flèches entre deux mêmes sommets}
]{mg}{Multigraph}{Multigraph}

\newacronym[
    description={Unité de données utilisée dans les réseaux DTN. Un bundle regroupe les données à transmettre et 
    les informations nécessaires à leur acheminement, notamment la source, la ou les destinations, la priorité, 
    la taille et le délai d'expiration}
]{bundle}{Bundle}{Bundle}

\newacronym[
    description={Mécanisme du chargeur dynamique permettant de précharger une bibliothèque partagée avant les bibliothèques standards, afin de redéfinir ou d’intercepter certaines fonctions de la Glibc}
]{ldp}{PRELOAD}{LD\_PRELOAD}

\newacronym[
    description={Bundle Protocol Socket. Type spécifique de socket Linux reposant sur la création d'une nouvelle famille de sockets, \texttt{AF\_BP}, afin de permettre l'envoi et la réception de bundles dans un environnement DTN}
]{bp}{BP Socket}{Bundle Protocole Socket}

\newacronym[
    description={Interplanetary Overlay Network. Implémentation Open Source de référence développée par la NASA pour les réseaux tolérants aux délais }
]{ion}{ION}{Interplanetary Overlay Network}

\title{Contributions on Schedule-Aware Bundle Routing \\ \Large Projet de Programmation 2 }
\author{DAGUE Bastien, AL FARAJ Samy, JOUINI Yassine, BROCHARD Vincent \\ \small L3 Informatique - Faculté des Sciences - Université de Montpellier}
\date{2025 - 2026}

\begin{document}

\maketitle
\tableofcontents

\newpage
\printglossary[type=\acronymtype, title={Glossaire et Abréviations}, style=altlist]

\newpage
\section{Introduction}
\subsection{Contribution Open Source DTN-MTP}
Le projet s’inscrit dans un contexte open source, ce qui a fortement influencé notre manière de travailler. Contrairement à un projet strictement scolaire, les contributions ne sont pas simplement ajoutées à un dépôt privé : elles doivent s’intégrer proprement dans un écosystème existant, être relues, discutées, corrigées puis validées par les mainteneurs du projet. Dans notre cas, les modifications apportées à A-SABR, DT-Chat ou BP-Socket ont donc été réalisées sous forme de branches puis de Pull Requests, avec des échanges réguliers autour de la qualité du code, de la maintenabilité et de la cohérence avec l’existant.

A-SABR, DTChat et BP-Socket font partie de l’écosystème DTN-MTP, qui regroupe plusieurs outils liés aux réseaux tolérants aux délais. A-SABR constitue la bibliothèque de routage, DTChat sert d’interface graphique permettant d’expérimenter et de visualiser des communications DTN, tandis que BP-Socket vise à fournir une interface socket adaptée au Bundle Protocol. Ces différents dépôts ne sont donc pas indépendants : ils participent à un ensemble logiciel plus large, où les évolutions d’un composant peuvent avoir des conséquences sur les autres.

Ce contexte open source nous a confrontés à des problématiques concrètes de développement collaboratif que nous découvrions en grande partie. La difficulté était renforcée par le fait que nous arrivions sur un projet déjà commencé, bien fourni, avec une architecture existante, des conventions de code, plusieurs dépôts liés entre eux et des choix techniques déjà établis. Il ne suffisait donc pas d’écrire du code fonctionnel : il fallait d’abord comprendre la base de code, identifier où intervenir sans casser l’existant, maintenir la compatibilité avec les autres dépôts, documenter les changements, répondre aux retours et parfois adapter notre travail à des refactorisations récentes. Cette organisation nous a permis de mieux comprendre le fonctionnement d’un projet logiciel réel, dans lequel la qualité d’intégration est aussi importante que la fonctionnalité développée.

\subsubsection{DTN}
Les réseaux \gls{dtn} constituent encore une approche relativement récente par rapport aux réseaux classiques, mais ils sont déjà utilisés dans des contextes réels, notamment pour le rapatriement de certaines données scientifiques depuis l'ISS. Leur intérêt est particulièrement fort dans le domaine spatial, où une connexion continue entre les nœuds ne peut pas toujours être garantie. Contrairement aux réseaux traditionnels, qui supposent souvent l'existence d'un chemin de bout en bout entre l'émetteur et le récepteur, les réseaux DTN acceptent les délais importants, les interruptions de connexion et les périodes d'indisponibilité. Les réseaux spatiaux du futur reposeront de plus en plus sur ce type de technologie.

Dans ce type de réseau, les données sont transmises selon un principe de stockage puis retransmission : un nœud peut conserver temporairement un message, appelé \gls{bundle}, puis le transmettre plus tard lorsqu’un contact avec un autre nœud devient disponible. Ce fonctionnement est particulièrement adapté aux communications spatiales, où les distances sont importantes, les délais de propagation élevés et les fenêtres de communication limitées.

Dans le contexte d’A-SABR, ces fenêtres de communication sont décrites à l’avance dans un \gls{cp}. Celui-ci indique quels nœuds peuvent communiquer, à quels moments, pendant quelle durée, avec quel débit et quel délai. La bibliothèque A-SABR exploite ensuite ces informations pour calculer des routes adaptées au contexte DTN, en tenant compte des contraintes temporelles et des ressources disponibles. Ainsi, le routage ne consiste pas seulement à trouver un chemin dans un graphe, mais à déterminer une succession de contacts compatibles dans le temps.

\subsubsection{Rust}

Rust est un langage particulièrement adapté à A-SABR, car la bibliothèque manipule de nombreuses structures de données liées au routage : nœuds, contacts, \gls{bundle}, \gls{cp} et \gls{mg}. Ces objets doivent être parcourus, modifiés et parfois partagés entre plusieurs composants, tout en conservant de bonnes performances.

Un premier intérêt de Rust est la gestion explicite des cas d'échec avec les types \texttt{Option} et \texttt{Result}. Dans A-SABR, une fonction peut par exemple échouer parce qu'aucun contact ne permet une transmission, ou parce qu'un \gls{cp} ne peut pas être correctement parsé :

\begin{minted}[fontsize=\small,breaklines,frame=single]{rust}
pub trait Router<NM: NodeManager, CM: ContactManager> {
    fn route(
        &mut self,
        source: NodeID,
        bundle: &Bundle,
        curr_time: Date,
        excluded_nodes: &[NodeID],
    ) -> Result<Option<RoutingOutput<NM, CM>>, ASABRError>;
}
\end{minted}

L'opérateur \texttt{?} permet ensuite de propager simplement une erreur à la fonction appelante :

\begin{minted}[fontsize=\small,breaklines,frame=single]{rust}
let graph = Multigraph::new(contact_plan)?;
\end{minted}

Si \texttt{Multigraph::new} retourne une erreur, celle-ci est automatiquement renvoyée par la fonction courante ; sinon, la valeur construite est récupérée dans \texttt{graph}. Cela permet de garder un code lisible tout en conservant une gestion explicite des erreurs.

Rust est aussi utile grâce à son système de traits et de généricité. Dans A-SABR, plusieurs composants partagent une même interface tout en ayant des comportements différents. C'est le cas des gestionnaires de contacts, qui implémentent le trait \texttt{ContactManager}. Celui-ci définit notamment une méthode de simulation, \texttt{dry\_run\_tx}, et une méthode de planification réelle, \texttt{schedule\_tx}.

\begin{minted}[fontsize=\small,breaklines,frame=single]{rust}
pub trait ContactManager: Debug {
    fn dry_run_tx(
        &self,
        contact_data: &ContactInfo,
        at_time: Date,
        bundle: &Bundle,
    ) -> Option<ContactManagerTxData>;

    fn schedule_tx(
        &mut self,
        contact_data: &ContactInfo,
        at_time: Date,
        bundle: &Bundle,
    ) -> Option<ContactManagerTxData>;

    fn get_original_volume(&self) -> Volume;
    fn try_init(&mut self, contact_data: &ContactInfo) -> bool;
}
\end{minted}

Cette signature est particulièrement intéressante : \texttt{dry\_run\_tx} prend \texttt{\&self}, ce qui correspond à une simulation sans modification de l'état interne du contact, tandis que \texttt{schedule\_tx} prend \texttt{\&mut self}, ce qui indique que la planification modifie réellement l'état du gestionnaire. Les deux méthodes retournent une \texttt{Option<ContactManagerTxData>}, ce qui exprime directement qu'une transmission peut échouer.

Enfin, les macros illustrent la métaprogrammation en Rust : elles permettent de générer du code à la compilation afin d'éviter la duplication. Dans A-SABR, elles sont notamment utilisées pour créer des variantes proches d'un même comportement ou pour décliner une même logique de test sur plusieurs modules.

\begin{minted}[fontsize=\small,breaklines,frame=single]{rust}
macro_rules! create_new_alternative_path_variant {
    ($struct_name:ident, $better_fn:ident) => { ... };
}
\end{minted}

La compilation de la librairie et de l'interface graphique nécessitant divers paquets Linux, notre travail a été 
réalisé soit par WSL, soit directement depuis une distribution Linux.

\subsection{Écosystème DTN-MTP}
\subsubsection{A-SABR}
A-SABR est la bibliothèque centrale sur laquelle porte notre projet. Elle implémente des mécanismes de routage pour les réseaux \gls{dtn}, en s’appuyant sur les principes du \gls{sabr}. Son rôle est de déterminer comment acheminer un \gls{bundle} d’un nœud source vers une ou plusieurs destinations, non pas à partir d’une connexion permanente, mais à partir des contacts disponibles dans le temps.

Pour cela, A-SABR lit un \gls{cp}, qui décrit les fenêtres de communication prévues entre les nœuds du réseau. Ces informations sont ensuite transformées en \gls{mg}, structure sur laquelle les algorithmes de routage peuvent travailler. La bibliothèque peut alors calculer des routes compatibles avec les contraintes temporelles du réseau, comme les dates de début et de fin des contacts, les délais de propagation, les débits disponibles ou encore la priorité des bundles.

L’intérêt d’A-SABR est aussi sa modularité. Elle permet de comparer différentes stratégies de gestion des contacts, différentes variantes de pathfinding et plusieurs méthodes de routage. Cette architecture rend la bibliothèque adaptée à l’expérimentation : nous pouvons tester plusieurs modèles de transmission, mesurer leurs performances, vérifier leur comportement par des tests, puis les intégrer progressivement dans l’écosystème open source DTN-MTP.

Le projet A-SABR est mené par Monsieur Olivier de Jonckère au sein du LIRMM. Il s’agit d’un projet entièrement open source, ce qui signifie que son développement repose sur un dépôt public, des contributions externes et un processus de validation par Pull Requests. Ce cadre nous a permis de travailler sur un projet réel, maintenu en dehors du contexte strictement universitaire, et d’inscrire nos contributions dans un écosystème logiciel existant.

\begin{figure}[h]
    \centering
    \includegraphics[width=1.0\textwidth]{resume_asabr.png}
    \caption{Schéma récapitulatif d'A-SABR}
    \label{fig:resume_asabr}
\end{figure}

\subsubsection{DTChat}
DTChat est une application graphique codée en Rust permettant d'illustrer une communication DTN. 
Elle supporte UDP, TCP et \gls{bp}.
\begin{figure}[h]
    \centering
    \includegraphics[width=0.49\textwidth]{DT_Message.png}
    \hfill 
    \includegraphics[width=0.49\textwidth]{DT_Network.png}
    \caption{Onglet Message (gauche) et Events (droite) d'un terminal DTChat}
    \label{fig:comparaison_dt}
\end{figure}

L'onglet message affiche un graphique des messages envoyés. Un rectangle vert indique qu’un message a été envoyé puis reçu avec confirmation.\\

À droite, l’onglet Events récapitule textuellement les interactions de l’interface et du réseau.

A gauche le recapitulatif de l'interaction logicielle et à droite celle du reseau, nous remarquons bien les différents messages envoyés et reçus.

Sur cet exemple, il y a eu 3 envois du même terminal, 1 UDP, 1 TCP et  1 tentative \gls{bp}.

\gls{bp} nécessite le chargement d'un module kernel spécifique. Dans l'exemple présenté, ce module n'a pas été chargé : l'application ne peut donc pas utiliser la socket \texttt{AF\_BP} et le message est finalement envoyé via UDP.

\subsubsection{Bp-socket}

BP-Socket est un projet visant à fournir une interface de type socket Linux pour le Bundle Protocol, utilisé dans les réseaux \gls{dtn}. L’idée est de permettre à une application d’envoyer et de recevoir des \gls{bundle} avec une API proche de celle utilisée pour des communications classiques en UDP ou TCP.

Dans son fonctionnement initial, BP-Socket repose sur la création d’une nouvelle famille de sockets, \texttt{AF\_BP}, afin de représenter les communications Bundle Protocol au niveau du système. Une application peut alors manipuler une socket BP comme une socket réseau classique, tandis que l’implémentation se charge de transmettre les données sous forme de bundles.

\begin{figure}[H]
\centering
\includegraphics[width=0.70\textwidth]{bp_kernel.png}
\caption{Architecture de BP-Socket.}
\label{fig:bp_kernel_intro}
\end{figure}

\newpage
\section{Développements Logiciel : Conception, Modélisation, Implémentation}
\subsection{DT Chat}
\subsubsection[DT Chat Backend]{Backend}
 \label{DTBackend} (Code en annexe \ref{CodeDTBackend})\\\\
Nous avons d’abord extrait une fonction \texttt{update} à partir de la logique de démarrage de l’interface. Cette fonction prend en entrée le nom de l’algorithme à utiliser ainsi que le chemin du Contact Plan à charger. La fonction \texttt{try\_init} a également été modifiée afin d’accepter ces paramètres et de retourner le nombre de nœuds et de contacts détectés dans le Contact Plan chargé.\\

La fonction \texttt{update} prend donc en entrée le nom de l’algorithme et le chemin du \gls{cp} à charger.

Elle va ensuite faire un match sur \texttt{"PredictionConfig::try\_init(path.clone(), algo)"} qui retournera donc un Result,
pour empêcher un crash total de l'interface au moindre souci.

Si Ok alors nous mettons à jour la configuration que nous envoyons au frontend et nous l'affichons dans l'App Event,
si Err, nous retournons l'erreur et nous l'affichons dans l'App Event.

Étant basé sur A-SABR, il faut régulièrement faire de légères modifications dûes à des refactorisations de fonctions ou de
l'ajout de Result remplaçant des Options dans la librairie. \\
\subsubsection[DT Chat Frontend]{Frontend}
\label{DTFrontend} (Code en annexe \ref{CodeDTFrontend})\\

L’interface DTChat peut être rapprochée d’une organisation de type MVC : le backend joue le rôle de modèle, la vue correspond aux éléments graphiques affichés, et le code réagissant aux interactions utilisateur joue le rôle de contrôleur.

Ici le but est donc d'afficher l'onglet Options et d'envoyer les différents ordres au Backend suivant les actions faites.

Le raccourci ci-dessus permet d'accéder au code de la vue Options, il se résume simplement en un bouton qui ouvre un explorateur
pour choisir son \gls{cp} et une ComboBox pour le choix d'algorithme, dès qu'une action est réalisée pour l'un ou pour l'autre,
l'interface se met directement à jour et affiche les nouveaux paramètres chargés dans l'App Event.

Une spécificité existe pour le choix d'algorithme, certains algorithmes sont conditionnés à l'activation
de "features", des fonctionnalités que nous choisissons d'activer ou non lors du lancement de l'interface, nous pouvons voir
dans le code que la ComboBox s'adapte aux choix de features choisies lors de la compilation.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.49\textwidth]{DT_Algos.png}
    \hfill  
    \includegraphics[width=0.49\textwidth]{DT_Changes.png}
    \caption{Onglet App Event mis à jour (gauche) et ComboBox adaptative (droite) d'un terminal DTChat}
    \label{fig:dt_changes}
\end{figure}

L'interface graphique a, sur cet exemple illustré, été compilé avec la feature \texttt{contact\_work\_area}, 
donc la ComboBox s'est adaptée. Nous avons ensuite sélectionné l’algorithme \texttt{VolCgrNodeParenting} ainsi que
chargé le \gls{cp} \texttt{ion\_format.cp} et nous voyons bien dans l'App Event que le changement est effectif
et que le nombre de noeuds et de contacts à bien été détecté.\\

En pratique, l'algorithme choisi ne change pas réellement ce que l'interface nous montre lors de la réception
ou l'envoi de message, il s'agit surtout ici de préparer de possible futur ajout car le Backend lui comprend totalement les changements effectués.\\

\begin{figure}[h]
    \centering
    \includegraphics[width=0.30\textwidth]{Contact_Plan_DT.png}
    \caption{Contact plan "A-SABR/examples/contact\_plans/ion\_format.cp"}
    \label{fig:dt_cp_ion_format}
\end{figure}

Ci-dessus, nous avons le format classique de \gls{cp} que l'interface comprend correctement :

Ce \gls{cp} au format ION, format de référence de la NASA, définit un réseau de 6 nœuds avec une topologie linéaire 1→2→3→4→5→6. 
Chaque lien est actif entre t=+60s et t=+7260s.

Les lignes contact définissent les fenêtres de communication avec leurs débits respectifs
 — le lien 1→2 dispose de 10 000 octets/s, 2→3 de 15 000,
et ainsi de suite jusqu'à 30 000 octets/s pour 5→6.

Les lignes range définissent le délai de propagation physique du signal sur chaque lien
 — 10 secondes entre les nœuds 1 et 2, 15 entre 2 et 3, jusqu'à 30 secondes pour 5→6,
  simulant des distances croissantes entre les nœuds
 comme nous pourrions en trouver en situation réelle.\\

\subsection[Pipeline]{Pipeline CI/CD}
\subsubsection{Outils de qualité logicielle}
Durant notre travail, l'une de nos tâches a été d'offrir aux différents dépôts des pipelines robustes et utiles aux développeurs de l'équipe.
Pour cela, il a fallu dans un premier temps définir les choses à vérifier grâce au travail de pipelines.
Après discussion avec Monsieur De Jonckère, nous avons décidé de travailler sur le format du code, ses failles de logique ou de sécurité et sur le lancement des tests puis de leur couverture.
Nous présenterons donc dans cette partie, les outils utilisés pour mener à bien cette tâche.

Comme tout projet Rust, nous utilisons ici \texttt{Cargo}. \texttt{Cargo} est l'outil de construction et le gestionnaire de paquets officiel de Rust, 
il permet de gérer les dépendances, compiler le code en offrant un retour intuitif au développeur en cas d'erreur ou encore de standardiser la structure d'un projet Rust.
\texttt{Cargo} possède beaucoup de commandes utiles comme \texttt{cargo build} pour compiler ou \texttt{cargo check} pour vérifier la compilation sans générer de binaire.

Dans le cadre de nos pipelines, nous utilisons d'autres commandes de \texttt{Cargo} dont voici leur explication:
\begin{itemize}
    \item \texttt{cargo fmt --check}: au lancement de cette commande, une vérification de format est lancée pour vérifier si le code respecte les standards de style de la communauté.
    L'ajout de \texttt{--check} impose de faire uniquement une vérification sans modifier le code.
    \item \texttt{cargo clippy -D warnings}: cet outil est le "linter" de Rust, il permet de trouver des erreurs de logique ou des mauvaises pratiques de programmation comme une fonction possédant trop de paramètres.
    En ajoutant \texttt{-D warnings}, nous permettons de rendre les avertissements bloquants.
    \item \texttt{cargo test}: grâce à cette commande, l'ensemble des tests du projet sont lancés. Les tests unitaires précédés du module \mintinline{rust}|#[cfg(test)]| et les tests d'intégration contenus dans le dossier \texttt{/tests}
\end{itemize}
\subsubsection{Intégration continue (CI)}
Pour ce projet, afin d'utiliser des outils de CI adaptés, nous avons utilisé GitHub Actions, un outil d'automatisation intégré à GitHub qui permet d'effectuer des vérifications sur les travaux poussés vers le dépôt.
Cet outil se base sur 3 choses essentielles:
\begin{itemize}
    \item Le déclencheur: quel événement va déclencher le travail de vérification (\texttt{git push} ou une PR).
    \item Le plan: le fichier crée en \texttt{.yml} permettant d'orchestrer le travail à faire.
    \item Le runner: serveur distant intégré à GitHub qui offre un environnement pour lancer nos vérifications.
\end{itemize}

Afin de rendre notre travail moins permissif à différents types d'erreurs, nous avons couplé les outils de GitHub Actions avec un autre principe, celui des pre-commit.
Nous mettons en place un outil qui nous permet de faire des vérifications avant la création du commit donc en local. Ce qui rend ce principe très utile est sa possibilité d'annuler un commit s'il ne respecte pas 
les règles dictées par le fichier \texttt{.pre-commit-config.yaml}. Si, par exemple, un développeur tente de commit des fichiers contenant des mauvaises pratiques de programmation (erreur clippy),
au moment de taper \texttt{git commit}, l'outil pre-commit annulera la commande.

Pour enrichir notre travail, nous utilisons des \texttt{hooks} qui sont des outils téléchargeables nous permettant de mettre en place de nouvelles vérifications et modifications utiles au projet:
\begin{itemize}
    \item \texttt{trailing-whitespace}: retire les espaces en trop dans le code.
    \item \texttt{end-of-file-fixer}: rajoute un saut de ligne unique vide à la fin de chaque fichier.
    \item  \texttt{check-yaml}: vérifie l'intégrité de nos fichiers de configuration de pipelines(\texttt{.yml} et \texttt{.yaml}).
    \item \texttt{check-added-large-files}: bloque le commit si celui si contient des fichiers trop lourds.
\end{itemize}

Quand tout cela a été mis en place, nous pouvons ajouter nos outils Rust présentés plus tôt. 
Dans la structure décidée avec Monsieur De Jonckère, nous avons fait le choix de faire 3 vérifications avant le commit:
les hooks précedemment cités, le format du code (\texttt{cargo fmt --check}) et les erreurs de logique et pratiques (\texttt{cargo clippy -D warnings}).
Il est important de noter un détail sur ce travail de pre-commit, nous avions 2 choix pour la maintenabilité: utiliser un dépôt en ligne contenant les outils Rust ou
utiliser les toolchains locales des développeurs. Nous avons fait le choix d'utiliser les toolchains locales en considérant que toute l'équipe mettait cargo régulièrement à jour, de plus
il nous apparaissait plus logique d'utiliser un outil stable en local plutôt qu'un dépôt en ligne pouvant devenir déprécié.
\begin{figure}[h]
    \centering
    \includegraphics[width=0.55\textwidth]{run-pre-commit-fail.png}
    \caption{Exemple de cas où une erreur est détectée par pre-commit}
    \label{fig:pre-commit-fail}
\end{figure}

Après avoir mis le travail de vérification locale en place, nous avons fait le choix d'effectuer également des vérifications après envoi du travail vers le dépôt distant.
Pour cela, nous demandons au fichier \texttt{ci.yml} d'aller chercher les outils utilisés en pre-commit et de les relancer, de cette manière les vérifications locales et distantes restent alignées. Ainsi, nous effectuons une double vérification
au cas où un développeur oublie d'installer pre-commit sur sa machine.

Nous remarquons que l'outil \texttt{cargo test} n'a pas été utilisé dans les vérifications locales, c'est également un choix. En effet, cette commande lance les tests de l'ensemble du projet, un développeur travaillant 
sur un module indépendant n'a pas d'intérêt à perdre du temps en lançant une vérification sur l'ensemble du projet à chaque fois qu'il fait un commit, il ne va tester que ce sur quoi il travaille.
C'est pour cette raison que les tests (et la génération du fichier de coverage) sont lancés uniquement sur le dépôt distant.


Pour résumer, nous avons des vérifications locales grâce au fichier \texttt{.pre-commit-config.yaml}\label{CI/CD} (Code en annexe \ref{CodeCIprecommit}) relancées en distant en plus de nos tests grâce au fichier \texttt{ci.yml}\label{CI/CD} (Code en annexe \ref{CodeCIyml}). Tout cela a été accepté sur le dépôt d'origine et profite maintenant à l'équipe de développement.
\begin{figure}[h]
    \centering
    \includegraphics[width=0.55\textwidth]{pre-commit-exemple.png}
    \caption{Résultats des vérifications du commit du 28 avril de Monsieur De Jonckère}
    \label{fig:pre-commit-exemple}
\end{figure}
\subsection{Test A-SABR}
\subsubsection[Module : Contact Manager]{Contact Manager}

Les \textit{Contact Managers} sont les composants responsables de la planification des transmissions sur un contact réseau dans A-SABR. Leur rôle est de déterminer si un bundle peut être transmis à un instant donné, de calculer les dates associées à cette transmission, puis de mettre à jour l’état du contact lorsque celle-ci est effectivement planifiée.

Tous les gestionnaires de contacts implémentent une interface commune reposant principalement sur deux opérations :
\begin{itemize}
    \item \texttt{dry\_run\_tx}, qui simule une transmission sans modifier l’état interne du contact,
    \item \texttt{schedule\_tx}, qui planifie effectivement la transmission et met à jour les ressources disponibles.
\end{itemize}

Plusieurs implémentations de \textit{Contact Manager} coexistent dans le projet afin de proposer différents niveaux de précision et de modélisation. Elles sont regroupées en deux grandes familles : les gestionnaires \textit{Legacy}, reposant sur des modèles historiques plus simples, et les gestionnaires basés sur la segmentation, offrant une représentation plus fine des contacts.

\paragraph{Legacy :}~\\\\
Les gestionnaires \textit{Legacy} correspondent aux premières implémentations de gestionnaires de contacts présentes dans A-SABR. Ils correspondent à une approche plus ancienne de gestion des contacts car ils reposent sur une modélisation plus simple du contact, avec moins de granularité que les approches plus récentes comme la segmentation. Ils restent présents dans la librairie pour des raisons de compatibilité et de comparaison, mais ne constituent plus l’approche principale utilisée pour représenter finement les transmissions.

Ces gestionnaires restent néanmoins importants à tester, car ils font encore partie du code existant et peuvent être utilisés comme référence pour vérifier le comportement d’autres modules.

\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{Images/legacy_managers_comparison.png}
\caption{Comparaison des comportements des gestionnaires Legacy (ETO, EVL, QD).}
\label{fig:legacy_managers_comparison}
\end{figure}

Le sous-module contient trois implémentations : EVL, QD et ETO. Ces implémentations sont générées à l'aide d'une macro qui prend différents paramètres :

\begin{itemize}
    \item \texttt{\$add\_delay} : indique si le délai de queue doit être pris en compte pour le calcul de \texttt{tx\_start}
    \item \texttt{\$auto\_update} : indique si la queue est mise à jour automatiquement après un \texttt{schedule\_tx} ou manuellement
    \item \texttt{\$prio\_count} : donne le nombre de niveaux de priorités
    \item \texttt{\$with\_budget} : indique si des budgets sont appliqués lorsque des priorités sont présentes
\end{itemize}

EVL, QD et ETO se distinguent par leurs valeurs de ces paramètres. EVL est le manager le plus simple : il se contente de consommer le volume d'un bundle à chaque \texttt{schedule\_tx} sur un contact. La queue est mise à jour automatiquement et il n'y a pas de délai. QD est similaire à EVL, à la différence que \texttt{tx\_start} est décalé depuis le début du contact en fonction du volume déjà réservé. Enfin, ETO est le plus complexe des trois : le décalage de la queue doit être géré manuellement et débute à \texttt{at\_time}, soit le temps courant.

Chacun de ces fichiers instancie la macro pour générer trois variantes : un manager de base sans priorités, un avec priorités, et un autre avec priorités et budgets, un budget étant la quantité maximale de données qu'un bundle peut réserver sur un contact.\\\\



Pour tester ces implémentations, et sachant qu'elles partagent une base de tests en commun, nous avons mis en place des macros de tests dans un fichier \texttt{test\_helpers.rs} afin d'éviter la duplication de code. Quatre macros ont été définies :

\begin{itemize}
    \item \texttt{generate\_common\_tests!} : regroupe les tests communs à tous les managers, en testant le comportement de \texttt{dry\_run\_tx} et \texttt{schedule\_tx} en termes de timing, de volume et de conditions limites.

    \item \texttt{generate\_auto\_update\_tests!} : teste les comportements liés à la mise à jour automatique de la queue, notamment la saturation du contact et l'isolation des priorités. Ces tests ne s'appliquent pas à ETO puisque \texttt{\$auto\_update=false} pour lui.

    \item \texttt{generate\_budget\_tests!} : vérifie que les limites de budget par priorité sont bien respectées.

    \item \texttt{generate\_budget\_auto\_update\_tests!} : teste l'indépendance des budgets entre priorités. Ces tests ont été séparés de \texttt{generate\_budget\_tests!} car ils nécessitent \texttt{auto\_update=true}, ce qui exclut ETO. \\
\end{itemize}

En complément de ces tests communs, des tests spécifiques ont été ajoutés afin de valider les comportements propres à chaque implémentation.

Pour \texttt{ETO}, les tests spécifiques vérifient que la file d’attente n’est pas automatiquement mise à jour après une planification. Ainsi, plusieurs appels successifs à \texttt{schedule\_tx} peuvent produire le même résultat tant qu’aucune mise à jour manuelle de la file n’est effectuée.

Pour \texttt{EVL}, les tests permettent de confirmer que l’occupation de la file d’attente influence la capacité disponible, sans modifier directement la date de début de transmission. Autrement dit, le volume restant diminue, mais \texttt{tx\_start} reste indépendant de cette occupation.

Enfin, pour \texttt{QD}, les tests valident que l’occupation de la file peut décaler la date effective de début de transmission. Ils vérifient également qu’une transmission peut devenir impossible si ce décalage repousse la fin de transmission au-delà de la fenêtre du contact.

L’ajout de ces tests spécifiques permet donc de compléter la couverture existante sur les gestionnaires \textit{Legacy}, tout en documentant plus clairement les différences de comportement entre \texttt{ETO}, \texttt{EVL} et \texttt{QD}.\\

Toute ces modifications sur la partie Legacy ont fait l'objet de 1 Pull request en attente d'acceptation de Samy/Yassine pour 502 lignes de code ajoutées (PR \texttt{\#54})

\paragraph{Segmentation :}~\\\\
Le module \textit{Segmentation} propose une modélisation plus fine des contacts que les gestionnaires \textit{Legacy}, en représentant les ressources disponibles sous forme d’intervalles temporels exploitables dynamiquement lors de la planification. Cette approche permet de gérer précisément l’occupation progressive d’un contact au fil des transmissions.

Le gestionnaire de segmentation repose sur trois structures principales :
\begin{itemize}
    \item les segments de débit (\texttt{rate\_intervals}),
    \item les segments de délai (\texttt{delay\_intervals}),
    \item les intervalles libres (\texttt{free\_intervals}), représentant les portions du contact encore disponibles pour de futures transmissions.
\end{itemize}

Chaque segment est défini par un intervalle temporel et une valeur associée, permettant de modéliser localement les caractéristiques du contact.

Deux opérations principales sont exposées :
\begin{itemize}
    \item \texttt{dry\_run\_tx} : simulation d’une transmission sans modification de l’état interne ;
    \item \texttt{schedule\_tx} : planification effective d’une transmission avec mise à jour des intervalles libres.
\end{itemize}

Lorsqu’une transmission est demandée, le gestionnaire recherche un intervalle libre compatible avec l’instant considéré (\texttt{at\_time}). Si l’insertion est possible, la durée de transmission est calculée en tenant compte des segments de débit traversés. Une même transmission peut ainsi s’étendre sur plusieurs segments successifs si nécessaire.

\begin{figure}[H]
\centering
\includegraphics[width=0.65\textwidth]{Images/segmentation_figure.png}
\caption{Évolution des intervalles libres lors de la planification successive de plusieurs bundles.}
\label{fig:free_intervals}
\end{figure}

Comme illustré ci-dessus, chaque nouvelle planification entraîne une mise à jour dynamique des intervalles libres. Lorsqu’un bundle est inséré dans un intervalle disponible, celui-ci peut être réduit ou découpé en plusieurs sous-intervalles afin de conserver une représentation cohérente de l’espace restant. Ce mécanisme permet de maintenir un état précis du contact après chaque réservation.

Les tests implémentés dans le fichier \texttt{seg.rs} ont permis de valider plusieurs comportements essentiels :
\begin{itemize}
    \item insertion correcte d’un bundle dans un intervalle libre ;
    \item enchaînement de plusieurs planifications successives ;
    \item mise à jour cohérente des intervalles libres après réservation ;
    \item gestion correcte de l’instant \texttt{at\_time} ;
    \item prise en compte de transmissions traversant plusieurs segments de débit.
\end{itemize}

Ces tests garantissent la cohérence entre les fonctions \texttt{dry\_run\_tx} et \texttt{schedule\_tx}, ainsi que la robustesse de la mise à jour des intervalles libres après chaque planification.

\vspace{0.3cm}

\textbf{Difficultés rencontrées :}

Plusieurs difficultés ont été rencontrées lors de l’implémentation des tests de segmentation. La manipulation d’intervalles temporels a notamment entraîné des erreurs d’indices, en particulier lors de l’accès à des segments inexistants. Ces problèmes ont été corrigés en ajoutant des vérifications sur les bornes des intervalles et en sécurisant les accès aux structures de données.

La gestion des cas limites s’est également révélée délicate, notamment pour les situations où l’instant de transmission se situe en dehors des intervalles définis ou à leurs frontières. Certains tests ont dû être ajustés afin de respecter strictement les contraintes temporelles du modèle.

Enfin, des contraintes liées aux outils d’intégration continue ont été rencontrées. Les hooks \textit{pre-commit}, incluant \texttt{cargo fmt} et \texttt{cargo clippy}, ont empêché certains commits en raison de problèmes de formatage ou de warnings. Ces difficultés ont été résolues en appliquant les corrections nécessaires et en respectant les règles imposées par le projet.

Ces difficultés ont permis de mieux comprendre les invariants du modèle et de renforcer la robustesse des tests implémentés.\\

Toutes ces modifications sur la partie Segmentation ont fait l'objet de 1 Pull request acceptée de Yassine pour 300 lignes de code ajoutées (PR \texttt{\#53})\\
\paragraph{PrioritySegmentation :}~\\\\
Le module \texttt{PrioritySegmentation} abrégé en \texttt{pseg}, quant à lui, reprend exactement le même principe 
que le module \texttt{Segmentation} mais ajoute une gestion de la priorité des paquets.
Le principe central de ce module est le suivant: un paquet de plus haute priorité peut remplacer un segment occupé par un paquet
de plus basse priorité. L'intérêt de cette approche est de pouvoir mettre en place une hiérarchie dans l'importance des paquets envoyés,
dans le cas du routage spatial, nous préfèrerons faire passer un message d'urgence avant un retour d'analyse quelconque.

Ci-dessous un schéma d'explication du fonctionnement de \texttt{pseg}:

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{schémapseg.png}
    \caption{Schéma de fonctionnement de PrioritySegmentation}
    \label{fig:SchemaPseg}
\end{figure}


Le fonctionnement de ce module est assez similaire à celui de \texttt{Segmentation} expliqué précedemment. 
Nous retrouvons les segments de débit et de délai pour construire l'univers dans lequel nous travaillerons. Cependant
nous n'utilisons pas ici des intervalles libres mais des segments appelés \texttt{bookings} qui seront initialisés avec 
une date de début et de fin avec une priorité -1 pour indiquer qu'ils sont libres.

Nous retrouvons le modèle des deux fonctions centrales: \texttt{dry\_run\_tx} pour la simulation et \texttt{schedule\_tx}
pour l'application effective des modifications. L'analyse algorithmique du module sera traitée dans la partie 3.

Dans le cadre du projet, nous avons apporté certaines modifications au module:
\begin{itemize}
    \item Optimisation et correction: nous avons ajouté une procédure qui fusionne les segments \texttt{bookings} adjacents de même priorité.
    Nous avons également corrigé un bug où la fonction \texttt{dry\_run\_tx} renvoyait un échec lors de l'insertion du bundle à tort lorsque ce dernier devait être placé sur le dernier segment des \texttt{bookings}.
    \item Automatisation des tests: nous avons également ajouté de nombreux tests pour assurer la fiabilité du module.
\end{itemize}

Nous avons conçu une fonction centrale \texttt{start\_test} permettant de faciliter les tests de \texttt{pseg} dont voici le fonctionnement: \label{pseg} (Code en annexe \ref{CodeStartTest}).
\begin{description}
    \item[Paramètres d'entrée: ] La fonction prend 3 arguments:
    \begin{itemize}
        \item \texttt{input}: une liste d'objets \texttt{InputSeg} pour créer l'environnement (segments de délai, de débit et potentiels \texttt{bookings} préexistants).
        \item \texttt{output}: une liste d'objets \texttt{OutputSeg} contenant les \texttt{bookings} attendus en sortie.
        \item \texttt{requests}: une liste de tuples contenant: le paquet à insérer (\texttt{Bundle}), le moment de l'insertion (\texttt{at\_time}) et \texttt{true} si nous nous attendons à une réussite de l'insertion, \texttt{false} sinon.
    \end{itemize} 
    \item[Construction de l'environnement: ] la fonction va interpréter les valeurs de \texttt{input}. Par exemple, la commande \mintinline{rust}{let input = vec![InputSeg::Delay(0.0, 200.0, 4.0), InputSeg::Rate(0.0, 200.0, 100.0)];} génère un segment de délai (de 0 à 200, délai de 4) et un segment de débit (de 0 à 200, débit de 100). Si aucun \texttt{booking} initial n'est donné, l'environnement démarre vide.
    \item[Vérifications: ] la fonction vérifie ces 3 choses:
    \begin{itemize}
        \item L'égalité des valeurs de retour des fonctions \texttt{dry\_run\_tx} et \texttt{schedule\_tx}.
        \item L'état de l'insertion du bundle: si dans le champ \texttt{requests} nous nous attendions à ce que l'insertion soit un succès, la fonction vérifie que c'est bien le cas.
        \item La cohérence entre le paramètre \texttt{output} et la sortie réellement construite: la fonction va construire ce que nous avons appelé \texttt{actual\_output} qui est la sortie réelle.nous vérifions donc que cette sortie soit bien égale à celle attendue dans \texttt{output}.
    \end{itemize}
\end{description}

La force de cette fonction est qu'elle permet de faire 3 tests communs en un seul appel pour différents scénarios. 
De plus, si des collaborateurs souhaitent écrire de nouveaux tests, ils n'auront besoin que de construire les valeurs attendues
sans avoir à réecrire à chaque fois les mêmes principes de tests.



\subsubsection[Module : Pathfinding]{Pathfinding}
Le rôle de ce module est de calculer, à partir d'un \gls{bundle} et d'un instant donnée, la meilleurs route vers
une destination en tenant compte des contraites temporelles de chaque contact.
Il expose un Trait, \texttt{PathFinding}, implémentée par trois variantes : Node, Contact et Hybride, détaillée ci-après,
ainsi qu'un sous module \texttt{limiting\_contact} qui permet de supprimer temporairement le contact le plus limitant d'une route 
pour forcer la découverte de routes alternatives. 

Chaque variante est instanciée avec une métrique de distance, soit \textbf{Hop} qui minimise le nombre de saut, 
soit \textbf{\gls{sabr}} qui minimise le temps d'arrivée.
\paragraph{Try\_make\_hop :}~\\\\

\texttt{try\_make\_hop} est la fonction centrale du module de pathfinding. Son rôle est de déterminer, depuis un nœud source à un instant donné, le meilleur contact disponible pour acheminer un bundle vers un nœud voisin en un seul saut. Elle prend en entrée un \texttt{RouteStage} source qui représente l'état courant du routage (temps d'arrivée au nœud, délai cumulé, expiration), une liste de contacts triés par ordre de début, ainsi que les nœuds émetteur et récepteur. Elle retourne soit le meilleur saut trouvé, soit \texttt{None} si aucun contact ne permet la transmission.

La fonction parcourt chaque contact fourni en entrée à partir d'un \texttt{first\_contact\_index}, ce qui permet un \textit{lazy pruning} : les contacts déjà dépassés par un saut précédent dans Dijkstra n'ont pas besoin d'être reconsidérés. Pour chaque contact, elle appelle \texttt{dry\_run\_tx} du Contact Manager pour simuler la transmission, puis retient le contact permettant la date d'arrivée \texttt{rx\_end} la plus rapide. Elle s'arrête dès que le début d'un contact est postérieur au meilleur \texttt{rx\_end} déjà trouvé, les contacts étant supposés triés. Si un contact est retenu, elle construit un nouveau \texttt{RouteStage} avec les métadonnées mises à jour : \texttt{hop\_count} incrémenté, \texttt{cumulative\_delay} mis à jour et \texttt{expiration} calculée comme le minimum entre la fin du contact et l'expiration propagée depuis la source.

\texttt{try\_make\_hop} intègre également des \textit{features}, c'est-à-dire des portions de code activées uniquement si la feature est spécifiée au lancement. Nous trouvons ainsi \texttt{contact\_suppression}, \texttt{node\_tx} et \texttt{node\_rx} qui permettent de faire échouer le \texttt{dry\_run} et donc de renvoyer \texttt{None}, ainsi que \texttt{node\_proc} qui permet le traitement du bundle avant l'émission, pouvant décaler le temps d'envoi.



Afin de tester cette fonction, nous avons créé un \textit{mock} de \texttt{NodeManager} dans le fichier \texttt{test\_helpers} du dossier pathfinding. En effet, nos tests doivent contrôler le comportement des nœuds émetteurs et récepteurs avec les features \texttt{node\_tx} et \texttt{node\_rx}, contrairement aux autres fichiers qui n'ont besoin que de \texttt{NoManagement}. Ce mock permet d'implémenter et de contrôler un \texttt{NodeManager} avec quatre fonctions : \texttt{accepting()} qui accepte toujours, \texttt{refusing\_rx()} et \texttt{refusing\_tx()} qui rejettent la transmission d'un côté, et \texttt{processing(delay)} qui simule un délai de traitement du bundle. Les fonctions helpers telles que \texttt{make\_node}, \texttt{make\_contact}, \texttt{make\_source} et \texttt{make\_bundle} ont été rendues génériques sur \texttt{NM} pour pouvoir être partagées entre les deux types de manager.

Les tests mis en place couvrent les cas suivants :

\begin{itemize}
    \item \textbf{Cas de base} : appel avec une liste de contacts vide, un \texttt{first\_contact\_index} dépassant le slice, ou un bundle trop volumineux pour un contact donné. Nous vérifions que la fonction renvoie bien \texttt{None} dans ces cas.

    \item \textbf{Transmission valide} : test avec un seul contact compatible. Nous vérifions que \texttt{Some} est bien retourné.

    \item \textbf{Lazy pruning} : test avec \texttt{first\_contact\_index=1} qui saute un contact valide. Nous vérifions que le résultat correspond bien au second contact et non au premier.

    \item \textbf{Feature \texttt{contact\_suppression}} : si tous les contacts sont supprimés, la fonction doit renvoyer \texttt{None}. Si un seul contact parmi deux est supprimé, le second doit bien être choisi.

    \item \textbf{Mock NodeManager (\texttt{node\_tx}, \texttt{node\_rx}, \texttt{node\_proc})} : ces trois tests ne peuvent pas utiliser \texttt{HopContext} car ils nécessitent des managers différents sur \texttt{tx} et \texttt{rx}. Nous vérifions qu'un nœud refusant l'émission ou la réception bloque le saut, et qu'un délai de traitement décale correctement le \texttt{sending\_time} et donc le \texttt{at\_time} dans le résultat.

    \item \textbf{Sélection du meilleur contact} : test avec 4 contacts dont certains sont élagués par le lazy pruning. Nous vérifions que le contact avec le \texttt{rx\_end} minimal est bien choisi et que les métadonnées finales sont correctes.

    \item \textbf{Test sur 2 sauts} : le résultat du premier saut est utilisé comme source pour le second. Nous vérifions que \texttt{expiration} et \texttt{cumulative\_delay} se sont bien propagés et que le meilleur contact est toujours sélectionné avec les bonnes métadonnées.
\end{itemize}

\paragraph{Contact, Hybrid et Node parenting}

Le module de pathfinding propose trois variantes de parenting : \texttt{Node}, \texttt{Contact} et \texttt{Hybrid}. Ces variantes reposent toutes sur une logique de recherche de chemin de type Dijkstra, mais elles ne manipulent pas le \gls{mg} avec la même granularité. Leur différence principale vient de la manière dont elles représentent les états explorés et conservent les routes candidates pendant le parcours.

\begin{center}
\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.2}

\begin{tabular}{|p{0.17\textwidth}|p{0.35\textwidth}|p{0.38\textwidth}|}
\hline
\textbf{Variante} & \textbf{Principe} & \textbf{Conséquence sur le routage} \\
\hline

\textbf{Node Parenting}
& Explore le \gls{mg} principalement nœud par nœud.
& Conserve une seule meilleure route par nœud selon la métrique choisie. Cette approche est plus simple, mais peut éliminer certaines routes alternatives trop tôt. \\
\hline

\textbf{Contact Parenting}
& Explore le \gls{mg} au niveau des contacts plutôt qu'au niveau des nœuds.
& Permet de distinguer plusieurs contacts entre deux mêmes nœuds et peut donc identifier des routes différentes lorsque plusieurs fenêtres de communication existent. \\
\hline

\textbf{Hybrid Parenting}
& Combine les deux approches en conservant plusieurs routes candidates dans une zone de travail.
& Permet de garder des routes alternatives pertinentes grâce aux fonctions \texttt{can\_retain} et \texttt{must\_prune}, au prix d'une logique plus complexe. \\
\hline

\end{tabular}

\captionof{table}{Comparaison des variantes de parenting du module \texttt{pathfinding}}
\label{tab:parenting-comparison}
\end{center}

Chaque variante peut être instanciée avec une métrique de distance différente. La métrique \texttt{Hop} cherche à minimiser le nombre de sauts, tandis que la métrique \texttt{SABR} cherche à minimiser le temps d'arrivée. Ainsi, un même réseau peut produire des routes différentes selon la variante de parenting et la métrique utilisée.

Chaque algorithme de parenting possède également trois déclinaisons générées par macros. Comme il existe trois approches principales, cela donne neuf variantes possibles au total :

\begin{itemize}
    \item \texttt{Path} : calcule un chemin vers une destination et s'arrête lorsque celle-ci est atteinte ;
    \item \texttt{PathExcl} : calcule un chemin vers une destination en excluant un nœud précis ;
    \item \texttt{TreeExcl} : explore l'ensemble du \gls{mg} en tenant compte d'une exclusion.
\end{itemize}

Le principe des tests est volontairement systématique : plusieurs \gls{mg} sont construits, puis chaque variante est exécutée avec les métriques \texttt{Hop} et \texttt{SABR}. Les résultats attendus sont principalement le nombre de sauts, le temps d'arrivée et, pour les variantes avec exclusion, l'échec attendu lorsqu'un nœud indispensable est exclu.

Dans un souci de lisibilité et de maintenabilité, les fonctions de création des objets de test, comme les nœuds, contacts, bundles et multigraphs, ont été regroupées dans un fichier \textbf{test\_helpers.rs}. Les fichiers de tests peuvent ainsi se concentrer sur les scénarios à vérifier plutôt que sur la construction répétée des données.\\

\subsubsection[Module : Routing]{Routing}

Le dossier \texttt{routing} regroupe l'ensemble de la logique de routage du projet. Son rôle est de prendre un bundle en entrée et de retourner un \texttt{RoutingOutput}, c'est-à-dire la liste des premiers sauts à emprunter pour acheminer ce bundle vers sa ou ses destinations.

Le fichier \texttt{mod.rs} de ce dossier définit les bases communes à tous les routeurs. Il contient d'abord le trait \texttt{Router}, qui impose à toute implémentation de routeur de fournir une méthode \texttt{route()} prenant en entrée le nœud source, le bundle à router, le temps courant et une liste de nœuds exclus. Il définit ensuite \texttt{RoutingOutput}, une structure qui associe à chaque premier saut (représenté par un contact) la liste des \texttt{RouteStage} des destinations atteignables via ce saut. Cette structure expose également \texttt{lazy\_get\_for\_unicast()}, une méthode qui recherche le premier saut menant à une destination précise, utilisée notamment dans les tests.

Le fichier contient aussi des fonctions partagées par tous les routeurs, comme \texttt{schedule\_unicast}, \texttt{schedule\_multicast}, \texttt{dry\_run\_unicast\_path}, \texttt{dry\_run\_unicast\_tree} et \texttt{dry\_run\_multicast}. Ces fonctions implémentent deux phases distinctes. Le \textit{dry run} simule le parcours d'un chemin ou d'un arbre sans réserver de ressources, pour vérifier qu'un bundle peut effectivement atteindre sa destination en fonction des contacts. Le \textit{scheduling} effectue ensuite la réservation réelle en appelant \texttt{schedule()} sur chaque \texttt{RouteStage} du chemin.

Le dossier contient trois implémentations concrètes du trait \texttt{Router} : CGR, VolCGR et SPSN. Dans le cadre de notre projet, nous n'avons travaillé que sur VolCGR et SPSN. 

VolCGR est une évolution de CGR qui est la méthode de la NASA, et Spsn qui est une implémentation toute nouvelle venant d'A-SABR.\\

Des premiers tests ont également été développés pour le module routing. 
Cependant, par manque de temps et à la suite d’une PR récente ayant refactorisé la gestion des nœuds et des contacts,
leur intégration propre dans la version finale du projet n’a pas pu être menée à terme. 
Ces travaux ne sont donc pas présentés en détail dans ce rapport, mais restent disponibles sur une branche
dédiée de notre dépôt afin de pouvoir être repris et finalisés ultérieurement.

\subsection{BP-Socket}

Le projet BP-Socket a pour objectif de fournir une interface de type socket pour le Bundle Protocol, utilisé dans les réseaux \gls{dtn}. L’idée est de permettre à une application d’envoyer et de recevoir des \gls{bundle} avec une API proche de celle utilisée pour des communications classiques en UDP ou TCP.

Dans son architecture initiale, BP-Socket repose sur un module kernel ajoutant une nouvelle famille de sockets, \texttt{AF\_BP}. Sans ce module, un appel à \texttt{socket(AF\_BP, ...)} n’est pas reconnu par le système et ne permet donc pas d’obtenir un descripteur de fichier utilisable. Or, maintenir et charger un module kernel complexifie fortement l’installation et les tests.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{bp_kernel.png}
    \caption{Architecture initiale de BP-Socket avec module kernel}
    \label{fig:bp-kernel}
\end{figure}

L’objectif de notre travail a donc été de remplacer cette partie kernel par une solution en espace utilisateur, fondée sur \texttt{LD\_PRELOAD}. Ce mécanisme permet de charger une bibliothèque partagée avant les bibliothèques standards, comme la \texttt{glibc}. Les fonctions définies dans cette bibliothèque peuvent alors intercepter certains appels utilisés par l’application, comme \texttt{socket}, \texttt{bind}, \texttt{sendto}, \texttt{recv} ou \texttt{close}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{bp_interceptor.png}
    \caption{Architecture visée de BP-Socket sans module kernel}
    \label{fig:bp-preload}
\end{figure}

Pour atteindre cet objectif, plusieurs modifications étaient nécessaires :

\begin{itemize}
    \item supprimer les fichiers liés au module kernel et nettoyer les \textbf{Makefile} ;
    \item nettoyer le daemon et supprimer les fichiers liés à General Netlink ;
    \item définir un nouveau protocole de communication entre le hook et le daemon, incluant notamment les opérations \texttt{OPEN\_ENDPOINT}, \texttt{CLOSE\_ENDPOINT}, \texttt{SEND\_BUNDLE} et \texttt{RECV\_BUNDLE} ;
    \item écrire le hook chargé d’intercepter les appels aux fonctions concernées ;
    \item écrire un fichier chargé de gérer l’état des descripteurs de fichier créés par notre version de \texttt{socket} ;
    \item modifier le daemon avec les nouvelles fonctions relatives au hook, notamment l’envoi de bundles, la connexion et les messages internes ;
    \item modifier l’interface \gls{ion} afin qu’elle utilise les nouvelles fonctions du daemon.
\end{itemize}

Le cœur de cette nouvelle architecture repose sur le hook chargé d’intercepter les appels aux fonctions réseau. Le schéma suivant résume le principe général de ce mécanisme.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{schemaPreload.png}
    \caption{Principe d’interception des appels avec \texttt{LD\_PRELOAD}}
    \label{fig:ld-preload}
\end{figure}

Comme illustré dans la figure~\ref{fig:ld-preload}, lorsqu’une application effectue un appel lié à \texttt{AF\_BP}, celui-ci est intercepté par la bibliothèque préchargée. Le hook peut alors appliquer un comportement spécifique à BP-Socket, par exemple en transmettant les informations nécessaires au daemon via une socket Unix. À l’inverse, les appels qui ne concernent pas \texttt{AF\_BP} sont simplement redirigés vers les fonctions standards de la \texttt{glibc}. Cette séparation permet de conserver le comportement normal de l’application pour les sockets classiques, tout en redéfinissant uniquement les appels propres à BP-Socket.

Concrètement, le hook maintient une table interne associant les descripteurs de fichier créés pour BP-Socket à leurs informations de connexion. Lorsqu’une application ouvre une socket \texttt{AF\_BP}, le hook crée en réalité une connexion vers le daemon. Les opérations suivantes, comme l’ouverture d’un endpoint ou l’envoi d’un bundle, sont alors traduites en messages internes envoyés à ce daemon, qui se charge ensuite d’interagir avec \gls{ion}.

Cette approche a permis de rendre la partie émission fonctionnelle dans l’environnement testé : des messages peuvent être envoyés depuis \gls{ion} vers \textmu D3TN sans passer par le module kernel. La partie réception reste cependant à finaliser, notamment pour gérer correctement la réception des bundles et la libération des ressources associées après traitement.


\newpage
\section{Algorithmes et Structures de Données}
\subsection{PrioritySegmentation}
Dans cette partie, nous allons détailler le fonctionnement algorithmique du module \texttt{PrioritySegmentation (pseg)}.
\subsubsection*{Algorithme \texttt{dry\_run\_tx} \label{CodeDRpseg} (Code en annexe \ref{PsegDR})}
\begin{description}
    \item[Les paramètres d'entrée :] La fonction prend 4 paramètres :
    \begin{itemize}
        \item \mintinline{rust}{&self} : la référence vers l'instance du \texttt{PSegmentationManager} contenant toute la logique. Un détail a son importance ici : comme la fonction ne fait qu'une simulation, nsou limitons l'attribut à une lecture seule en ne mettant pas le mot-clé \mintinline{rust}{mut}, indiquant à Rust que cette variable n'est pas mutable.
        \item \mintinline{rust}{contact_data: &ContactInfo} : c'est une structure contenant des attributs \texttt{source} et \texttt{dest} de type \texttt{NodeID}, et deux attributs \texttt{start} et \texttt{end} de type \texttt{Date}. Cette structure représente les limitations physiques : \texttt{source} et \texttt{dest} sont les nœuds concernés par le contact, et \texttt{start} et \texttt{end} représentent la fenêtre temporelle du contact.
        \item \mintinline{rust}{at_time: Date} : c'est l'instant t à partir duquel nous souhaitons insérer le paquet.
        \item \mintinline{rust}{bundle: &Bundle} : la référence vers le paquet que nous souhaitons transmettre.
    \end{itemize}
    \item[La valeur renvoyée :] la fonction renvoie un objet de type \texttt{Option<ContactManagerTxData>} donc deux valeurs sont possibles:
    \begin{itemize}
        \item Le cas d'échec \texttt{None}: la transmission est impossible.
        \item Le cas de succès \texttt{Some(ContactManagerTxData)}: un créneau utilisable a été trouvé par la fonction. Nous renvoyons donc un objet de type \texttt{struct ContactManagerTxData}
        qui comprend toutes les informations du paquet inséré: les heures de début et de fin d'émission représentés par \texttt{tx\_start} et \texttt{tx\_end}, les heures de réception prenant en compte le délai de propagation représentés par \texttt{rx\_start} et \texttt{rx\_end}
        et la date d'expiration du paquet représenté par le champ \texttt{expiration}.
    \end{itemize}
    \item[Le fonctionnement algorithmique :] détaillons les étapes de fonctionnement:
    \begin{itemize}
        \item Les valeurs à initialiser: deux variables: \texttt{tx\_start} initialisée à \texttt{at\_time}
        et \texttt{tx\_end\_opt} représentant l'état de la recherche, \texttt{None} si nous cherchons un point de départ et \texttt{Some(date)} sinon.
        \item La boucle: la fonction utilise une boucle \texttt{for} qui va parcourir l'ensemble des N segments.
        \item Les vérifications préalables: avec la ligne \mintinline{rust}{if seg.end <= at_time { continue; }} nous ignorons les segments qui se trouvent avant du paramètre \texttt{at\_time}.
        Grâce à la ligne \mintinline{rust}{if bundle.priority <= seg.val} \mintinline{rust}{tx_end_opt = None; continue;}, nous ignorons également les segments dont la priorité est supérieur à celle du paquet à insérer.
        \item Le calcul: la valeur de \texttt{tx\_end\_opt} nous indique dans quel cas nous sommes:
        \begin{itemize}
            \item Cas \texttt{None}: un espace libre a été trouvé, nous fixons \texttt{tx\_start} à \texttt{at\_time} et nous calculons les besoins du paquet.
            Si tout rentre dans le segment en cours, nous pouvons renvoyer le résultat, sinon nous passons dans l'état \texttt{Some(tx\_end)} pour aller chercher de l'espace dans le ou les segments suivants.
            \item Cas \texttt{Some(tx\_end)}: nous cherchions déjà à insérer dans le segment précédent donc nous cherchons de l'espace supplémentaire dans ce segment. Si \texttt{tx\_end} tombe dans ce segment, nous renvoyons le résultat sinon nous répètons l'opération.
        \end{itemize}
    \end{itemize}
    \item[La complexité en temps :]
    Dans le pire des cas, nous avons une complexité de l'ordre de \texttt{O(N * r)} avec N le nombres de segments \texttt{booking} er r le nombre de segments de débit.
    En effet, à chaque fois qu'un créneau potentiellement libre est trouvé, le calcul de la fin de transmission est relancé par \mintinline{rust}|super::get_tx_end|, cette fonction doit reparcourir les r intervalles de débit.
    Donc dans le pire cas, cette sous-recherche est lancée à chaque nouveau segment libre soit une complexité \texttt{O(N * r)}.

    Cependant, il est intéressant de noter que physiquement le délai est une valeur qui reste souvent fixe, il sera donc représenté dans la majorité des cas par un segment unique.
    Donc la plupart du temps, nous aurons plutôt une compelexité de l'ordre de \texttt{O(N)}.
    
\end{description}

\subsubsection*{Algorithme \texttt{schedule\_tx} \label{CodeSCpseg} (Code en annexe \ref{PsegSC})}
\begin{description}
    \item[Les paramètres d'entrée :]
    Ce sont les mêmes que \texttt{dry\_run\_tx} mais à une différence près: nous utilisons \mintinline{rust}|&mut self| à la place de \mintinline{rust}|&self|,
    car cette fois-ci nous voulons rendre la variable mutable pour la modifier.
    \item[La sortie de la fonction :] 
    Ici aussi, nous renvoyons un objet de type \texttt{Option<ContactManagerTxData>} comme précédemment. De plus, la fonction modifie le \texttt{manager pseg} dans \texttt{self}
    pour y mettre les segments nouvellement crées.
    \item[Le fonctionnement algorithmique :] détaillons les étapes de fonctionnement:
    \begin{itemize}
        \item Récupération de la simulation: avec \mintinline{rust}{let out = self.dry_run_tx} \mintinline{rust}{(contact_data, at_time, bundle)?;}, 
        nous mettons dans une variable le résultat de la simulation à suivre.
        \item La boucle: nous utilisons ici une boucle \texttt{while} pour parcourir tous les segments en ignorant ceux qui ne sont pas dans la fenêtre temporelle
        \texttt{tx\_start -- tx\_end}.
        \item Les opérations: sur ces segments, il est possible de faire ces 3 choses:
        \begin{itemize}
            \item La coupe à gauche avec \mintinline{rust}{seg.start < tx_start}: si le segment commence avant le début de la fenêtre, il est scindé en deux parties.
            Nous insèrons un "nouveau" segment gauche intact grâce à \mintinline{rust}{insert(i)} et nous repoussons le début du segment actuel à \texttt{tx\_start}.
            \item La coupe à droite avec \mintinline{rust}{seg.end > tx_end}: le principe est très similaire au cas précédent mais avec le côté droit. Si le segment s'arrête après la fin de notre fenêtre,
            nous créeons un "nouveau" segment droit et nous raccourcissons la fin du segment à \texttt{tx\_end}.
            \item L'écrasement: maintenant que nous possèdons un segment qui rentre dans notre fenêtre, nous pouvons simplement remplacer sa valeur de priorité par celle du paquet à insérer grâce à \mintinline{rust}{self.booking[i].val = bundle.priority}
        \end{itemize}
        \item L'agrégat des segments: après le travail de segmentation, nous parcourons les segments crées par paire (i,j), nous les fusionnons s'ils ont la même priorité et si i se termine quand j commence.
    \end{itemize}
    \item[La complexité en temps: ] le temps de recherche est de l'ordre de \texttt{O(n)}. Les modifications faites pendant cette recherche comme \mintinline{rust}|Vec::insert()| et \mintinline{rust}|Vec::remove()| ont aussi un coût \texttt{O(n)}.
Nous avons donc dans le pire des cas, une complexité \texttt{O(n²)} pour \texttt{schedule\_tx}.
\end{description}

\subsection{HybridParenting}

Dans cette partie, nous allons détailler le fonctionnement algorithmique du module \texttt{HybridParenting},
qui constitue une variante avancée de l'algorithme de Dijkstra implémentant le \textit{Multi-Path Tracking} (MPT).

\subsubsection*{Structures de données}

Avant d'aborder les algorithmes, il est important de comprendre les structures utilisées.
Le module repose sur le trait \texttt{HybridParentingOrd<NM, CM>}, qui impose deux méthodes à implémenter~:
\begin{itemize}
    \item \texttt{can\_retain(prop, known) -> bool}~: détermine si une proposition de route peut être conservée
    même si elle est dominée sur le critère principal. Par exemple dans A-SABR, une route avec
    un nombre de sauts plus faible peut être retenue même si son temps d'arrivée est plus tardif.
    \item \texttt{must\_prune(prop, known) -> bool}~: détermine si une route connue doit être supprimée
    suite à l'insertion d'une proposition.
\end{itemize}


La structure centrale est \texttt{HybridParentingWorkArea<NM, CM>}, qui sert de zone de travail pendant
le calcul des routes. Contrairement au \texttt{PathFindingOutput} classique qui ne stocke qu'une seule
route par destination via un \mintinline{rust}{Vec<Option<SharedRouteStage>>},
la zone de travail maintient un \mintinline{rust}{Vec<Vec<SharedRouteStage>>}~: un vecteur de routes
ordonné par préférence pour chaque destination. Cela permet de suivre plusieurs chemins candidats
simultanément par nœud, ce qui est le principe du MPT. La méthode
\texttt{into\_pathfinding\_output()} convertit ensuite cette structure en \texttt{PathFindingOutput}
standard en ne conservant que le meilleur candidat (index 0) pour chaque destination.

\subsubsection*{Algorithme \texttt{try\_insert}}

La fonction \texttt{try\_insert} est le cœur du mécanisme MPT. Elle tente d'insérer une proposition
de route dans la liste ordonnée des routes connues pour un nœud destination donné.
Sa signature prend deux paramètres~:
\begin{itemize}
    \item \texttt{proposition: RouteStage<NM, CM>}~: la nouvelle proposition de route à évaluer.
    \item \texttt{tree: \&mut HybridParentingWorkArea<NM, CM>}~: la zone de travail contenant
    les listes de routes par destination.
\end{itemize}
Elle retourne un \texttt{Result<Option<SharedRouteStage<NM, CM>>, ASABRError>}.
Le \mintinline{rust}{Option} vaut \mintinline{rust}{Some(route)} si la proposition a été insérée,
\mintinline{rust}{None} sinon. En Rust, \texttt{Result<T, E>} est une énumération qui modélise
la réussite (\mintinline{rust}{Ok(T)}) ou l'échec (\mintinline{rust}{Err(E)}), ici utilisée pour
propager les erreurs d'emprunt (\texttt{ASABRError}).

Passons au corps de l'algorithme. Nous commençons par récupérer la liste des routes connues pour le nœud
destination de la proposition~: \mintinline{rust}{let routes_for_rx_node = &mut tree.by_destination[proposition.to_node as usize]}.
Nous initialisons également deux variables~: \texttt{insert\_index} à la taille de la liste (cas d'insertion
en fin de liste) et \texttt{insert} à \mintinline{rust}{false}.

Si la liste est vide, nous insèrons directement la proposition et nous retournons. Sinon, nous parcourons les routes
existantes avec une boucle \texttt{for} selon trois cas via \mintinline{rust}{match D::cmp(&proposition, &route_borrowed)}~:
\begin{itemize}
    \item \mintinline{rust}{Ordering::Less}~: la proposition est strictement meilleure sur le critère
    principal (par exemple, temps d'arrivée plus court). Nous fixons \texttt{insert\_index = idx} et
    \texttt{insert = true} et nous sortons de la boucle.
    \item \mintinline{rust}{Ordering::Equal}~: doublon détecté, nous posons \texttt{insert = false}
    et nous sortons.
    \item \mintinline{rust}{Ordering::Greater}~: la proposition est moins bonne sur le critère principal.
    Nous appelons alors \mintinline{rust}{D::can_retain(&proposition, &route_borrowed)}~: si vrai,
    la proposition peut malgré tout être utile (critère lexicographique secondaire) et nous continuons
    l'itération~; sinon, \texttt{insert = false} et nous sortons.
\end{itemize}

Si \texttt{insert} est vrai, nous procèdons en trois étapes~:
\begin{enumerate}
    \item \textbf{Détection des routes à éliminer}~: nous cherchons le premier indice \texttt{truncate\_index}
    à partir de \texttt{insert\_index} où \mintinline{rust}{D::must_prune(&proposition, route)} est vrai,
    ce qui délimite les routes que la nouvelle proposition rend obsolètes.
    \item \textbf{Désactivation}~: les routes à partir de \texttt{truncate\_index} sont marquées
    \mintinline{rust}{is_disabled = true} via leur référence partagée \texttt{Rc<RefCell<...>>},
    ce qui permet de les ignorer si elles se trouvent encore dans la file de priorité.
    \item \textbf{Troncature et insertion}~: nous tronquons la liste avec \mintinline{rust}{truncate()}
    puis nous insèrons la proposition à \texttt{insert\_index} avec \mintinline{rust}{Vec::insert()}.
\end{enumerate}

Concernant la complexité de \texttt{try\_insert}~: le parcours de la liste est en \texttt{O(k)}
avec $k$ le nombre de routes stockées pour la destination. Les opérations \mintinline{rust}{Vec::insert()}
et \mintinline{rust}{Vec::truncate()} ont également un coût en \texttt{O(k)}. La complexité totale
de \texttt{try\_insert} est donc en \texttt{O(k)}.

\subsubsection*{Algorithme \texttt{get\_next}}

La méthode \texttt{get\_next} implémente la boucle principale de Dijkstra MPT. Sa signature
est identique à celle du trait \texttt{Pathfinding<NM, CM>} et prend quatre paramètres~:
\begin{itemize}
    \item \texttt{\&mut self}~: référence mutable vers l'instance, nécessaire pour emprunter le graphe.
    \item \texttt{current\_time: Date}~: le temps courant à partir duquel démarrer le calcul.
    \item \texttt{source: NodeID}~: l'identifiant du nœud source.
    \item \texttt{bundle: \&Bundle}~: le paquet à router.
    \item \texttt{excluded\_nodes\_sorted: \&[NodeID]}~: liste triée des nœuds à exclure.
\end{itemize}

L'algorithme débute par l'initialisation~: nous créeons un \texttt{HybridParentingWorkArea} et une
\mintinline{rust}{BinaryHeap<Reverse<DistanceWrapper>>} servant de file de priorité min-heap.
Le nœud source est ajouté à la fois dans \texttt{tree.by\_destination} et dans la file.

La boucle principale \mintinline{rust}{while let Some(Reverse(DistanceWrapper(from_route, _))) = priority_queue.pop()}
extrait la meilleure route courante. Si celle-ci est marquée \mintinline{rust}{is_disabled}, nous la
sautons~: c'est le mécanisme de \textit{lazy deletion} évitant de retirer explicitement des éléments
de la file. Si le mode \textit{path} (non-tree) est activé et que le nœud extrait est la destination,
nous nous arrêtons immédiatement.

Pour chaque nœud \texttt{receiver} accessible depuis \texttt{from\_route}, nous enchaînons trois
appels en \textit{let-chain}~:
\begin{enumerate}
    \item \mintinline{rust}{receiver.lazy_prune_and_get_first_idx(current_time)}~: obtient le premier
    contact non expiré vers ce récepteur.
    \item \mintinline{rust}{try_make_hop(...)}~: construit la proposition de route en calculant le
    temps d'arrivée via le contact sélectionné.
    \item \mintinline{rust}{try_insert::<NM, CM, D>(route_proposition, &mut tree)}~: tente d'insérer
    la proposition dans la zone de travail.
\end{enumerate}
Si les trois réussissent, la nouvelle route est ajoutée à la file de priorité.

En fin de boucle, nous tronquons chaque liste de \texttt{by\_destination} à un seul élément~:
\mintinline{rust}{v.truncate(1)}, puis nous convertissons la zone de travail en \texttt{PathFindingOutput}
via \texttt{into\_pathfinding\_output()}.

Concernant la complexité de \texttt{get\_next}~: le Dijkstra classique sur $V$ nœuds et $E$ arêtes
avec une min-heap a une complexité de $O((V + E) \cdot \log E)$. Ici, chaque relaxation
d'arête appelle \texttt{try\_insert} en $O(k)$, ce qui donne une complexité globale de
$O((V \cdot k + E \cdot k) \cdot \log E)$ soit $O(k \cdot (V + E) \cdot \log E)$.
En pratique, $k$ reste petit (le nombre de routes lexicographiquement non dominées par destination
est généralement faible), ce qui rend l'algorithme très proche du Dijkstra standard.

Le module expose trois variantes générées par la macro \mintinline{rust}{define_mpt!}~:
\texttt{HybridParentingTreeExcl} (arbre complet avec exclusions), \texttt{HybridParentingPath}
(chemin simple sans exclusions) et \texttt{HybridParentingPathExcl} (chemin simple avec exclusions).




\newpage
\section{Analyse des résultats}
\subsection{Benchmark}
Benchmarker une librairie de routage DTN comme A-SABR présente plusieurs défis inhérents.
Contrairement à des algorithmes classiques facilement isolables,
le routage DTN implique une chaîne complète de composants interdépendants
— le parsing du Contact Plan, la construction du Multigraph, le pathfinding, et la gestion des routes — 
ce qui rend difficile l'isolation d'un composant précis pour en mesurer les performances.

Les tentatives de benchmarks réalisées avec \texttt{Criterion} souffrent également du bruit inhérent aux runners GitHub Actions partagés,
où les variations entre deux runs peuvent atteindre ±15\% sans aucun changement de code.
Cela rend la détection de régressions fines impossible en CI.

Une autre tentative, toujours avec \texttt{Criterion} mais exécutée localement, génère des violin plots. Bien que ces graphiques soient lisibles, ils restent peu conclusifs. Les distributions sont larges et se chevauchent 
partiellement, rendant difficile l'affirmation catégorique qu'une variante est strictement meilleure qu'une autre.
Par exemple la comparaison entre Node, Hybrid et Contact Parenting montre des tendances cohérentes avec la théorie,
mais les intervalles de confiance se superposent trop pour conclure avec certitude.

Enfin le choix d'un seul Contact Plan de référence (\texttt{"A-SABR/benches/ptvg\_files/sample1.json"}) 
avec une unique paire source/destination limite la portée des résultats — un seul scénario peut être 
favorable ou défavorable à une variante particulière sans que ce soit représentatif du comportement général 
de la librairie.\\

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{PathFinding.pdf}
    \caption{Comparaison des 3 algos de routage Contact, Node et Hybrid}
    \label{fig:bench_pathfinding}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{Router.pdf}
    \caption{Comparaison entre les deux principaux routers, VolCgr et Spsn}
    \label{fig:bench_router}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.5\textwidth]{CI_Criterion.png}
    \caption{Exemple qui illustre le bruit de la tentative de CI Régression pour un même code}
    \label{fig:ci_Criterion}
\end{figure}

Pour ces raisons, les résultats de benchmark doivent être interprétés avec prudence. Ils permettent d'observer certaines tendances, par exemple entre les variantes Node, Contact et Hybrid Parenting, ou entre VolCGR et SPSN, mais ils ne suffisent pas à établir une hiérarchie définitive entre les approches. La mise en place de benchmarks fiables pour A-SABR reste donc une problématique ouverte, notamment parce qu'elle nécessite de varier les Contact Plans, les couples source/destination, les tailles de bundles et les métriques utilisées.

\subsection{Statistiques}

Afin de compléter l'analyse technique, nous avons synthétisé les contributions réalisées sous forme de Pull Requests. Ces tableaux permettent de distinguer les contributions intégrées, celles encore en cours, ainsi que leur impact sur les différents dépôts. Ils montrent que le travail réalisé ne s'est pas limité à l'ajout de fonctionnalités, mais a aussi porté sur la correction de bugs, la mise en place d'outils de qualité, l'ajout de tests et l'amélioration de la maintenabilité.

\small
\renewcommand{\arraystretch}{1.25}

\begin{longtable}{|p{0.055\textwidth}|p{0.08\textwidth}|p{0.08\textwidth}|p{0.28\textwidth}|p{0.31\textwidth}|p{0.08\textwidth}|}
\hline
\textbf{PR} & \textbf{Auteur} & \textbf{Statut} & \textbf{Contribution principale} & \textbf{Impact sur le projet} & \textbf{Volume} \\
\hline
\endfirsthead

\hline
\textbf{PR} & \textbf{Auteur} & \textbf{Statut} & \textbf{Contribution principale} & \textbf{Impact sur le projet} & \textbf{Volume} \\
\hline
\endhead

\#29 & Samy & Acceptée
& Correction d’un bug dans \textbf{contact.rs}.
& Corrige un comportement fautif localisé dans le module de contact.
& +1 ligne \\
\hline

\#30 & Bastien & Acceptée
& Ajout d’une CI avec \texttt{pre-commit}, \texttt{clippy}, \texttt{cargo test}, génération de coverage, et différenciation entre les Pull Requests et les lancements sur \texttt{main}.
& Améliore la qualité, la robustesse et la maintenabilité du dépôt. Cette PR a nécessité de longs échanges pour aboutir à une version adaptée aux besoins du projet.
& +90 lignes \\
\hline

\#35 & Samy & Acceptée
& Mise à jour de la documentation des fonctions et correction d’un bug dans \textbf{pseg}.
& Améliore la compréhension du module \textbf{PrioritySegmentation} et corrige un défaut fonctionnel.
& +147 lignes \\
\hline

\#38 & Vincent & Acceptée
& Ajout de tests pour Node, Contact et Hybrid Parenting.
& Renforce la couverture de tests du module de pathfinding et valide plusieurs variantes de l’algorithme de routage.
& +886 lignes \\
\hline

\#44 & Vincent & Acceptée
& Refactorisation et simplification des tests, mise en place d’un fichier \textbf{test\_helpers.rs} pour éviter la duplication, mise en conformité des fichiers avec \texttt{clippy} et \texttt{fmt} en vue de la CI.
& Améliore fortement la maintenabilité des tests. La PR a demandé un travail de rebase et de fusion propre avec les refactorisations parallèles effectuées par Théo sur les erreurs A-SABR.
& -409 lignes, +152 lignes \\
\hline

\#51 & Bastien & Acceptée
& Ajout de  tests pour \textbf{PrioritySegmentation} avec légère retouche du code.
& Augmente la couverture de tests de \textbf{pseg} et valide plusieurs comportements du module.
& +556 lignes \\
\hline

\#52 & Samy & Acceptée
& Ajout de tests pour \texttt{try\_make\_hop} et harmonisation du fichier \textbf{test\_helpers.rs} pour le rendre réutilisable par plusieurs fichiers de tests.
& Renforce les tests du pathfinding et améliore la réutilisabilité des outils de test.
& +546 lignes \\
\hline

\#53 & Yassine & Acceptée
& Ajout de  tests pour \textbf{Segmentation}.
& Valide le comportement du module de segmentation, notamment la gestion des intervalles et des transmissions.
& +300 lignes \\
\hline

\#54 & Samy & Acceptée
& Ajout de tests pour les Contact Managers Legacy.
& Vise à compléter la couverture des anciens gestionnaires de contacts et à documenter leurs différences de comportement.
& +502 lignes \\
\hline

\#56 & Bastien & En cours
& Ajout d’une fonctionnalité d’agrégation pour \textbf{PrioritySegmentation} ainsi que des tests associés.
& Réduit la fragmentation des bookings adjacents de même priorité et améliore la cohérence interne du module.
& +44 lignes \\
\hline

\caption{Synthèse des contributions réalisées sur le dépôt A-SABR}
\label{tab:contributions-asabr}
\end{longtable}

\newpage

\begin{center}
\footnotesize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.2}

\begin{longtable}{|p{0.055\textwidth}|p{0.075\textwidth}|p{0.095\textwidth}|p{0.075\textwidth}|p{0.31\textwidth}|p{0.30\textwidth}|p{0.075\textwidth}|}
\hline
\textbf{PR} & \textbf{Partie} & \textbf{Auteur} & \textbf{Statut} & \textbf{Contribution principale} & \textbf{Impact sur le projet} & \textbf{Volume} \\
\hline
\endfirsthead

\hline
\textbf{PR} & \textbf{Partie} & \textbf{Auteur} & \textbf{Statut} & \textbf{Contribution principale} & \textbf{Impact sur le projet} & \textbf{Volume} \\
\hline
\endhead

\#6 & Backend & Vincent & Acceptée
& Modification de \texttt{try\_init}, ajout d’une fonction \texttt{update}, ajout de méthodes pour compter le nombre de nodes et de contacts d’un Contact Plan chargé, et correction du backend pour correspondre au dépôt A-SABR.
& Permet au backend de recharger dynamiquement la configuration utilisée par DT Chat et de fournir des informations utiles au frontend lors du changement de Contact Plan.
& +65 lignes \\
\hline

\#7 & Backend & Vincent & Acceptée
& Correction du backend pour maintenir la compatibilité avec le dépôt A-SABR.
& Assure la compatibilité du backend avec les évolutions de la bibliothèque A-SABR.
& +5 lignes \\
\hline

\#8 & Backend & Bastien, Vincent & En cours
& Ajout d’une CI dérivée de celle d’A-SABR avec \texttt{pre-commit}, \texttt{clippy}, \texttt{fmt} et vérification de la compilation. Correction du backend après la refactorisation des Contact Plans et mise en conformité du dépôt avec \texttt{clippy} et \texttt{fmt}.
& Améliore la robustesse du backend, automatise les vérifications et prépare le dépôt à une meilleure intégration continue.
& +111 lignes \\
\hline

\#13 & Frontend & Vincent & Acceptée
& Ajout d’une vue Options permettant de changer à la volée l’algorithme ou le Contact Plan. Affichage dans l’App Event du nombre de contacts et de noeuds trouvés dans le Contact Plan chargé.
& Rend accessible depuis l’interface graphique le changement dynamique de configuration et permet de détecter plus facilement les Contact Plans mal interprétés.
& +174 lignes \\
\hline

\#14 & Frontend & Vincent, Bastien & En cours
& Ajout d’une CI dérivée de celle d’A-SABR pour vérifier la bonne compilation entre le backend, le frontend et A-SABR, avec \texttt{pre-commit}, \texttt{clippy} et \texttt{fmt}. Mise en conformité du dépôt frontend avec \texttt{clippy} et \texttt{fmt}.
& Doit fiabiliser le dépôt frontend et vérifier que les trois composants restent compatibles entre eux après modification.
& +115 -99 lignes \\
\hline

\caption{Synthèse des contributions réalisées sur DT Chat}
\label{tab:contributions-dtchat}
\end{longtable}
\end{center}

\begin{center}
\footnotesize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.2}

\begin{longtable}{|p{0.34\textwidth}|p{0.20\textwidth}|p{0.20\textwidth}|p{0.16\textwidth}|}
\hline
\textbf{Élément dans \texttt{src/}} & \textbf{Lignes couvertes} & \textbf{Lignes couvrables} & \textbf{Coverage} \\
\hline
\endfirsthead

\hline
\textbf{Élément dans \texttt{src/}} & \textbf{Lignes couvertes} & \textbf{Lignes couvrables} & \textbf{Coverage} \\
\hline
\endhead

\texttt{src/} & 588 & 1949 & 30,2 \% \\
\hline

\texttt{multigraph.rs} & 76 & 86 & 88,4 \% \\
\hline

\texttt{pathfinding/} & 247 & 295 & 83,7 \% \\
\hline

\texttt{distance/} & 34 & 52 & 65,4 \% \\
\hline

\texttt{contact\_manager/} & 186 & 299 & 62,2 \% \\
\hline

\texttt{vnode.rs} & 4 & 16 & 25,0 \% \\
\hline

\texttt{contact.rs} & 11 & 49 & 22,4 \% \\
\hline

\texttt{node.rs} & 4 & 20 & 20,0 \% \\
\hline

\texttt{route\_stage.rs} & 18 & 113 & 15,9 \% \\
\hline

\texttt{node\_manager/} & 3 & 20 & 15,0 \% \\
\hline

\texttt{contact\_plan/} & 5 & 317 & 1,6 \% \\
\hline

\texttt{bundle.rs} & 0 & 6 & 0,0 \% \\
\hline

\texttt{errors.rs} & 0 & 8 & 0,0 \% \\
\hline

\texttt{main.rs} & 0 & 25 & 0,0 \% \\
\hline

\texttt{parsing.rs} & 0 & 68 & 0,0 \% \\
\hline

\texttt{route\_storage/} & 0 & 96 & 0,0 \% \\
\hline

\texttt{routing/} & 0 & 420 & 0,0 \% \\
\hline

\texttt{types.rs} & 0 & 11 & 0,0 \% \\
\hline

\texttt{utils/} & 0 & 48 & 0,0 \% \\
\hline

\caption{Synthèse de la couverture de tests du dossier \texttt{src/} d'A-SABR}
\label{tab:coverage-src-asabr}
\end{longtable}

\end{center}

La couverture globale du dossier \texttt{src/} atteint 30,2 \%. Cette valeur reste modérée, mais elle doit être replacée dans le contexte initial du projet : à notre arrivée, A-SABR ne disposait pratiquement d’aucune couverture de tests automatisés exploitable. Cette absence ralentissait le développement, car chaque modification importante demandait des vérifications manuelles et augmentait le risque de régression. Les tests ajoutés pendant le projet ont donc eu un impact concret : ils ont sécurisé plusieurs modules critiques, facilité les refactorisations et permis aux mainteneurs de reprendre un rythme de développement plus régulier. Ils ont aussi été utiles en situation réelle, en détectant des erreurs lors de modifications récentes du code.

Ainsi, même si la couverture reste perfectible, elle représente une progression importante pour la fiabilité d’A-SABR. Elle met également en évidence les modules encore peu couverts, comme \texttt{routing/} ou \texttt{route\_storage/}.

\newpage
\section{Gestion du Projet}
\subsection{Organisation prévue en début de projet}
Lors du début de notre travail, il était prévu de travailler sur ces 3 tâches:
\begin{itemize}
    \item Les tests et pipelines
    \item La modification de l'interface graphique \texttt{dtchat}
    \item L'implémentation d'A-SABR dans Hardy
\end{itemize}
Il était prévu de suivre le calendrier suivant.
\begin{figure}[h]
    \centering
    \includegraphics[width=0.55\textwidth]{GanttPrevision.png}
    \caption{Diagramme de Gantt prévisionnel}
    \label{fig:GanttDiagramme}
\end{figure}

Concernant la structure de notre GitHub, comme nous devions travailler sur
plusieurs dépôts, nous avons fait le choix de créer un dépôt principal avec à l'intérieur
des sous-modules pointant vers les dépôts sur lesquels nous avons travaillé en laissant à la racine tout ce qui
concerne le côté scolaire du projet notamment le dossier pour le rapport.

Quant à notre manière de travailler, il était prévu de suivre la structure bien connue Gitflow.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.55\textwidth]{ImageGitflow.png}
    \caption{Structure Gitflow}
    \label{fig:Gitflow}
\end{figure}

\subsection{Organisation réelle du travail}
Après avoir débuté notre travail, nous avons rapidement compris que les choix
d'organisation que nous avons fait allaient devoir être adaptés afin 
d'être plus efficace. 
\\[1pt]

Pour commencer, nous voulions suivre une structure Gitflow, donc travailler dans des branches feature/
puis mettre en commun dans la branche develop pour ensuite envoyer une version stable vers notre main puis
vers le dépôt de Monsieur De Jonckère. Mais en commençant notamment nos tests, nous nous sommes vite rendus
compte que la mise en commun dans develop n'était pas nécessaire puisque nos tâches ne dépendaient pas de la réussite des autres.
Cette approche était même plus compliquée à manipuler pour notre encadrant: à chaque Pull Request de notre main
vers le sien, il aurait dû vérifier un très grand nombre de modifications en une seule fois.
\\[0.5pt]

La solution trouvée a donc été la suivante: notre main suivra toujours la branche main du dépôt d'origine par synchronisation automatique,
à chaque nouvel ajout nous créeons une branche depuis le main, sur cette branche nous effectuons un \texttt{git rebase main}
pour récupérer le travail le plus récent et nous commençons à travailler. Quand le travail est jugé satisfaisant,
nous ouvrons une Pull Request vers le main du dépôt d'origine afin d'avoir des retours. Après la prise en compte des suggestions,
la Pull Request est acceptée et notre travail est merge dans le dépôt d'origine.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{Workflow_Github_Groupe_V.png}
    \caption{Structure suivie par le groupe}
    \label{fig:StructureGit}
\end{figure}

Concernant les tâches effectivement réalisées, la majorité des objectifs prévus ont été atteints: modification de l'interface \texttt{dtchat}, ajout de nombreux tests et pipeline performante.
Certains changements de tâches ont quand même eu lieu au cours du projet: le travail d'intégration dans Hardy a été mis de côté
pour laisser la place au travail de BP-Socket jugé plus intéressant pour nous
et pour Monsieur De Jonckère. 

Même si l'objectif BP-Socket n'a été atteint que partiellement par manque de temps, 
le début de travail déjà fait pourrait amener à quelque chose de plus élaboré lors de potentielles
nouvelles contributions en dehors du cadre de projet de programmation.
\\[1pt]

Nous avons dû confronter nos connaissances académiques à celles du monde professionnel afin d'en tirer les conclusions
qui ont le mieux fonctionné pour nous. Même si une petite sensation de désordre s'est fait ressentir en début de parcours, 
elle nous a permis de devenir par la suite plus performants avec les outils Git et de faire preuve d'une meilleure flexibilité
face à un environnement inconnu.
\newpage
\section{Bilan et conclusion}

\subsection{Travaux futurs}

Même si les objectifs principaux du projet ont été atteints, plusieurs pistes restent ouvertes pour poursuivre le travail réalisé.

Un premier axe concerne le module \texttt{routing}. Des premiers tests ont été développés sur une branche de notre dépôt, mais leur intégration n’a pas pu être finalisée dans la version principale du projet. Une refactorisation récente de la gestion des nœuds et des contacts a rendu leur rebase plus complexe que prévu. Ces tests constituent néanmoins une base de travail utile pour renforcer à terme la couverture du module \texttt{routing}, qui reste aujourd’hui l’un des modules les moins couverts par les tests automatisés.

Un deuxième axe concerne la couverture de tests globale d’A-SABR. Les tests ajoutés pendant le projet ont permis de sécuriser plusieurs modules importants, notamment les Contact Managers, la segmentation et le pathfinding. Cependant, certains modules restent encore peu couverts, comme \texttt{routing/}, \texttt{route\_storage/}, \texttt{contact\_plan/} ou \texttt{utils/}. Une poursuite logique du projet consisterait donc à étendre progressivement la couverture de tests à ces parties de la bibliothèque.

Le travail sur DTChat pourrait également être prolongé. L’onglet Options permet désormais de changer dynamiquement d’algorithme ou de Contact Plan, mais l’application ne prend pas encore correctement en charge tous les formats de Contact Plan supportés par A-SABR. Une amélioration future consisterait à rendre le chargement plus robuste, en adaptant le parsing au format du fichier fourni ou en signalant plus clairement les erreurs à l’utilisateur.

Enfin, BP-Socket constitue une autre piste importante. Le remplacement du module kernel par une solution fondée sur \texttt{LD\_PRELOAD} a permis de rendre la partie émission fonctionnelle dans l’environnement testé. La réception des bundles reste cependant à finaliser, notamment pour gérer correctement la lecture des messages et la libération des ressources associées. Ces travaux ne sont toutefois pas perdus : ils seront transmis à un collègue de Monsieur de Jonckère, qui reprend le sujet afin de poursuivre activement le développement de cette solution.
\subsection{Conclusion}

Ce projet nous a permis de contribuer à un écosystème open source réel autour des réseaux \gls{dtn}, en travaillant sur A-SABR, DT-Chat et BP-Socket. Les contributions réalisées ont porté à la fois sur la fiabilisation de l’existant, l’ajout de tests, la mise en place d’outils de qualité logicielle, l’évolution de l’interface graphique et l’expérimentation d’une nouvelle architecture pour BP-Socket.

Au-delà des aspects purement techniques, ce projet a été particulièrement formateur. Nous sommes arrivés sur une base de code déjà existante, active et maintenue, ce qui nous a obligés à adapter nos habitudes de travail. Il a fallu comprendre l’architecture du projet, respecter les conventions existantes, travailler avec des Pull Requests, répondre aux retours des mainteneurs et produire du code suffisamment fiable pour être intégré dans un dépôt public. Cette expérience nous a rapprochés d’un fonctionnement professionnel, bien différent d’un projet universitaire développé isolément.

Voir nos noms apparaître parmi les contributeurs d’un projet open source comme A-SABR représente pour nous une réelle fierté. Au-delà de la validation académique du projet, cela montre que notre travail a pu s’inscrire dans une bibliothèque existante et être utile à son développement. C’est également un honneur d’avoir pu participer, même à notre échelle, à un projet de recherche et de développement lié aux réseaux DTN.

Nous continuerons à suivre l’évolution d’A-SABR et de l’écosystème DTN-MTP avec intérêt. Les travaux réalisés pendant ce projet nous donnent désormais une meilleure compréhension de la bibliothèque, et il n’est pas exclu que nous puissions proposer de nouvelles contributions à l’avenir, notamment sur les tests, le routage ou l’intégration avec les autres outils de l’écosystème.

Nous tenons enfin à remercier Monsieur Olivier de Jonckère pour son encadrement, sa disponibilité et ses retours tout au long du projet. Nous remercions également Théo et Nathael, qui participent eux aussi au développement du projet, pour leurs échanges, leurs retours techniques et leur aide lors de notre prise en main de l’écosystème DTN-MTP.
\vfill
\begin{thebibliography}{3} 

\bibitem{sabr} 
J. A. Fraire, O. De Jonckère, and S. Burleigh \textit{Routing in the space internet: A
contact graph routing tutorial.}, Journal of Network and Computer Applications, 174, 102884.

\bibitem{a-sabr} 
O. De Jonckère, L. Ma, and J. A. Fraire. \textit{ A-SABR: The Adaptive Library for Schedule-Aware Bundle
Routing.}, WiSEE 2025 - IEEE International Conference on Wireless for Space and Extreme Environments, Oct 2025,
Halifax, Canada.

\bibitem{bp} 
Sylvain Pierrot, Olivier de Jonckère, Scott Burleigh, Charley Geoffroy.\textit{ Design and Implementation of a Native Socket Interface for the Bundle Protocol.}
, WiSEE 2025 - 13th Annual IEEE International Conference on Wireless for Space and Extreme Environments, Oct 2025, Halifax, Canada.

\end{thebibliography}
\newpage
\section{Annexe de codes}

\phantomsection
\label{CodeDTBackend}
\ref{DTBackend} (Cliquer sur le chapitre pour revenir au texte correspondant)\\

La fonction \texttt{update} est tiré de DTChat Backend \texttt{"src/dtchat.rs"} ligne 218

La fonction \texttt{try\_init} est tiré de DTChat Backend \texttt{"src/prediction.rs"} ligne 30

\texttt{pred\_res} est tiré de DTChat Backend \texttt{"src/config/mod.rs"} ligne 83

\begin{minted}[frame=lines, framesep=2mm, linenos]{rust}
    pub fn update(&mut self, path:String, algo: &str){
        match PredictionConfig::try_init(path.clone(), algo){
            Ok(update_config) => {
                self.a_sabr = ASabrInitState::Enabled(update_config);
                self.notify_observers(ChatAppEvent::Info(format!("Update done with : {path} and  {algo}")));
            }
            Err(error) => {
                self.a_sabr = ASabrInitState::Error(error.to_string());
                self.notify_observers(ChatAppEvent::Info("Update failed".to_string()));
            }
        }
    }

pub fn try_init(cp_path: String, algo : &str) -> io::Result<Self> {
        let cp = IONContactPlan::parse::<NoManagement, EVLManager>(&cp_path)?;

        let nodes_length = cp.nodes.len();
        let contacts_length = cp.contacts.len();

        let node_index_map: HashMap<String, NodeID> = cp.nodes
            .iter()
            .enumerate()
            .map(|(index, node)| (node.get_node_name().to_string(), index as NodeID))
            .collect();

        let router_box = build_generic_router::<NoManagement, EVLManager>(
            algo,
            cp,
            None,
        ).expect("Failed to build router");

        let router: Box<dyn Router<NoManagement, EVLManager> + Send + Sync> =
            unsafe { std::mem::transmute(router_box) };
        // in seconds
        let cp_start_time = DTChatTime::now().timestamp_millis() as f64 / 1000.0;

        Ok(PredictionConfig {
            ion_to_node_id: node_index_map,
            router,
            cp_start_time,
            nodes_length,
            contacts_length,
        })
    }


let pred_res = PredictionConfig::try_init(cp_path_unwrapped,"VolCgrHybridParenting");
\end{minted}

\newpage

\phantomsection
\label{CodetestPathFinding}
\ref{testPathFinding} (Cliquer sur le chapitre pour revenir au texte correspondant)\\

La fonction de création d'un \gls{mg} \texttt{exemple\_2\_graph} provient de A-SABR \texttt{"src/pathfinding/test\_helpers.rs"} ligne 229.

La fonction \texttt{test\_exemple\_2} qui permet de tester le \gls{mg} précedemment créer grâce à la fonction 
ci dessus est présente dans les trois fichiers \texttt{Node,Contact et Hybrid Parenting} dans A-SABR \texttt{"src/pathfinding"}.

\begin{minted}[frame=lines, framesep=2mm, linenos]{rust}
pub(crate) fn exemple_2_graph()
-> Result<Rc<RefCell<Multigraph<NoManagement, EVLManager>>>, ASABRError> {
    Ok(Rc::new(RefCell::new(Multigraph::new(ContactPlan::new(
        vec![
            make_node(0, "source", NoManagement {}),
            make_node(1, "from_C0", NoManagement {}),
            make_node(2, "from_C2_C1", NoManagement {}),
            make_node(3, "from_C3", NoManagement {}),
            make_node(4, "from_C4", NoManagement {}),
        ],
        vec![
            make_contact::<NoManagement>(0, 1, 0.0, 10.0, 1.0, 0.0),
            make_contact::<NoManagement>(0, 2, 25.0, 35.0, 1.0, 0.0),
            make_contact::<NoManagement>(1, 2, 10.0, 20.0, 1.0, 0.0),
            make_contact::<NoManagement>(2, 3, 20.0, 40.0, 1.0, 0.0),
            make_contact::<NoManagement>(3, 4, 50.0, 60.0, 1.0, 0.0),
        ],
        None,
    )?)?)))
}

fn test_exemple_2() -> Result<(), ASABRError> {
        let mg = exemple_2_graph()?;

        let mut algo_hop = NodeParentingPath::<NoManagement, EVLManager, Hop>::new(mg.clone());
        let mut algo_sabr = NodeParentingPath::<NoManagement, EVLManager, SABR>::new(mg.clone());

        let bundle = make_bundle(4, 0, 0.0, 1000.0);

        let res_hop = algo_hop
            .get_next(0.0, 0, &bundle, &[][..])
            .expect("Routing Failed !");

        assert_time_hop(&res_hop, 4, 50.0, 3, "Hop");

        let res_sabr = algo_sabr
            .get_next(0.0, 0, &bundle, &[][..])
            .expect("Routing Failed !");

        assert_time_hop(&res_sabr, 4, 50.0, 4, "SABR");

        Ok(())
    }
}
\end{minted}

\newpage
\phantomsection
(Code du chapitre \ref{DTFrontend})\\
\label{CodeDTFrontend}

Entièreté du code de la vue Options de DT Chat Egui \texttt{"src/options\_view.rs"}

\begin{minted}[frame=lines, framesep=2mm, linenos]{rust}
    use crate::main_view::MirroredData;
use eframe::egui;
use egui_file_dialog::FileDialog;


pub struct OptionsView{
    pub selected_algo: String,
    pub selected_cp_path: String,
    file_dialog: FileDialog,
}

impl OptionsView{
    pub fn new() -> Self {
        Self {
            selected_algo: "VolCgrHybridParenting".to_string(),
            selected_cp_path: "./db/ion.cp".to_string(),
            file_dialog: FileDialog::new(),
        }
    }
    pub fn show(&mut self,ui: &mut egui::Ui, _data: &mut MirroredData) -> Option<(String,String)>{
        self.file_dialog.update(ui.ctx());
        let mut update = false;
        ui.vertical(|ui| {
            ui.add_space(5.0);
            ui.heading("Options");
            ui.add_space(5.0);
            ui.separator();
            ui.add_space(15.0);

            //Contact Plan
            ui.horizontal(|ui| {
                ui.label("Change Contact Plan :");
                if ui.button("Explore ").clicked() {
                    self.file_dialog.pick_file();
                    }
            });

            if let Some(path) = self.file_dialog.take_picked(){
                self.selected_cp_path = path.display().to_string();
                update = true;
            }

            ui.add_space(10.0);

            //Algorithme
            ui.horizontal(|ui| {
                ui.label("Change Algo :");
                let old_algo = self.selected_algo.clone();
                
                let mut algos: Vec<&str> = vec![
                    "VolCgrNodeParenting",
                    "VolCgrHybridParenting",
                    "VolCgrHybridParentingHop",
                    "VolCgrNodeParentingHop",
                ];

                #[cfg(feature = "contact_work_area")]
                algos.extend([
                    "VolCgrContactParenting",
                    "VolCgrContactParentingHop",
                ]);

                #[cfg(feature = "contact_suppression")]
                algos.extend([
                    "CgrFirstEndingHybridParentingHop",
                    "CgrFirstEndingHybridParenting",
                    "CgrFirstEndingNodeParentingHop",
                    "CgrFirstEndingNodeParenting",
                ]);

                #[cfg(all(feature = "contact_work_area", feature = "contact_suppression"))]
                algos.extend([
                    "CgrFirstEndingContactParenting",
                    "CgrFirstEndingContactParentingHop",
                ]);

                #[cfg(all(feature = "contact_suppression", feature = "first_depleted"))]
                algos.extend([
                    "CgrFirstDepletedHybridParentingHop",
                    "CgrFirstDepletedHybridParenting",
                    "CgrFirstDepletedNodeParentingHop",
                    "CgrFirstDepletedNodeParenting",
                ]);

                #[cfg(all(feature = "contact_work_area", feature = "contact_suppression", feature = "first_depleted"))]
                algos.extend([
                    "CgrFirstDepletedContactParentingHop",
                    "CgrFirstDepletedContactParenting",
                ]);

                egui::ComboBox::from_id_salt("algo_selector")
                    .selected_text(&self.selected_algo)
                    .show_ui(ui, |ui| {
                        for algo in algos{
                            ui.selectable_value(&mut self.selected_algo, algo.to_string(), algo);
                        }
                    });
                if old_algo != self.selected_algo{
                    update = true;
                }
            });
        });
        if update{
            Some((self.selected_cp_path.clone(),self.selected_algo.clone()))
        } else{
            None
        }
    }
}
\end{minted}

\newpage
\phantomsection
\label{CodeCIprecommit}
\ref{CI/CD} (Cliquer sur le chapitre pour revenir au texte correspondant)
Code complet du fichier \texttt{.pre-commit-config.yaml}.
\begin{minted}[frame=lines, framesep=2mm, linenos]{yaml}
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files

  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        entry: cargo fmt --all -- --check
        language: system
        types: [rust]
        pass_filenames: false

      - id: cargo-clippy
        name: cargo clippy
        entry: cargo clippy --all-targets --all-features -- -D warnings
        language: system
        types: [rust]
        pass_filenames: false
\end{minted}

\newpage
\phantomsection
\label{CodeCIyml}
\ref{CI/CD}
Code complet de la version finale du fichier \texttt{ci.yml}.
\begin{minted}[frame=lines, framesep=2mm, linenos]{yaml}
name: A-SABR CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint and Style (Pre-commit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: swatinem/rust-cache@v2
      - uses: pre-commit/action@v3.0.1

  tests:
    name: Run Tests
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
      - name: Run Tests
        run: cargo test --all-features --all-targets

  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    needs: tests
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
      - uses: taiki-e/install-action@cargo-tarpaulin
      - run: cargo tarpaulin --ignore-tests --out Html
      - uses: actions/upload-artifact@v4
        with:
          name: code-coverage-report
          path: tarpaulin-report.html
          retention-days: 7
\end{minted}

\newpage
\phantomsection
\label{CodeStartTest}
\ref{pseg}
Code de la fonction \texttt{start\_test}.
\begin{minted}[frame=lines, framesep=2mm, linenos]{rust}

    #[derive(Debug, PartialEq, Clone)]
    enum InputSeg {
        Delay(Date, Date, Duration),
        Rate(Date, Date, DataRate),
        Booking(Date, Date, Priority),
    }

    #[derive(Debug, PartialEq, Clone)]
    enum OutputSeg {
        Booking(Date, Date, Priority),
    }
    #[track_caller]
    fn start_test(
        input: Vec<InputSeg>,
        output: Vec<OutputSeg>,
        requests: Vec<(Bundle, f64, bool)>,
    ) {
        let contact_info = ContactInfo::new(0, 1, 0.0, 200.0);
        let mut delay_segments: Vec<Segment<Date>> = Vec::new();
        let mut rate_segments: Vec<Segment<DataRate>> = Vec::new();
        let mut actual_output = Vec::new();
        let mut initial_bookings: Vec<Segment<Priority>> = Vec::new();

        for seg in input {
            match seg {
                InputSeg::Delay(start, end, val) => {
                    delay_segments.push(Segment { start, end, val })
                }
                InputSeg::Rate(start, end, val) => rate_segments.push(Segment { start, end, val }),
                InputSeg::Booking(start, end, priority) => initial_bookings.push(Segment {
                    start,
                    end,
                    val: priority,
                }),
            }
        }
        let mut manager = PSegmentationManager::new(rate_segments, delay_segments);
        manager.try_init(&contact_info);
        if !initial_bookings.is_empty() {
            manager.booking = initial_bookings;
        }

        for (i, (bundle, at_time, expect_success)) in requests.iter().enumerate() {
            let dry_run_res = manager.dry_run_tx(&contact_info, *at_time, bundle);
            let schedule_tx_res = manager.schedule_tx(&contact_info, *at_time, bundle);

            assert_eq!(
                dry_run_res, schedule_tx_res,
                "TEST N°{} FAILED: dry_run and schedule_tx doesn't match.\n",
                i
            );
            assert_eq!(
                schedule_tx_res.is_some(),
                *expect_success,
                "TEST N°{} FAILED: expected: {} actual: {}",
                i,
                expect_success,
                schedule_tx_res.is_some()
            );
        }

        //Building actual output

        for seg in &manager.booking {
            actual_output.push(OutputSeg::Booking(seg.start, seg.end, seg.val));
        }
        assert_eq!(
            actual_output, output,
            "TEST FAILED: Actual output is not the one expected."
        );
    }
\end{minted}

\newpage
\phantomsection
\label{CodeHook}
\ref{Result_Hook}
Code complet du hook BP Socket \texttt{"bp\_socket/interceptor.c"}.
\begin{minted}[frame=lines, framesep=2mm, linenos]{C}
#define _GNU_SOURCE
#include "bp_ipc.h"
#include "bp_socket.h"
#include "fd_table.h"
#include <dlfcn.h>
#include <sys/socket.h>
#include <sys/un.h>
#include <unistd.h>

static int (*real_socket)(int, int, int) = NULL;
static int (*real_close)(int) = NULL;
static int (*real_bind)(int, const struct sockaddr*, socklen_t) = NULL;
static ssize_t (*real_sendto)(int, const void*, size_t, int, const struct sockaddr*, socklen_t) = NULL;

static void __attribute__((constructor)) init()
{
	real_socket = dlsym(RTLD_NEXT, "socket");
	real_close = dlsym(RTLD_NEXT, "close");
	real_bind = dlsym(RTLD_NEXT, "bind");
	real_sendto = dlsym(RTLD_NEXT, "sendto");
}

static int connect_to_daemon()
{
	struct sockaddr_un addr;
	int fd = real_socket(AF_UNIX, SOCK_STREAM, 0);
	if (fd < 0)
		return -1;

	addr.sun_family = AF_UNIX;
	strncpy(addr.sun_path, BP_IPC_SOCKET_PATH, sizeof(addr.sun_path));

	if (connect(fd, (struct sockaddr*)&addr, sizeof(addr)) < 0) {
		real_close(fd);
		return -1;
	}
	return fd;
}

int socket(int domain, int type, int protocol)
{
    if (domain != AF_BP)
        return real_socket(domain, type, protocol);

    int unix_fd = connect_to_daemon();
    if (unix_fd < 0) {
        return -1;
    }

    fd_table_add(unix_fd, unix_fd, 0, 0);
    return unix_fd;
}

int bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen)
{
    if (!is_bp_fd(sockfd))
        return real_bind(sockfd, addr, addrlen);

    struct sockaddr_bp *bp_addr = (struct sockaddr_bp *)addr;
    bp_fd_entry_t *entry        = fd_table_get(sockfd);

    entry->node_id    = bp_addr->bp_addr.ipn.node_id;
    entry->service_id = bp_addr->bp_addr.ipn.service_id;

    bp_ipc_msg_t msg = {
        .cmd            = BP_IPC_OPEN_ENDPOINT,
        .src_node_id    = entry->node_id,
        .src_service_id = entry->service_id,
        .payload_len    = 0,
    };
    write(entry->unix_fd, &msg, sizeof(msg));

    bp_ipc_response_t resp;
    read(entry->unix_fd, &resp, sizeof(resp));

    if (resp.status == BP_IPC_OK)
        return 0;

    return -1;
}

int close(int fd)
{
    if (!is_bp_fd(fd))
        return real_close(fd);

    bp_fd_entry_t *entry = fd_table_get(fd);

    bp_ipc_msg_t msg = {
        .cmd            = BP_IPC_CLOSE_ENDPOINT,
        .src_node_id    = entry->node_id,
        .src_service_id = entry->service_id,
        .payload_len    = 0,
    };
    write(entry->unix_fd, &msg, sizeof(msg));

    bp_ipc_response_t resp;
    read(entry->unix_fd, &resp, sizeof(resp));

    fd_table_remove(fd);
    real_close(fd);

    return 0;
}

ssize_t sendto(int sockfd, const void* buf, size_t len, int flags,
    const struct sockaddr* dest_addr, socklen_t addrlen)
{
	if (!is_bp_fd(sockfd))
		return real_sendto(sockfd, buf, len, flags, dest_addr, addrlen);

	struct sockaddr_bp* bp_dest = (struct sockaddr_bp*)dest_addr;
	bp_fd_entry_t* entry = fd_table_get(sockfd);

	bp_ipc_msg_t msg = {
		.cmd = BP_IPC_SEND_BUNDLE,
		.src_node_id = entry->node_id,
		.src_service_id = entry->service_id,
		.dst_node_id = bp_dest->bp_addr.ipn.node_id,
		.dst_service_id = bp_dest->bp_addr.ipn.service_id,
		.flags = (uint32_t)flags,
		.payload_len = (uint32_t)len,
	};
	write(entry->unix_fd, &msg, sizeof(msg));
	write(entry->unix_fd, buf, len);

	bp_ipc_response_t resp;
	read(entry->unix_fd, &resp, sizeof(resp));

	if (resp.status == BP_IPC_OK)
		return (ssize_t)len;

	return -1;
}
\end{minted}

\newpage
\phantomsection
\label{PsegDR}
\ref{CodeDRpseg}
Code de la fonction dry\_run\_tx de pseg
\begin{minted}[frame=lines, framesep=2mm, linenos]{rust}
    fn dry_run_tx(
        &self,
        contact_data: &ContactInfo,
        at_time: Date,
        bundle: &Bundle,
    ) -> Option<ContactManagerTxData> {
        let mut tx_start = at_time;
        let mut tx_end_opt: Option<Date> = None;

        for seg in &self.booking {
            // Allows to advance to the first valid segment
            if seg.end <= at_time {
                continue;
            }

            // Segment is not valid, we need to reset the building process with the next segment
            if bundle.priority <= seg.val {
                tx_end_opt = None;
                continue;
            }
            // Start building or pursue ?
            match tx_end_opt {
                // Try to pursue the build process
                Some(tx_end) => {
                    // the seg is valid, check if this is the last one to consider
                    if tx_end <= seg.end {
                        let (d_start, d_end) =
                            super::get_delays(tx_start, tx_end, &self.delay_intervals);
                        return Some(ContactManagerTxData {
                            tx_start,
                            tx_end,
                            expiration: seg.end,
                            rx_start: tx_start + d_start,
                            rx_end: tx_end + d_end,
                        });
                    }
                    // if we reach this point, the seg is valid, but transmission didn't reach terminaison, check next
                }
                // (re)-start the build process
                None => {
                    tx_start = Date::max(seg.start, at_time);
                    // In most cases, there should be a single rate seg
                    if let Some(tx_end) = super::get_tx_end(
                        &self.rate_intervals,
                        tx_start,
                        bundle.size,
                        contact_data.end,
                    ) {
                        if tx_end <= seg.end {
                            let (d_start, d_end) =
                                super::get_delays(tx_start, tx_end, &self.delay_intervals);
                            return Some(ContactManagerTxData {
                                tx_start,
                                tx_end,
                                expiration: seg.end,
                                rx_start: tx_start + d_start,
                                rx_end: tx_end + d_end,
                            });
                        }
                        tx_end_opt = Some(tx_end);
                    };
                }
            }
        }
        None
    }
\end{minted}


\newpage
\phantomsection
\label{PsegSC}
\ref{CodeSCpseg}
Code de la fonction schedule\_tx de pseg
\begin{minted}[frame=lines, framesep=2mm, linenos]{rust}
    fn schedule_tx(
        &mut self,
        contact_data: &ContactInfo,
        at_time: Date,
        bundle: &Bundle,
    ) -> Option<ContactManagerTxData> {
        let out = self.dry_run_tx(contact_data, at_time, bundle)?;
        let tx_start = out.tx_start;
        let tx_end = out.tx_end;

        let mut i = 0;
        while i < self.booking.len() {
            let seg = &self.booking[i];

            // Segment completely before tx window
            if seg.end <= tx_start {
                i += 1;
                continue;
            }

            // Segment completely after tx window
            if seg.start >= tx_end {
                break;
            }

            let old_prio = seg.val;

            // Cut before
            if seg.start < tx_start {
                let left = Segment {
                    start: seg.start,
                    end: tx_start,
                    val: old_prio,
                };
                self.booking.insert(i, left);
                self.booking[i + 1].start = tx_start;
                i += 1;
            }

            // Cut after
            if self.booking[i].end > tx_end {
                let right = Segment {
                    start: tx_end,
                    end: self.booking[i].end,
                    val: old_prio,
                };
                self.booking.insert(i + 1, right);
                self.booking[i].end = tx_end;
            }

            // Assign TX priority
            self.booking[i].val = bundle.priority;
            i += 1;
        }

        //Checking if  same priority segments can be fused
        let mut j = 0;
        while j < self.booking.len() - 1 {
            if self.booking[j].val == self.booking[j + 1].val
                && self.booking[j].end == self.booking[j + 1].start
            {
                self.booking[j].end = self.booking[j + 1].end;
                self.booking.remove(j + 1);
            } else {
                j += 1;
            }
        }

        Some(out)
    }
\end{minted}


\end{document}